---
seq: 24
commit: null
status: not-taken
behaviour: A
migration: M0
flags: []
title: Index maintenance on extras_objectchange, priced at a ~0 ceiling
site: to be determined from pg_stat_user_indexes
summary: 'extras_objectchange carries at least eleven indexes, every one maintained on every
  insert, and it is the highest-insert-volume table in Nautobot. The write path is where the
  remaining cost sits: bulk.create.x100 is still 1,404 queries dominated by those inserts,
  and nothing on this branch has looked at index maintenance as a cost.'
result:
  ceiling: 'dropping all 17 non-unique indexes: wall 3358 -> 3300ms median, -59ms (-1.7%),
    db_ms 776 -> 752ms, on bulk.create.interfaces.x100.deferred'
  observed: 18 non-primary indexes on extras_objectchange, 17 of them non-unique
wall_clock: −1.7% ceiling, inside the ordering artifact
wall_clock_detail: 'Three rounds each way, snapshot restore before every arm, arm order reversed
  between rounds: with 3434/3307/3358ms, without 3296/3414/3300ms.


  The medians differ by 59ms, and in all three rounds the arm that ran SECOND was faster by
  60-130ms regardless of which arm it was. The difference and the artifact are the same size,
  so the real cost of index maintenance here is indistinguishable from zero.


  Consistent with the rest of the branch: db_ms is only ~780ms of a 3358ms operation, and
  index maintenance is a fraction of that. The cost on this path is Python-side.'
caveat: null
note: 'Scoped to non-unique indexes deliberately, and the reason is worth keeping. The first
  attempt tried to drop all 18 and failed: extras_objectchange_time_request_id_changed__f5f870cc_uniq
  backs a UNIQUE constraint, and PostgreSQL refuses to drop such an index directly. That is
  the right boundary rather than an obstacle -- a unique index is a constraint, not an optimization,
  so dropping one removes a guarantee and is a behaviour change rather than index tidying.


  It also corrects the count this record used to carry. It said eleven indexes; that was the
  never-scanned subset from a pg_stat reading. The table has 18 non-primary indexes, 17 non-unique.


  Adding indexes remains a dead end here and that stays measured rather than assumed: 325,152
  queries totalling 4.6 seconds, mean 0.014ms, and query count fell 57% while total DB time
  went 601ms to 602ms. Both directions on this table are now priced.'
reason: Measured at a 1.7% ceiling that is the same magnitude as the run-ordering artifact,
  from dropping every non-unique index on the table. There is nothing to win, so the question
  of which indexes are safe to drop -- which our 3.6% endpoint coverage makes hard to answer
  -- does not need answering.
