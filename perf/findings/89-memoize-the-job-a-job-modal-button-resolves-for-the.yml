---
seq: 89
id: null
commit: 5d3ef8031
recommended_commit: e787131d5
status: accepted
behaviour: B1
migration: '-'
flags: []
title: Memoize the Job a job-modal button resolves, for the duration of the request
site: nautobot/core/ui/object_detail.py `_JobModalButton.build_trigger_context` / `_get_job`
summary: '`_JobModalButton.build_trigger_context()` resolved its Job with
  `Job.objects.restrict(user, "view").get_for_class_path(...)` on every rendered trigger, to decide
  whether the trigger is disabled. `export_button` builds a trigger for the main button and one
  more per ExportTemplate on that content type, so the identical permission-filtered query ran
  **1 + N times per list view**. The device lists show N=1 here because this dataset holds one
  ExportTemplate and it is on `dcim.device`.


  The lookup is a pure function of `(class_path, user)`, so it memoizes into the `request_cache()`
  scope the middleware already opens around every request. The `Job.DoesNotExist` outcome is
  memoized too, or a disabled trigger goes on querying once per render.


  **This removes a duplicate, not the lookup.** It is the smaller half of finding 88 and does not
  undo it.'
result:
  queries: 'Tier 1, 57 scenarios: **901 -> 897**. Exactly four scenarios moved, each -1 --
    ui.device.list 11 -> 10, ui.device.list.deep 12 -> 11, ui.device.list.filtered 13 -> 12,
    ui.device.list.page100 12 -> 11. Zero regressions, zero endpoints lost 200/302, zero
    `query_count_stable` went false.'
  the_isolating_evidence: 'A shape probe over 46 `ui.*` scenarios attributes the whole delta to one
    normalised SQL shape, `SELECT ... FROM "extras_job" WHERE (job_class_name = ? AND module_name =
    ?) LIMIT ?`, going **2 -> 1** on those four scenarios and nothing else. Total shapes across the
    46 scenarios 738 -> 734.'
  what_it_does_not_reach: 'The same probe counts how many triggers each list view renders: **14
    render one, 4 render two.** A per-request memo can only collapse a repeat, so **18 of the 22
    queries upstream #9501 added remain** and are not addressable this way.'
wall_clock: '−2.8% ui.device.list.page100, −1.5% ui.device.list, at the instrument''s resolution floor'
wall_clock_detail: 'Three alternating rounds with the arm order reversed (A/B, B/A, A/B), n=60 c=1
  under `measure.sh` on a quiesced box, medians of `server_ms_median`: ui.device.list 167.5 ->
  165.0 (−1.5%), page100 178.0 -> 173.0 (−2.8%), deep 175.0 -> 173.0 (−1.1%), filtered 169.0 ->
  167.5 (−0.9%).


  **Read weakly.** cassowary reports integer milliseconds, so on a ~170ms page the resolution floor
  is ~0.6% and these deltas are 1 to 5ms. The direction is consistent across all four targets and
  larger than every control, but the deterministic counter is the result and this is corroboration.
  On `server_ms_mean` the targets and the controls cannot be separated at all.'
basis: 'one SQL shape, 2 -> 1, on the four scenarios whose content type has an ExportTemplate'
controls: 'ui.device.detail +0.0% (252.0ms both arms), ui.location.list −0.3%, api.device.list
  −0.7%. ui.location.list is the sharp one: it renders the same trigger through the same code path
  and its counter does not move, so it prices what this instrument reads on a page the change
  cannot help. Arms proved distinct by content hash on every timed run -- stock
  `33f4cb8930f61178`, memo `cb8e6a4ec415f1e5` -- and arms were toggled through a git worktree,
  never `git stash` and never a path-scoped checkout. All 42 measurements probed 200, zero
  failures.'
tests: '2,116 tests, zero failures. 268 OK across `nautobot.core.tests` test_ui, test_buttons,
  test_import_export and test_templatetags_ui_framework; 1,848 OK (skipped=53) across
  `nautobot.core.tests.test_views` and `nautobot.extras.tests.test_views`. `_JobModalButton` has
  direct coverage in `nautobot/core/tests/test_ui.py`. Every label was confirmed to name a real
  module before running -- an unresolvable label prints FAILED (errors=1), runs the remaining
  labels and still exits 0.'
caveat: 'The memoized value is **permission-filtered**, so the B1 argument rests entirely on the
  memo''s scope being one request and one user. That holds because `request_cache()` is opened in
  exactly one place in the product tree, `nautobot/core/middleware.py:117`, which wraps a single
  HTTP request. An app or a future caller that opened a `request_cache()` scope around anything
  broader -- a job run iterating users, a management command, a batch -- would make a Job''s
  view-permission decision outlive the user it was computed for. Anyone adding a second opener
  needs to read this.


  `None` and an `AnonymousUser` both have no pk and are deliberately keyed differently, because
  `user=None` skips the object-level restriction entirely and is therefore not the same lookup.


  Nothing else changes: same query, same disabled state, same rendered markup. The disabled-reason
  branches are reordered to read from a returned `None` rather than from a caught exception, and
  are otherwise identical.'
note: '**What this is worth scales with the deployment, and this dataset is the floor.** The memo
  keys on `(class_path, user)` and every one of the `1 + N` calls per list view resolves the same
  class path, so it collapses all of them to one. Here N is 0 on 17 of 18 list views and 1 on
  `dcim.device`, which is why the measured saving is 4 queries. A deployment with five
  ExportTemplates on a model saves five on that model''s list view, twenty saves twenty. The
  measured figure is not a ceiling and should not be read as one.


  **This is the small half of finding 88, and it is worth being plain that it does not close
  it.** #9501 added 22 queries across the 57-scenario read loop; this returns 4. The other 18 are
  one lookup per list view with nothing to collapse them against.


  **What would reach them, and why it was rejected.** A cross-request cache -- Redis via
  `cache_get_or_set()` -- is the only option that touches all 18. Its key must include the user,
  and its invalidation would have to cover Job enable/disable, permission grants and group
  membership. That is an unbounded staleness window on an authorization decision: B2 plus
  `security-visible`, and the same shape as the per-user nav-menu cache this branch already
  rejected (finding 19). Not measured, rejected on risk.


  **Also rejected: a per-instance memo** (`self._job`), which produces the identical −4.
  `_JobModalButton` instances are module-level singletons registered in
  `registry["job_modal_buttons"]` at import, so an instance attribute outlives every request and
  would serve the first user''s permission-filtered result to every subsequent user for the life of
  the process. Same counter, B2 plus `security-visible` instead of B1.


  **And: not building a trigger per ExportTemplate**, which fixes the duplication at source for
  the same −4 here. Rejected as tier C against tier B1 for an identical counter on this dataset --
  it changes rendered output, removing the per-template export actions, and this does not.


  The commissioning brief for this experiment predicted the +1 would disappear from every list
  view. It was wrong, and the shape probe is what established that rather than any reading of the
  code.'
