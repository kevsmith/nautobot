# Nautobot Performance Experiments

A measured account of Nautobot core's read and write paths, and the ranked findings that come
out of it. Every number here was produced by a reproducible harness against a fixed dataset.

| | |
| --- | --- |
| Branch | `next` · 3.3.0a0 |
| Dataset | databot `enterprise-campus / large / seed 42` · 24,091 objects |
| Read scenarios | 38 |
| Write operations | 13 |
| Findings recorded | 32 |
| Accepted | 20 |
| Queries | **2,235 → 695** (-68.9%) |
| Since the round-two baseline | 957 → 695 |

> Generated by `perf/build_report.py` from `perf/findings/` and `perf/baselines/`. Do not edit this file.
>
> Sources: tree `de96a97c4` with 0 dirty path(s); read-path queries from `perf/baselines/uwsgi-tier1-current.json` against `large-tier1-baseline.json`; write path from `uwsgi-tier1w-baseline.json`; in-process wall clock from `uwsgi-bench-r{1,2,3}.json`; HTTP wall clock from `uwsgi-tier2-c1-r{1,2,3}.json`, concurrency 1, median.

## What was measured

An isolated stack on port 8180 with `DEBUG=False`, no Apps enabled, and pinned CPU and
memory, so results reflect production-shaped code paths and every finding is attributable to
core. The primary dataset is databot's `enterprise-campus / large / seed 42`: **24,091
objects** — 2,902 devices, 8,925 interfaces, 3,278 cables, 110 locations, 595 prefixes, 2,937
IP addresses and 36,552 existing ObjectChange records, applied over REST in 1,013 seconds.

Round one ran against `enterprise-campus / small / seed 42` (2,113 objects). Those numbers
are kept below as history, clearly separated, because they are the reference the first four
commits were measured against. Everything presented as current is large-dataset.

Four instruments, because the failure modes differ:

- **Query profiling** drives each scenario through the Django test client and counts SQL.
  Deterministic — it repeated **2,235 → 2,235** across runs, and reproduced exactly on a
  second machine with a different CPU architecture, which is what makes it usable as a
  regression gate.
- **Write-path profiling** runs each operation inside a real change context and a rolled-back
  transaction, so change logging and signals behave as they would for a live write.
- **Redis backend read counting**, because one endpoint was making more cache round-trips
  than database queries and no SQL-shaped tool could see it.
- **Wall-clock timing**, in-process and over HTTP, because some costs remove no queries at
  all.

The workload is hand-maintained rather than auto-generated. Endpoints are named by Django
view name and resolved through `reverse()`, and objects are selected by strategy at run time
— never a baked-in primary key — so the workload survives a reseed and fails loudly if a view
is renamed.

> ### The database is not the bottleneck
>
> Across a full baseline run, PostgreSQL executed **325,152 queries in 4.6 seconds total** — a
> mean of 0.014 ms each. The most-repeated query in the worst endpoint averaged **0.004 ms**.
> Every second of user-visible latency measured here is Python-side: ORM round-trip overhead,
> serialization, and repeated work per object.
>
> This has a practical consequence. Adding indexes would buy almost nothing at this scale, and
> the entire set of findings below is addressable in application code.
>
> The completed work confirms it from the other direction. Across the 38 scenarios, query
> count fell **57%** while total measured database time was **unchanged — 601 ms before,
> 602 ms after**. The 1,278 queries removed were worth almost nothing in SQL; what they cost
> was Python-side per-query overhead and the serialization work wrapped around them.

## Baseline — read path

Ten most expensive read scenarios of 38, on the large baseline (pristine tree, 24,091
objects). Duplicates are repeated query shapes after literal normalization.

| Scenario | Queries | Duplicate |
|---|---:|---:|
| `api.interface.depth1` | 1,229 | 1,198 |
| `api.device.list.depth1` | 112 | 101 |
| `ui.device.detail` | 108 | 55 |
| `ui.device.detail.worst` | 104 | 49 |
| `ui.rack.detail` | 99 | 45 |
| `ui.home` | 79 | 32 |
| `ui.prefix.detail` | 79 | 34 |
| `ui.device.interfaces` | 71 | 34 |
| `ui.location.detail` | 68 | 27 |
| `api.interface.list` | 23 | 3 |

All 38 endpoints returned 200, and every query count repeated exactly across runs. List views
are efficient at 9–10 queries each; the cost concentrates in nested API serialization and in
detail pages.

The last row is the outlier worth noting: `api.interface.list` issues only 23 queries yet was
the third-slowest endpoint on the branch — no N+1 at all, purely CPU-bound work per row. It is
the row that shaped the whole exercise, because no query-count instrument can see it.

## Baseline — write path

Each operation measured inside a change context and rolled back, on the large baseline.
"Changes" counts ObjectChange records created.

| Operation | Queries | Wall | Changes |
|---|---:|---:|---:|
| `create.device` | 34 | 19.6 ms | 1 |
| `update.device.name` | 35 | 19.9 ms | 1 |
| **`update.device.noop`** | **35** | 19.9 ms | 1 |
| `delete.device` | 174 | 68.2 ms | 2 |
| `create.interface` | 20 | 15.1 ms | 1 |
| `create.ipaddress` | 22 | 12.9 ms | 1 |
| `create.tag` | 10 | 8.3 ms | 1 |
| `bulk.create.x100.loop` | 1,604 | 1,255 ms | 100 |
| `bulk.create.x100.deferred` | 1,507 | 1,115 ms | 100 |
| **`bulk.create.x100.bulk_create`** | **3** | 9.4 ms | 0 |
| `bulk.update.x100.deferred` | 1,675 | 1,152 ms | 49 |
| `bulk.delete.x100` | 2,222 | 1,191 ms | 50 |

Two rows carry the story. `update.device.noop` — a save that changes no field — costs the same
35 queries as a real update, because change logging runs regardless. And the bulk triple
isolates what change logging costs: the same 100 rows take 1,604 queries with logging inline,
1,507 with logging deferred, and **3** with signals and validation bypassed entirely.

Deferral is the mechanism Nautobot's own bulk-edit views use, and it removes only 6% of
queries. Independently, `INSERT INTO extras_objectchange` is the single largest consumer of
database time across a whole run at **479 ms over 4,879 calls** — 10.4% of all time spent in
PostgreSQL. (That `pg_stat_statements` capture is from the round-one run and has not been
retaken at large scale; it is the one figure on this page whose dataset does not match the
tables around it.)

## Findings

Every experiment, in order. The tier columns are a price tag rather than a filter -- nothing here is disqualified for being expensive, it is labelled so the price is visible. `perf/README.md` defines the taxonomy.

| # | Change | Tier | Wall clock | Queries | Cache reads | Status |
|---:|---|---|---|---|---|---|
| 01 | [Make the natural-key fallback lazy, so it is not computed and discarded per row](#make-the-natural-key-fallback-lazy-so-it-is-not-computed-and-discarded-per-row) | `A` | −25.1% api.interface.list | unchanged at 2357 | — | accepted |
| 02 | [Prefetch nested natural keys, and fetch each cable path once](#prefetch-nested-natural-keys-and-fetch-each-cable-path-once) | `B1` | −26% api.interface.depth1 | 2235 -> 1903 (-332) | — | accepted |
| 03 | [Render each detail-page component once per request, not three times](#render-each-detail-page-component-once-per-request-not-three-times) | `B1` | not measured — no wall-clock A/B was run | ui.device.detail 108 -> 68, ui.device.interfa… | — | accepted |
| 04 | [Memoize natural-key lookups for the duration of one serialization](#memoize-natural-key-lookups-for-the-duration-of-one-serialization) | `B1` | −13% bulk create, within variance | unchanged at 1404 | 1800 -> 200 per 100-object create loop (-89%) | accepted |
| 05 | [Extend the natural-key lookup cache across a whole read request](#extend-the-natural-key-lookup-cache-across-a-whole-read-request) | `B1` | −48% api.interface.list, under load | — | api.interface.depth1 1938 -> 2 (-99.9%), api.… | accepted |
| 06 | [Stop paying Redis round-trips for tree display and config lookups](#stop-paying-redis-round-trips-for-tree-display-and-config-lookups) | `B2` | −27% api.location.list | — | api.location.list 207 -> 7 (-96.6%), api.devi… | accepted |
| 07 | [Compile each TemplateColumn once instead of once per table cell](#compile-each-templatecolumn-once-instead-of-once-per-table-cell) | `B1` `third-party-coupled` | −18.5% ui.device.interfaces | — | — | accepted |
| 08 | [Cut the residual duplicate queries on object detail pages](#cut-the-residual-duplicate-queries-on-object-detail-pages) | `B1` | −9% detail pages, own A/B inconclusive | ui.device.detail 68 -> 55, ui.rack.detail 77… | — | accepted |
| 09 | [Build nested serializers once, and share one instance per cabled row](#build-nested-serializers-once-and-share-one-instance-per-cabled-row) | `B1` | −45.8% api.interface.depth1 | 1229 -> 629 (-600) | — | accepted |
| 10 | [Walk tree ancestor chains in blocks instead of one query per level](#walk-tree-ancestor-chains-in-blocks-instead-of-one-query-per-level) | `A` | not measured — below what wall clock could resolve | ui.device.detail 55 -> 52, ui.rack.detail 66… | — | accepted |
| 11 | [Use the caching TemplateColumn everywhere, not just device tables](#use-the-caching-templatecolumn-everywhere-not-just-device-tables) | `A` `third-party-coupled` | not measured — same mechanism as 07, −18.5% there | — | — | accepted |
| 12 | [Build the nav menu once per request instead of once per component](#build-the-nav-menu-once-per-request-instead-of-once-per-component) | `A` | −24% ui.device.detail, −25% ui.rack.detail | unchanged | unchanged | accepted |
| 13 | [Reuse one API serializer per model when change-logging a transaction](#reuse-one-api-serializer-per-model-when-change-logging-a-transaction) | `B1` | −28% bulk create | — | 200 -> 2 per 100-object batch | accepted |
| 14 | [Resolve natural keys from a whole-table map for small reference models](#resolve-natural-keys-from-a-whole-table-map-for-small-reference-models) | `B1` | −15% api.interface.depth1 | api.interface.depth1 629 -> 292; dcim_locatio… | — | accepted |
| 15 | [Pre-warm natural-key FK chains with one select_related](#pre-warm-natural-key-fk-chains-with-one-select-related) | `A` | +62% api.interface.depth1 | 2357 -> 1359 (-666 beyond the accepted fixes) | — | rejected |
| 16 | [Stop double-serializing every change record by writing an empty object_data](#stop-double-serializing-every-change-record-by-writing-an-empty-object-data) | `C` | not measured — never a candidate to ship | -200 per 100 writes (-14.2% on bulk.create.lo… | — | parked |
| 17 | [Cross-request Redis-backed natural-key map with signal invalidation](#cross-request-redis-backed-natural-key-map-with-signal-invalidation) | `B2` | +0–2%, inside variance | — | — | rejected |
| 18 | [Enable LOCATION_NAME_AS_NATURAL_KEY](#enable-location-name-as-natural-key) | `C` | −12% api.interface.depth1 | — | — | not-taken |
| 19 | [Process-level per-user nav menu cache](#process-level-per-user-nav-menu-cache) | `B2` `security-visible` | ≈−1% detail pages (~0.6ms) | — | — | rejected |
| 20 | [Drop the request argument in render_component_template](#drop-the-request-argument-in-render-component-template) | `C` | ≈−2% detail pages (~1ms) | — | — | not-taken |
| 21 | [Make tab badges reuse the panel paginator COUNT](#make-tab-badges-reuse-the-panel-paginator-count) | `C` | no measurable change | — | — | not-taken |
| 22 | [Stop writing ObjectChange.object_data, with the column made nullable](#stop-writing-objectchangeobject-data-with-the-column-made-nullable) | `C` `M0` | not measured — parked before a wall-clock A/B was run | exactly 2 per ObjectChange record: bulk.creat… | — | parked |
| 23 | [Backfill object_data_v2 for historical ObjectChange rows](#backfill-object-data-v2-for-historical-objectchange-rows) | `A` `M2` | not applicable — priced by migration runtime | — | — | priced |
| 24 | [Index maintenance on extras_objectchange, priced at a ~0 ceiling](#index-maintenance-on-extras-objectchange-priced-at-a-0-ceiling) | `A` `M0` | −1.7% ceiling, inside the ordering artifact | — | — | not-taken |
| 25 | [Join parent_bay when serializing nested Devices](#join-parent-bay-when-serializing-nested-devices) | `A` | −5.8% api.interface.depth1 | api.interface.depth1 292 -> 192 (-100, exactl… | — | accepted |
| 26 | [Prefetch cable paths for peer terminations that are not on the page](#prefetch-cable-paths-for-peer-terminations-that-are-not-on-the-page) | `B1` | −14.4% api.interface.depth1 | api.interface.depth1 192 -> 30 (-162); Tier 1… | — | accepted |
| 27 | [Batch get_prev_change for deferred change logging](#batch-get-prev-change-for-deferred-change-logging) | `B1` | −3.1% ceiling, bulk update | — | — | not-taken |
| 28 | [Screening pass over every read endpoint, and what it found](#screening-pass-over-every-read-endpoint-and-what-it-found) | `A` | not applicable — an instrument, not a change | top residual costs per returned object, page… | — | accepted |
| 29 | [Replace Tier 1W's rolled-back transactions with restore-based isolation](#replace-tier-1ws-rolled-back-transactions-with-restore-based-isolation) | `A` | −0.9% commit vs rollback, inside variance | rollback 1323, commit 1321 -- a 2-query diffe… | — | not-taken |
| 30 | [Prefetch both ends of an interface connection, which the serializer renders in full](#prefetch-both-ends-of-an-interface-connection-which-the-serializer-renders-in-full) | `B1` | −61.3% api.interface-connections at limit 50, −49.6% at limit 25 | linear before, constant after, and the fit is… | — | accepted |
| 31 | [Fix the tag cache in serialize_object, which can never fire for an untagged object](#fix-the-tag-cache-in-serialize-object-which-can-never-fire-for-an-untagged-object) | `C` | −6.9% bulk.create.x100.loop, −5.6% deferred | per Tier 1W operation, identical across three… | — | accepted |
| 32 | [Tier 1W's wall clock is biased by a GC pause that three alternating rounds cannot cancel](#tier-1ws-wall-clock-is-biased-by-a-gc-pause-that-three-alternating-rounds-cannot-cancel) | `A` | not applicable — a measurement artifact, not a change | — | — | accepted |

Tiers: `A` no observable change, `B1` state scoped to a request, transaction or instance, `B2` state outliving its scope, needs invalidating, `C` changes observable behaviour, `M0` metadata-only migration, `M1` bounded single-pass migration, `M2` unbounded migration.

## Free wins

No migration, no user-visible change, nothing to weigh. Adoptable as-is.

### Make the natural-key fallback lazy, so it is not computed and discarded per row

**01** · `A` · status `accepted` · commit `1974922f0` · (F-03)

`nautobot/core/api/serializers.py:495`

getattr(instance, 'natural_slug', construct_natural_slug(...)) evaluates its third argument before the attribute lookup. natural_slug always exists, so the fallback was computed and thrown away on every row, and the property then recomputed natural_key() a second time.

| Instrument | Result |
|---|---|
| **wall clock** | −25.1% api.interface.list |
| in process | api.interface.list 357.9 -> 268.2ms (-25.1%) |
| queries | unchanged at 2357 |

**Wall clock.** api.interface.list 357.9 -> 268.2ms (-25.1%), median of 15 reps; api.interface.depth1 1479 -> 1254ms (-15.2%); ui.interface.list 64.2 -> 56.0ms (-12.9%)

**Controls.** api.prefix.list flat, as predicted -- its natural keys touch no config-dependent model

**Tests.** core test_api 61 OK

Removes no queries at all, which is why a query-count gate alone would have missed it. The first evidence on this branch that query count is not the objective.

### Prefetch nested natural keys, and fetch each cable path once

**02** · `B1` · status `accepted` · commit `33b15177e` · (F-01/F-06)

`nautobot/core/api/views.py:263 - nautobot/dcim/api/serializers.py:166`

The viewset's prefetch logic inspects top-level serializer fields only, skipping any source containing a dot, so objects reached through nested ?depth serializers were never prefetched. Separately, three SerializerMethodFields each called obj.cable_paths.first() independently.

| Instrument | Result |
|---|---|
| **wall clock** | −26% api.interface.depth1 |
| queries | 2235 -> 1903 (-332) |

**Wall clock.** api.interface.depth1 1479 -> 1092ms (-26%); api.device.list.depth1 301 -> 238ms (-21%); api.interface.list 268 -> 250ms

**Tests.** dcim test_api 1673 OK

The cable-path memo is instance-scoped, so it is discarded with the object.

### Render each detail-page component once per request, not three times

**03** · `B1` · status `accepted` · commit `20499687b` · (F-02)

`nautobot/core/templates/generic/object_retrieve.html:45,:122,:129`

The detail template walks the same tab and panel tree three times and nothing memoized between passes. should_render() ran three times per tab, render_label() twice, and ObjectsTablePanel.get_extra_context() -- which builds and paginates a table -- twice. One device page issued the identical extras_contactassociation COUNT from seven distinct call sites.

| Instrument | Result |
|---|---|
| **wall clock** | not measured — no wall-clock A/B was run |
| queries | ui.device.detail 108 -> 68, ui.device.interfaces 74 -> 42, total -186 |

**Wall clock.** not measured. The commit reports query counts and a byte-for-byte HTML diff only; no wall-clock A/B was run

**Controls.** Rendered HTML diffed byte-for-byte before and after; only the CSRF token differed

**Tests.** core test_ui 59 OK, core test_views 95 OK

Model-agnostic, which is why Device, Rack, Prefix and Location all showed the same shape while list views stayed at 9-10 queries. An App panel whose get_extra_context() must return a fresh object per call would now see one shared object.

### Memoize natural-key lookups for the duration of one serialization

**04** · `B1` · status `accepted` · commit `33ad09279` · (F-04)

`nautobot/core/models/utils.py`

natural_key_field_lookups is an uncached classproperty, rebuilt on every access, and for Device and Location each rebuild reads Constance -- that is, Redis. Serializing one object at depth 1 touches it dozens of times.

| Instrument | Result |
|---|---|
| **wall clock** | −13% bulk create, within variance |
| config reads | 1800 -> 200 per 100-object create loop (-89%) |
| queries | unchanged at 1404 |

**Wall clock.** ~1010 -> ~877ms on the 100-interface create loop, directionally consistent across repeats but within run-to-run variance. Quoted as supporting evidence, not as the result -- the deterministic config-read count is the result

**Tests.** core test_models 35 OK, core test_api 61 OK

Scoped to a single serialization deliberately: Location's value depends on live tree depth and Device's on Constance config, so a permanent class-level cache would go stale. A permanent variant measured +21.7% against +19.8% -- indistinguishable, and not worth the risk.

### Extend the natural-key lookup cache across a whole read request

**05** · `B1` · status `accepted` · commit `2f7bf3003`

`nautobot/core/api/views.py`

ModelViewSetMixin.dispatch() wraps safe methods in the cache_natural_key_field_lookups() scope. Writes deliberately keep recomputing: a POST can deepen the Location tree and then serialize the result in the same request, and must observe the new depth.

| Instrument | Result |
|---|---|
| **wall clock** | −48% api.interface.list, under load |
| config reads | api.interface.depth1 1938 -> 2 (-99.9%), api.interface.list 612 -> 2, api.device.list 156 -> 2 |

**Wall clock.** api.interface.list 305 -> 158ms and api.interface.depth1 1114 -> 726ms, both halves of the same run under equal load. Supporting evidence only: the container was under ~260% CPU from concurrent profiling, so the config-read count is the result

**Controls.** api.prefix.list 0 -> 0 and api.cable.list 0 -> 0 -- a previously considered global cache had regressed prefix ~18% by adding Redis reads to models that needed none

**Tests.** core test_api 61 OK

Wall clock was unusable during this experiment -- the container was under ~260% CPU from concurrent profiling -- so the deterministic config-read count is the primary evidence.

### Compile each TemplateColumn once instead of once per table cell

**07** · `B1` `third-party-coupled` · status `accepted` · commit `40c3af5fe`

`nautobot/core/tables.py`

django_tables2's TemplateColumn.render() calls Template(self.template_code) on every cell, so a table re-parses the same source once per row per column. Django's cached template loader covers the sibling template_name branch; the template_code branch has no such cache. Profiling one page showed Template.parse called 1814 times.

| Instrument | Result |
|---|---|
| **wall clock** | −18.5% ui.device.interfaces |
| in process | ui.device.interfaces 147.5 -> 119.3ms (-18.5%), three alternating rounds |

**Wall clock.** ui.device.interfaces 147.5 -> 119.3ms (-18.5%), median of 12 reps over three alternating rounds on an idle machine

**Controls.** ui.device.list and ui.interface.list both within noise across three rounds

**Tests.** core test_tables 17 OK, dcim test_tables 34 OK

Compiled templates are immutable and hold no per-render state, so sharing one across requests and threads is safe -- new state, but nothing to invalidate. Flagged third-party-coupled: it reimplements django_tables2 3.0.1's render(), depending on the get_context_data() hook and the parent_context request assignment. A future version changing those would diverge silently. Revisit on any django_tables2 upgrade. An earlier two-round comparison suggested a ~2ms regression on two list views; a third round showed it was drift.

### Cut the residual duplicate queries on object detail pages

**08** · `B1` · status `accepted` · commit `0ccf16a4e`

`nautobot/core/ui/object_detail.py - nautobot/core/templatetags/helpers.py - nautobot/core/views/utils.py`

Three independent causes: Panel.get_data() called twice per render; render_ancestor_hierarchy rendering each ancestor's location_type at one query per ancestor; and prepare_cloned_fields instantiating a full ModelForm just to read to_field_name off a few fields, which cost 6.5ms and 10 queries on device detail alone.

| Instrument | Result |
|---|---|
| **wall clock** | −9% detail pages, own A/B inconclusive |
| queries | ui.device.detail 68 -> 55, ui.rack.detail 77 -> 66, total -46 |

**Wall clock.** own A/B inconclusive and no number claimed from it -- ui.rack.detail swung 84 to 110ms across two runs of the same config, larger than the effect. An independent interleaved measurement at 125 samples per config put it at roughly -8.7ms on rack and -8.5ms on device (~-9%), consistent with prepare_cloned_fields measured directly at 6.5ms

**Tests.** core test_views 95 OK, test_ui 59 OK, test_tree_queries 11 OK

Corrects two earlier attributions: Breadcrumbs.as_pair was blamed for calling .ancestors() four times and calls it once; prepare_cloned_fields was suspected of running twice and runs once. My own A/B here was inconclusive -- ui.rack.detail swung 84ms to 110ms across two runs of the same config -- so no wall-clock number is claimed from it.

### Build nested serializers once, and share one instance per cabled row

**09** · `B1` · status `accepted` · commit `295b54700`

`nautobot/core/api/utils.py - nautobot/dcim/api/serializers.py`

build_nested_field() synthesizes a new serializer class on every call and instantiating it deep-copies every declared field; a page of Interfaces at depth 1 built ~250 where 3 are needed. Separately, cable_peer and connected_endpoint resolved to the same 166 rows with 100% overlap -- two Python instances per row, each with its own empty FK cache, each re-walking the chain from cold.

| Instrument | Result |
|---|---|
| **wall clock** | −45.8% api.interface.depth1 |
| queries | 1229 -> 629 (-600) |
| in process | api.interface.depth1 753.4 -> 404.8ms (-45.8%) |

**Wall clock.** api.interface.depth1 753.4 -> 404.8ms (-45.8%), median of 12 reps over two alternating rounds each way

**Controls.** api.device.list.depth1 flat at 209.8/212.7 vs 212.4/214.7

The largest single win on the branch, and the earlier attribution missed it entirely -- it named ~150 GFK fetches, ~100 device bays and ~77 cable paths, which were the small items, and missed the 70% item. Found by grouping queries by originating call site and checking instance provenance.

### Walk tree ancestor chains in blocks instead of one query per level

**10** · `A` · status `accepted` · commit `2e929c137`

`nautobot/core/models/tree_queries.py`

TreeQuerySet.ancestors() falls back to a parent-by-parent walk when tree fields are absent, one query per level. Each cache miss now pulls the next four levels in one query via select_related.

| Instrument | Result |
|---|---|
| **wall clock** | not measured — below what wall clock could resolve |
| queries | ui.device.detail 55 -> 52, ui.rack.detail 66 -> 63, total -8 |

**Wall clock.** not measured. Worth 8 queries across three detail pages, below what wall clock could resolve on that machine

**Tests.** core test_tree_queries 11 OK, test_views 95 OK, test_dynamicgroups 92 OK

Took only the Tier A half of the proposed change. The proposal also primed the returned queryset's _result_cache, which is correct for count/exists/first/iteration and discarded by anything that clones -- a subtler contract than one query justified.

### Use the caching TemplateColumn everywhere, not just device tables

**11** · `A` `third-party-coupled` · status `accepted` · commit `d25990a84`

`nautobot/circuits/tables.py and nine other table modules`

Converts the remaining 67 TemplateColumn call sites across circuits, tenancy, ipam, extras, the App-plugins table and the dcim cables/racks/locations/power modules. Zero remain outside tests.

| Instrument | Result |
|---|---|
| **wall clock** | not measured — same mechanism as 07, −18.5% there |
| note | same mechanism and measured effect as 40c3af5fe; the saving scales with rows x TemplateColumns per table |

**Wall clock.** not measured for this commit -- the machine was under concurrent profiling load. The mechanism is identical to finding 07, measured there at -18.5% on ui.device.interfaces, and the saving scales with rows x TemplateColumns per table

**Tests.** core test_tables 17 OK, dcim test_tables 34 OK, ipam test_tables 7 OK, extras test_tables 7 OK, ipam test_views 596 OK

No wall-clock number quoted: the machine was under concurrent profiling load, and timings here need an idle box and three alternating rounds. Deterministic signals unchanged as expected, since this removes Python work rather than queries.

### Build the nav menu once per request instead of once per component

**12** · `A` · status `accepted` · commit `5b912a905`

`nautobot/core/context_processors.py`

render_component_template() passes a request to Template.render(), which makes Django construct a fresh RequestContext and re-run every context processor. The UI component framework calls that once per component per render pass, so nav_menu was built 70 times on a rack detail page and 81 times on device detail -- each build walking a 180-node registry and performing 176 permission checks, exactly 12,320 has_one_or_more_perms calls per request.

| Instrument | Result |
|---|---|
| **wall clock** | −24% ui.device.detail, −25% ui.rack.detail |
| in process | ui.device.detail -24%, ui.rack.detail -25%, three alternating rounds |
| queries | unchanged |
| redis reads | unchanged |

**Wall clock.** ui.device.detail -24% and ui.rack.detail -25%, alternating rounds on an idle machine. An independent interleaved measurement at n=42 per cell put it at -14 to -17ms across six detail pages (-19.5% to -28.2%). Note the retracted figure: cProfile reported nav_menu at 40ms/request; true wall clock is ~16ms of an ~88ms request

**Controls.** ui.device.list 48.3/49.1 vs 48.9/49.5 and ui.interface.list flat

**Tests.** core test_navigations 6 OK, test_views 95 OK, test_ui 59 OK

Memoized on the request object, so the cache is scoped to one request and therefore one user -- no user-keyed map, no module-level dict, nothing outliving the response. Verified with two users interleaved four times each in one process: the superuser sees 131 distinct nav links, a permissionless user sees 10, both stable, no leakage. Corrects an earlier figure: this was reported at 40ms/request from cProfile, which inflates call-heavy code; true wall clock is ~16ms of an ~88ms request.

### Reuse one API serializer per model when change-logging a transaction

**13** · `B1` · status `accepted` · commit `5f351dc5b`

`nautobot/core/models/utils.py - nautobot/extras/context_managers.py`

serialize_object_v2 runs the full DRF API serializer at depth 1 with every M2M included, so change logging pays the same nested-serialization cost that dominates depth-1 list endpoints. Profiling 100 Interfaces: 517ms, 445 serializer constructions, 7444 build_field calls. The context is a fixed constant and to_representation() reads only the object handed to it, so one instance per class can render them all.

| Instrument | Result |
|---|---|
| **wall clock** | −28% bulk create |
| config reads | 200 -> 2 per 100-object batch |
| wall | bulk.create.loop and .deferred both -28%, three alternating rounds |
| isolated | serialize_object_v2 -41% for 100 Interfaces over five paired rounds |

**Wall clock.** bulk.create.loop and .deferred both -28%, three alternating rounds each way. serialize_object_v2 in isolation -41% for 100 Interfaces over five paired rounds (553 -> 326ms)

**Controls.** the cfg column confirms the two arms really differ, 2 against 200

**Tests.** extras test_changelog 35 OK, extras test_models 295 OK, core test_models 35 OK, core test_api 61 OK

A first attempt at this A/B used git stash to toggle, which silently did nothing because the change was already committed -- all six runs measured the same code, and the cfg counter is what caught it. Also: this commit credited itself with bulk.update 1542 -> 1472 and bulk.delete 2089 -> 2019, which were already present in the preceding run; it compared against a stale before-file. The config-read result is the real effect. Serialized output compared object-by-object across seven models, 172 objects, zero mismatches.

### Resolve natural keys from a whole-table map for small reference models

**14** · `B1` · status `accepted` · commit `3d59d0854`

`nautobot/core/models/__init__.py - nautobot/dcim/models/locations.py - nautobot/tenancy/models.py`

BaseModel.natural_key() walks each lookup with plain getattr(), one SELECT per uncached FK hop. Interface's lookups include five Location hops, so a page of 100 interfaces issued 257 dcim_location SELECTs against a 110-row table and 82 tenancy_tenant SELECTs against a five-row table. When the referenced table is small enough to hold entirely, resolving from a map beats traversing per object.

| Instrument | Result |
|---|---|
| **wall clock** | −15% api.interface.depth1 |
| queries | api.interface.depth1 629 -> 292; dcim_location 257 -> 0, tenancy_tenant 82 -> 0; total 1294 -> 957 |
| in process | -15% on api.interface.depth1, three alternating rounds |

**Wall clock.** api.interface.depth1 -15%, three alternating rounds (328/352/347ms against 398/420ms). Controls flat: api.device.list 176/178/177 vs 174/180; api.prefix.list 56/61/56 vs 55/59

**Controls.** api.device.list and api.prefix.list both flat

**Tests.** core test_models 35 OK, test_csv 28 OK, test_api 61 OK, dcim test_api 1673 OK

Opt-in per model via natural_key_map_enabled, guarded at 5000 rows, built with values_list so no model instantiation, living in the existing request_cache() scope. Falls back to traversal on any miss. A cross-request Redis-backed variant was built and measured alongside and rejected -- see seq 17. Byte-identical responses on six endpoints; a sweep over 1310 objects produced identical natural_key, composite_key and natural_slug, and 60 objects round-tripped back to the same PK.

### Join parent_bay when serializing nested Devices

**25** · `A` · status `accepted` · commit `4c9635e52`

`nautobot/dcim/api/views.py InterfaceViewSet`

DeviceSerializer pulls parent_bay in explicitly, because it is the reverse side of DeviceBay.installed_device and DRF does not auto-include it. A reverse one-to-one costs one query per Device unless it is joined, and at ?depth=1 each row on the page has its own Device instance -- so a 100-row page paid exactly 100 dcim_devicebay queries. select_related(device__parent_bay) on the viewset joins it.

| Instrument | Result |
|---|---|
| **wall clock** | −5.8% api.interface.depth1 |
| queries | api.interface.depth1 292 -> 192 (-100, exactly as attributed); Tier 1 total 957 -> 857 (-61.7% from baseline) |
| duplicates | total 346 -> 247 |

**Wall clock.** api.interface.depth1 1808.3 -> 1703.9ms (-5.8%), median of three separate runs of 15 reps on a quiesced host, spread 0.7% across rounds. All 13 other benched endpoints flat within +-1.6%. The before-state is the round-two baseline bench, which is this experiment's immediate predecessor on the same host.

Worth reading alongside the query figure rather than instead of it: removing 34% of the endpoint's queries bought 5.8% of its wall clock, about 1ms per query. That is consistent with this branch's finding that a query costs ORM round-trip overhead rather than SQL time -- total database time across the whole run moved 601ms to 602ms while query count fell 57%.

**Controls.** All 38 scenarios 200/302, no query count unstable, no other scenario moved. Re-running the attribution shows the core/api/serializers.py:619 call site gone entirely rather than reduced.

**Tests.** dcim test_api 1673 OK (41 skipped), core test_api 61 OK (2 skipped)

Found by attribution rather than by reading code, and the attribution predicted the exact figure: 100 dcim_devicebay queries from one call site, 100 removed. Verified the mechanism in isolation first -- 20 interfaces cost 20 queries with a plain select_related(device) and 0 with select_related(device__parent_bay) -- so the change was measured before it was written.

Rollout candidates: every viewset whose serializer nests a Device carries the same cost. FrontPort, RearPort, ConsolePort, PowerPort and the other component viewsets are the obvious set, and they use the same connection_prefetch_related_fields() helper. Left for a follow-up so this commit's number stays attributable, exactly as the TemplateColumn fix was rolled out separately from its measurement.

### Prefetch cable paths for peer terminations that are not on the page

**26** · `B1` · status `accepted` · commit `ea0aab07c`

`nautobot/dcim/api/serializers.py:215 _first_cable_path, :224 _destination`

After the parent_bay join, 166 of api.interface.depth1's remaining 192 queries come from two call sites: 83 dcim_cablepath from _first_cable_path and 83 dcim_interface from _destination resolving the path's GenericForeignKey destination.

| Instrument | Result |
|---|---|
| **wall clock** | −14.4% api.interface.depth1 |
| queries | api.interface.depth1 192 -> 30 (-162); Tier 1 total 857 -> 695, and 2235 -> 695 (-68.9%) from the large baseline |

**Wall clock.** api.interface.depth1 1703.9 -> 1458.0ms, median of three rounds of 15 reps against the finding 25 state on the same host. Controls flat: api.cable.list +0.3%, ui.device.detail -1.2%, api.interface.list -0.8%.

It beat the -9.5% predicted from finding 25's ~1ms-per-query relationship, because the batch collapses the GenericForeignKey destination fetches as well as the path lookups. A per-query heuristic understates a fix that removes two query classes at once.

The depth guard is worth 56ms on its own. Without it api.interface.list runs 791ms against 735ms guarded -- 7.6% -- over three alternating rounds each way, because four batch queries fetching 83 rows each are not four point lookups. Arm-differ proof: guarded reported api.interface.list at [23, 23, 23] queries and unguarded at [27, 27, 27], with api.interface.depth1 at [30, 30, 30] in both.

Method caveat: the A/B ran the guarded arm first in every round rather than reversing order, so a monotonic drift biases one arm. api.device.list and api.device.list.depth1 read +7.1% and +4.9% for the guarded arm despite having no get_cable_peer at all -- within their known 7.4% worst-case spread and aligned with that bias, so those two readings are not claimed.

**Controls.** api.cable.list +0.3%, ui.device.detail -1.2%, api.interface.list -0.8%; all 38 endpoints 200/302 with no unstable query counts

**Tests.** dcim test_api 1673 OK (41 skipped), circuits test_api 162 OK (3 skipped)

Found by attribution, and attribution also killed the obvious version of the fix. Adding a cable_paths prefetch to the viewset would have measured exactly zero: Interface.connection_prefetch_related_fields() already declares a GenericPrefetch for cable_paths__destination. The queries were never a missing prefetch -- they are peers that are not page members, so the viewset's prefetches never reach them, and the fix has to land where peers are fetched.

Also disproved: .first() on a prefetched reverse FK does not clone the queryset and re-query. It costs 0 queries, so _first_cable_path's memoization was never the bottleneck it resembled, and a fix built on that premise would have been wrong.

Rollout candidates: the other PathEndpointMixin viewsets -- FrontPort, RearPort, ConsolePort, PowerPort -- serialize peers the same way and share the same helper. Left for a follow-up so this commit's number stays attributable.

**Retracted follow-up, and finding 30 is what retracts it.** This record used to claim that the depth guard withholds this fix from dcim.interface-connections -- that the endpoint ignores `depth`, never satisfies `depth > 0`, and so pays _first_cable_path and _destination 50 times each on a page of 25, and that re-keying the guard on whether peers are serialized would fix both endpoints. It would not. With the guard deleted outright on an otherwise unchanged tree, interface-connections still costs 359 queries and those two sites still cost 50 each: its page is CablePath rows, CablePath has no get_cable_peer, and _warm_peer_cable_paths therefore skips every row whatever the guard says. The guard here is measured, correct, and not involved in that endpoint at all. What actually fixed it was prefetching both ends of the connection from the viewset -- see finding 30, which carries the measurement.

### Screening pass over every read endpoint, and what it found

**28** · `A` · status `accepted` · commit `fddc2fc80`

`perf/screen_reads.py`

The inner loop measures 38 hand-picked scenarios. This enumerates every REST list endpoint from the URL resolver at run time and measures list, list?depth=1, detail and detail?depth=1 for each, normalizing to cost per returned object so a 5-row model and a 6,556-row one are comparable. 518 measurements across 166 list endpoints in 84 seconds, against the tree with all sixteen accepted changes applied -- so these are residual costs, not costs the branch has already removed.

| Instrument | Result |
|---|---|
| **wall clock** | not applicable — an instrument, not a change |
| coverage | 6 of 166 API list endpoints were named in workload.yml; 160 had never been measured |
| queries | top residual costs per returned object, page of 25: dcim.cabletocabletermination?depth=1 19.5 q/obj (487 queries, 473 duplicates, 1226ms, 6556 rows); dcim.interfaceconnections 14.4 q/obj (359 queries, 345 duplicates, 777ms, 2780 rows, and a clean 9 + 14n fit against page size); vpn.vpntunnelendpoint?depth=1 9.8; circuits.circuit?depth=1 7.3; dcim.interfaceredundancygroupassociation?depth=1 6.6; ipam.ipaddresstointerface?depth=1 5.9 |

**Tests.** n/a; reads only, no mutation, safe to re-run

Two of these are worse per object than anything the inner loop has ever measured, and neither was in it.

**dcim.cabletocabletermination at depth 1** is the largest: 19.5 queries per returned object with 473 duplicates on a page of 25. For comparison, api.interface.depth1 at its original worst was 1,229 queries for 100 objects -- 12.3 per object -- and that endpoint absorbed most of this branch's effort.

**dcim.interfaceconnections is a clean N+1: 9 fixed queries plus 14 per object.** Page-size sensitivity gives an exact linear fit -- 23 queries at limit 1, 79 at 5, 149 at 10, 359 at 25, 709 at 50 -- so the cost is per-row work rather than pagination overhead. On 2,780 rows that is the largest untouched N+1 the screen found.

**Correction to an earlier reading of this record.** It said interfaceconnections showed 'a different cost class' because its query count is identical at depth 0 and depth 1, and that none of the accepted fixes would touch it. Both halves were wrong. The endpoint simply ignores `depth`: diffing the two responses shows the only difference is the pagination `next` link echoing `depth=1`, exactly 8 bytes at every page size. Identical counts across depth therefore say nothing about cost class. It is the ordinary per-row N+1 class this branch has been fixing all along, which makes it more tractable rather than less. powerconnections and consoleconnections share the shape at 5.4 q/obj and presumably the same explanation.

Caveats carried from the design. This weights every endpoint equally, which is a selection-free sample rather than a usage-weighted one -- nobody may list cabletocabletermination at depth 1 in practice, and that is a product question rather than a measurement one. Per-object cost is misleading on pages of one or two objects, where fixed overhead dominates, so the ranking above is restricted to pages of ten or more. And dcim.connected-device returns 400 without its required peer_device and peer_interface parameters, which is correct behaviour for a lookup endpoint rather than a failure.

It is a screening instrument, not a regression gate. 518 measurements do not belong in the inner loop, which stays at 38 scenarios.

### Prefetch both ends of an interface connection, which the serializer renders in full

**30** · `B1` · status `accepted` · commit `89bb05579`

`nautobot/dcim/api/views.py InterfaceConnectionViewSet - nautobot/dcim/models/cables.py CablePath.interface_connections`

The largest untouched N+1 the read screening pass found: 9 fixed queries plus 14 per returned object, exactly linear against page size, on a 2,780-row endpoint. InterfaceConnectionSerializer embeds a full InterfaceSerializer for both ends of every connection, so each per-interface lookup is paid twice per row. CablePath.interface_connections() now takes an optional endpoint queryset, and the API viewset passes one carrying the prefetches its serializer actually reads; the UI list view, which renders two columns rather than two whole interfaces, keeps the lighter default.

| Instrument | Result |
|---|---|
| **wall clock** | −61.3% api.interface-connections at limit 50, −49.6% at limit 25 |
| queries | linear before, constant after, and the fit is exact at both ends: limit 5 79 -> 17 (9 + 14*5), limit 25 359 -> 17 (9 + 14*25), limit 50 709 -> 17 (9 + 14*50) |
| call sites | all seven eliminated -- .cable''s two queries, _get_cable_peer_rows, CableToCableTermination.termination, _first_cable_path, _destination, and the tag lookup. What remains is the viewset's own prefetches. |
| response | byte-identical between arms -- same sha256 for all three connection endpoints at all three page sizes |
| read path | Tier 1 total 695 -> 695, no endpoint moved by a single query |

**Wall clock.** Three alternating rounds (B/A, A/B, B/A), each round the median of five reps, under the timing lock on a quiesced host. Round medians then A / B / delta: limit 50 1407/1699/1432 -> 1432 against 556/555/548 -> 555, −61.3%. Limit 25 695/755/698 -> 698 against 358/350/352 -> 352, −49.6%. Limit 5 191/277/189 -> 191 against 184/202/183 -> 184, −3.4%, which is what a fixed-cost-dominated page should look like and is the reason the per-object normalization in finding 28 exists.

The saving scales with page size because the cost removed was per object and what is left is not.

**Controls.** power-connections limit 25 +5.1% and limit 50 −0.5%; console-connections limit 25 −0.5% and limit 50 −2.8%; all four with query counts identical in both arms. The UI interface-connections list view is the important one -- it shares the classmethod this changed -- and reads 8 queries in both arms and −1.4%. The +5.1% sets the noise floor, which the two headline figures clear by an order of magnitude. Tier 1: all 38 scenarios 200/302, nothing unstable, gate PASS.

**Tests.** dcim test_api + test_views 4158 OK; dcim test_cablepaths + test_tables + test_filters + test_models 1150 OK (29 skipped). 5308 in total, no failures.

This record previously carried three claims about this endpoint. One was right and two were wrong, and the wrong ones are the more useful entry.

Wrong, and it was called the interesting part: that finding 26's `depth > 0` guard withholds its own fix from here, and that re-keying the guard on whether peers are serialized would fix both endpoints. Falsified by measurement rather than by reading -- with the guard deleted outright on an otherwise unchanged tree, this endpoint still costs 359 queries and _first_cable_path and _destination still cost 50 each. The page here is CablePath rows; CablePath has no get_cable_peer, so _warm_peer_cable_paths skips every row whatever the guard says. Finding 26's guard is measured, correct, and simply not involved. Finding 26's own record carried this as a follow-up and is corrected too.

Wrong: that the queryset has "no select_related and no prefetch_related of any kind". It has a GenericPrefetch on both ends joining the parent device, and its two queries are visible in the attribution under paginate_queryset. The endpoint was under-prefetched for what its serializer renders, not unprefetched.

Wrong: that powerconnections and consoleconnections are "almost certainly the same viewset shape, so one fix plausibly covers three endpoints". They are separate viewsets over PowerPort and ConsolePort with their own querysets. This change does not touch them and they are measured here still at 5.2-5.4 queries per object. One fix covered one endpoint. The other two are a named follow-up and want the same treatment: their serializers render a full ConsolePortSerializer / PowerPortSerializer per row and their querysets prefetch cable_paths__destination but nothing for .cable, the cable peer rows, or tags.

Right, and it was the whole basis of the fix: the 9 + 14n fit, which held to the query at three page sizes, and the attribution of 350 of 359 queries to seven call sites at exactly 50 each.

The first two are the same mistake in two forms -- reading the code and reporting what it looked like, in a record whose own numbered sites were measured. The measured half was correct and the read half was not, which is the pattern this branch has now hit six times.

### Tier 1W's wall clock is biased by a GC pause that three alternating rounds cannot cancel

**32** · `A` · status `accepted` · commit `d52169a34`

`perf/tier1w_writes.py - perf/probe_f31_wall.py`

tier1w_writes.py runs a warmup plus three reps of each operation and reports the wall clock of the LAST rep. A gen-2 cyclic garbage collection costs about 200ms in this process and recurs on a period set by allocation count -- which is deterministic for a given tree. So the pause lands on the reported rep for one arm and not the other, and it does so in every independent run of that arm. The arm-reversal rule cancels monotonic drift; it cannot cancel a bias that is not random.

| Instrument | Result |
|---|---|
| **wall clock** | not applicable — a measurement artifact, not a change |
| incident | finding 31 read create.device at 89/89/89ms unpatched and 241/250/248ms patched, three rounds, both orderings -- a 179% regression that reproduced perfectly and was not real. The same operation measured over nine reps costs 83.7ms and 79.8ms. db_ms was flat at ~24ms in both arms, which is what said the cost was not in the database. |
| mechanism | 24 consecutive calls, gc counters read between each: 329ms on the cold first call, 122ms typical, and one 323ms call carrying a gen-2 collection. Median 122ms, min 111ms, max 329ms. |
| exposure | ~200ms on operations tier1w reports between 25ms and 300ms, where it is up to 8x the true cost; ~6-7% on the ~3s bulk operations. |

**Wall clock.** The fix is an instrument, not an edit to tier1w: perf/probe_f31_wall.py runs nine reps of each write operation and reports the median, min, max and per-rep timings, so one pause cannot move the answer. Observed round-to-round spread on that instrument is 6-13% max-to-min within a round, and the round medians agree to about 2.6% across rounds -- which is the noise floor finding 31's controls independently reported.

**Tests.** n/a; a measurement artifact, no product code involved

Query counts are untouched by this. The collector issues no SQL, so every deterministic counter this branch has published stands, including the ones taken with tier1w. Only its per-operation wall_ms is affected.

Two earlier findings are worth re-reading in this light, and both survive. Finding 24 measured dropping all 17 non-unique ObjectChange indexes at −59ms on a 3358ms operation and called the real cost indistinguishable from zero; a ~200ms artifact makes that conclusion stronger, not weaker. Finding 29 measured commit against rollback at 31ms on a 3.3-second operation and called it inside variance; same. Neither claimed a win that this would erase. What this does retire is the idea that any tier1w wall figure smaller than a few hundred milliseconds meant anything.

tier1w_writes.py is deliberately left alone. Changing it to report a median would silently redefine every wall figure in the committed baselines, and its job is the deterministic counters, which are correct. The wall-clock instrument is separate and says so.

Found by refusing to average away a regression. The honest reading of 89ms against 248ms, three rounds, both orderings, is that the change made create.device three times slower -- and the branch's own rule is to explain a tripwire rather than accept or dismiss it.

## Wins with a user-visible caveat

Each one works and is measured. Whether the caveat is acceptable is a product decision rather than a measurement one, so it is quoted as a release note would have to write it.

### Stop paying Redis round-trips for tree display and config lookups

**06** · `B2` · status `accepted` · commit `6020f8ebf`

`nautobot/core/utils/config.py - nautobot/core/models/tree_queries.py - nautobot/core/signals.py`

get_settings_or_config() now memoizes in the per-request cache; TreeManager.max_depth goes through cache_get_or_set; TreeModel.display walks the in-memory parent chain when it is complete instead of fetching a cached string it could concatenate for free.

| Instrument | Result |
|---|---|
| **wall clock** | −27% api.location.list |
| redis reads | api.location.list 207 -> 7 (-96.6%), api.device.list.depth1 128 -> 19 |
| in process | api.location.list 75.2 -> 54.9ms (-27%) on a quiet machine |

**Wall clock.** api.location.list 75.2 -> 54.9ms (-27%), interleaved over 7 rounds on a quiet machine. The fix also removed variance, since the Redis round-trips were the jitter source

**Controls.** api.prefix.list 5 -> 5 unchanged

> **Caveat.** A configuration value written by another process is not picked up mid-request. Constance already has its own eventual propagation, and a config_updated receiver covers same-process writes.

**Tests.** core test_tree_queries 11 OK, test_config 30 OK, test_utils 73 OK

B2 because max_depth uses the cross-request Redis cache. The invalidate_max_depth_cache receiver was extended to evict the request-local entry too, and that line is necessary -- without it a tree change mid-request leaves a stale max_depth. Note the pre-existing hazard this shares: all six invalidate_*_cache receivers miss QuerySet.update() and .bulk_update(), which compile to one SQL statement and emit no signal.

### Stop double-serializing every change record by writing an empty object_data

**16** · `C` · status `parked` · commit `43db1b018` · (F-05)

`nautobot/extras/models/change_logging.py:45-46`

to_objectchange() calls both serialize_object() for v1 and serialize_object_v2(). Every current consumer reads v1 only as a fallback when v2 is null, which never happens for a newly created record.

| Instrument | Result |
|---|---|
| **wall clock** | not measured — never a candidate to ship |
| queries | -200 per 100 writes (-14.2% on bulk.create.loop); exactly two queries per ObjectChange record |

**Wall clock.** not measured. Deterministic query counts were the point; the change was never a candidate to ship

**Controls.** read path unaffected: Tier 1 total unchanged, all endpoints 200

> **Caveat.** Every /api/extras/object-changes/ response would carry an empty object_data, and the GraphQL objectData field likewise, because both serialize with all fields. The Object Data panel on the ObjectChange detail page would render an em-dash instead of JSON. Historical records whose only snapshot is v1 would lose it.

**Why not.** Tier C on two public API contracts, and the premise did not survive measurement: the redundant serializer is the cheap one. serialize_object costs 0.73ms and two queries; serialize_object_v2 costs 8.75ms and seven queries -- twelve times more. Removing the duplication is worth ~12% of write-path queries, not half of them.

**Tests.** breaks 13 tests in nautobot.extras.tests.test_changelog -- six failures and seven errors across ChangeLogAPITest, ChangeLogViewTest and ObjectChangeModelTest, including test_legacy_object_data

Kept in history with its measurements rather than deleted, because the number is the deliverable. The route that gets this win without the contract change is seq 22.

### Fix the tag cache in serialize_object, which can never fire for an untagged object

**31** · `C` · status `accepted` · commit `d52169a34`

`nautobot/core/models/utils.py:225 - nautobot/extras/api/mixins.py:30 - nautobot/extras/signals.py - nautobot/extras/context_managers.py`

serialize_object read a cached tag list if one was present on the instance:

    tags = getattr(obj, "_tags", []) or obj.tags.only("name")

`[] or X` evaluates X, so an object whose cached tag list was empty fell through and queried anyway. Most objects in a bulk write have no tags, so the cache could never fire for the common case. Testing for presence instead of truth makes it fire, and two callers can then populate it: a just-created object provably has no tags, and the deferred change-logging flush can read the whole batch's tags in one query instead of one per record.

| Instrument | Result |
|---|---|
| **wall clock** | −6.9% bulk.create.x100.loop, −5.6% deferred |
| queries | per Tier 1W operation, identical across three rounds each arm: bulk.create.x100.loop 1404 -> 1304 (-100), bulk.create.x100.deferred 1307 -> 1208 (-99), bulk.update.x100.deferred 1472 -> 1424 (-48), create.interfaces.x50 704 -> 654 (-50), create.device 34 -> 33, create.interface 18 -> 17, create.ipaddress 22 -> 21 |
| read path | unchanged -- Tier 1 total 695 -> 695, no endpoint moved by a single query |
| serialize object | 40 queries for 20 Interfaces -> 20; the extras_tag lookup disappears entirely |
| not improved | bulk.delete.x100 stays at 2019. Django clears an instance's pk on delete, so at flush time there is nothing to look the tags up by, and the batch warm skips those rows. |

**Wall clock.** Three alternating rounds (B/A, A/B, B/A), snapshot restore and container restart before every arm, each round the median of nine reps. Round medians then A / B / delta: bulk.create.x100.loop 3256/3218/3110 -> 3218 against 3047/2926/2994 -> 2994, −6.9%. bulk.create.x100.deferred 3108/3111/3090 -> 3108 against 2933/2817/2990 -> 2933, −5.6%. create.interfaces.x50 1573 -> 1499, −4.7%. create.interface 48.9 -> 45.1, −7.6%. create.device 83.7 -> 79.8, −4.7%.

bulk.update.x100.deferred is the interesting non-result: −48 queries and −0.8% wall, inside the noise floor. The tag lookups it removes are point selects on an empty table, so removing them buys nothing there, and the operation is another entry in this branch's ledger of query count misranking a change.

The nine-rep median is a new instrument, and the reason for it is finding 32: tier1w_writes.py reports the LAST of its reps, and a gen-2 GC collection costing ~200ms recurs on a period that is deterministic for a given tree. It landed on the reported rep of create.device for this change and not for its baseline, in all three rounds, reading 89ms against 248ms for an operation that actually costs ~84ms either way. Three alternating rounds cannot cancel a bias that is not random. See finding 32.

**Controls.** bulk.create.x100.bulk_create, which skips change logging entirely, −2.2%; bulk.delete.x100, whose query count does not move, +2.6%. Those two set the noise floor at about ±2.6%, which every claimed figure above clears. Read path: all 38 Tier 1 scenarios 200/302, no query count changed, no query_count_stable went false.

> **Caveat.** One observable change, and it is a correction. Clearing every tag from an object through the REST API previously recorded the object's pre-clear tags in its change-log entry: TaggedModelSerializerMixin.update set `instance._tags = []` before saving, exactly so change logging would see the new (empty) tag list, and the falsy check then discarded that and queried the database, which still held the old tags. The change record now says the tags were removed, because they were. Anything reading ObjectChange.object_data["tags"] for a tag-clearing PATCH -- the changelog UI diff, the REST /extras/object-changes/ payload, GraphQL, and webhook snapshots -- sees the corrected value from now on. Historical rows are not rewritten, so a changelog spanning the upgrade shows the old behaviour before it and the new behaviour after.

No other path changes. Seven change-logging paths were serialized in both arms and diffed: inline create, tags added after create, deferred create, deferred create with tags assigned before the flush, an ORM update of a tagged object, a REST PATCH that omits tags, and deleting a tagged object. All seven are byte-identical; the tag-clearing PATCH is the only line that differs.

**Tests.** 4027 OK across five suites and no failures -- extras test_changelog + extras test_tags + core test_utils 113 OK; extras test_api + circuits test_api + core test_api 1698 OK (74 skipped); dcim test_api + ipam test_api 2216 OK (59 skipped)

Three parts, and the first is not optional. `getattr(obj, "_tags", None) is not None` in serialize_object makes the cache fire. But TaggedModelSerializerMixin.update already wrote `instance._tags = tags or []` unconditionally, and `tags` is None whenever the payload does not mention tags -- so fixing the check alone would have made every REST PATCH that leaves tags alone log the object as untagged. The mixin now sets the cache only when the request actually carried tags. This is the trap the falsy check was accidentally covering, and it is why the check and the mixin have to change together.

Then the two populate sites. A just-created object provably has no tags: the row must exist before a TaggedItem can reference it, assigning tags fires its own m2m_changed and its own ObjectChange, and deleting an object deletes its TaggedItem rows with it -- verified -- so a recycled primary key cannot leave stale ones. The deferred flush cannot make that assumption, because it serializes later, so it reads the whole batch's tags in one query instead. Both caches are dropped again at the end of the scope that knew they were true.

This is the Tier A route to roughly half of what finding 22 measured, and it arrives at Tier C for a reason finding 22 does not share: 22 changes the API payload by removing data, this one changes a single logged value by correcting it. 22 needs a deprecation cycle and 13 test updates; this needs neither, and no test asserted the old behaviour -- 1,811 tests pass unchanged.

The obvious follow-up is bulk.delete, and it is probably not worth taking. Warming `_tags` to an empty list for an instance whose row is gone would remove its remaining 50 tag queries, and it would be correct -- the TaggedItem rows are deleted with the object, so both paths return nothing. But the comparable case is already measured: bulk.update.x100.deferred lost 48 queries of exactly this kind and moved 0.8%, inside the noise floor. Priced before it is built rather than after.

Supersedes this record's own earlier claim of −50% on serialize_object (4.59 -> 2.30ms per object). That figure is still what the isolated probe measures and it reproduced exactly (40 / 40 / 20 queries, 4.88 -> 2.49ms per object), but it is the ceiling of the serializer in isolation, not what the change is worth in a write: the operations it sits inside improve by 5-7%, not 50%.

## Wins with an operational cost

These need a migration. The cost of running it is stated, because it lands on operators rather than on the release.

### Stop writing ObjectChange.object_data, with the column made nullable

**22** · `C` `M0` · status `parked` · commit `9b8831711`

`nautobot/extras/models/change_logging.py plus a new AlterField migration`

object_data holds the legacy v1 snapshot -- Django's serialize("json"), flat columns with foreign keys as bare primary keys. object_data_v2 holds what every current consumer reads. Both were written for every change record, so each write paid for two serializations. Migration 0146 makes the v1 column nullable -- AlterField, metadata-only, no table rewrite -- and to_objectchange stops populating it.

| Instrument | Result |
|---|---|
| **wall clock** | not measured — parked before a wall-clock A/B was run |
| queries | exactly 2 per ObjectChange record: bulk.create.loop 1404 -> 1204 (-14.2%), bulk.create.deferred 1307 -> 1107 (-15.3%), bulk.update.deferred 1472 -> 1374, bulk.delete 2019 -> 1919, create.interfaces.x50 704 -> 604, create.device 34 -> 32, create.interface 18 -> 16 |
| prize remeasured | the v1 serializer costs 454.4ms of a 3775.4ms bulk.create.loop (12.0%), 575.8ms of 4525.7ms deferred (12.7%), 4.5ms per call against v2's 14.4ms -- 3.2x, where finding 16 measured 12x. The ratio narrowed because finding 13 made v2 cheaper, so v1 is a larger share of what remains than when it was first measured |

**Wall clock.** not measured -- proposed, not yet run

**Controls.** read path unaffected: Tier 1 total 695, all 38 endpoints 200/302, gate PASS

> **Caveat.** object_data is null for every record written after this lands. Both the REST serializer and the auto-generated GraphQL type use fields="__all__", so it surfaces as null in /api/extras/object-changes/ and in the GraphQL objectData field. Separately, a change whose prior change predates object_data_v2 -- Nautobot 1.2, migration 0022, which added the column with no backfill -- reports no prechange rather than comparing two v1 snapshots, because a v1 snapshot and a v2 snapshot are different shapes and cannot be diffed. With CHANGELOG_RETENTION at its 90-day default those records are long gone; where retention is 0 they persist.

**Why not.** Tier C on two counts, and I classified it B1 before running the tests. It changes the API and GraphQL payload, and it changes get_snapshots semantics for pre-1.2 records. Landing it needs a deprecation cycle for the payload plus updates to 13 tests. Reverted from the tree in the commit following 9b8831711 to keep the suite green, and kept in history with its measurements.

**Tests.** extras test_models 295 OK, extras test_api 1475 OK, and **13 failures in extras test_changelog** -- 7 errors from assertions on oc.object_data[...] against None, 6 failures across test_legacy_object_data and test_get_snapshots on mixed-format snapshot pairs

What the nullable route buys over 43db1b018, which wrote {} into the NOT NULL column, is not the 2 queries -- both save those. It is that {} makes the field present and lying: an empty dict is not "no data", so updating the 13 tests to match it would have encoded a falsehood. null is the correct JSON for "not recorded", so those same test updates become legitimate. A Tier C change whose tests can be honestly updated is a different proposition from one whose tests cannot.

Two things established on the way that outlive this finding. Every object_data site turned out to be paired with an object_data_v2 site -- signals.py at 152/153, 391/392 and 488/489 -- so the write path needed no other changes. And get_snapshots carried a latent AttributeError: its mixed-format branch dereferenced prior_change.object_data without checking prior_change is not None, reachable in principle for an UPDATE whose prior changelog was pruned on a record lacking v2. Unreachable today because every record has v1.

### Backfill object_data_v2 for historical ObjectChange rows

**23** · `A` `M2` · status `priced`

`a new data migration`

Migration 0022_objectchange_object_datav2 added v2 as nullable with no backfill, and two data migrations construct rows carrying only v1. Backfilling would let object_data be dropped outright rather than merely left null.

| Instrument | Result |
|---|---|
| **wall clock** | not applicable — priced by migration runtime |
| cost | 8.75ms per row measured. ~5 minutes at this dataset's 36,552 rows; ~2.4 hours at one million; unbounded where CHANGELOG_RETENTION is 0 or its cleanup Job was never scheduled |

**Wall clock.** not applicable -- priced by migration runtime (8.75ms per row), not by request latency

> **Caveat.** A migration whose runtime is set by the operator's retention configuration rather than by anything in the release.

**Why not.** M2 on cost, and a correctness stop that pricing does not rehabilitate: v1 stores foreign keys as bare primary keys while v2 needs nested natural keys, so reconstructing v2 for a row referencing a since-deleted object is not possible at any cost. Re-serializing the live object instead would falsify the historical record.

Recorded so the option is priced rather than rediscovered. seq 22 gets the same win without it.

## Measured, and not worth it

Plausible optimizations that measurement or blast-radius analysis killed. These are results, not omissions: they say what a tempting option actually costs.

### Pre-warm natural-key FK chains with one select_related

**15** · `A` · status `rejected` · commit `27b3ec422`

`nautobot/core/models/__init__.py`

Replaces the hop-by-hop walk with a single select_related covering the whole natural-key chain.

| Instrument | Result |
|---|---|
| **wall clock** | +62% api.interface.depth1 |
| queries | 2357 -> 1359 (-666 beyond the accepted fixes) |
| in process | api.interface.depth1 1092 -> 1767ms (+62%) |

**Wall clock.** -666 queries and api.interface.depth1 1092 -> 1767ms, +62%. The wall-clock measurement IS the result here: it is what rejected the change

**Why not.** Failed the gate: slower. Location's natural key is variadic on live tree depth, so with max_depth 4 it expands to eight prefixes and an 8-join, 5,420-character query per object. PostgreSQL answers it quickly, but Django then materializes wide rows across eight tables and instantiates a model object for each. It also added a per-call inspection walk to every natural_key(), slowing a depth-0 endpoint it was never meant to touch.

The most useful result on the branch. The variant that looked three times better by query count was the one that regressed past the original baseline in wall time. Deeper Location trees make it worse, not better.

### Cross-request Redis-backed natural-key map with signal invalidation

**17** · `B2` · status `rejected` · commit `3d59d0854`

`nautobot/core/models/__init__.py`

The whole-table map from seq 14, cached across requests in Redis and invalidated by post_save receivers.

| Instrument | Result |
|---|---|
| **wall clock** | +0–2%, inside variance |
| in process | +0 to 2 percentage points over the request-scoped variant -- inside run-to-run variance |

**Wall clock.** +0 to 2 percentage points over the request-scoped variant, inside run-to-run variance

> **Caveat.** A stale Location name would propagate into composite_key and natural_slug in every REST response for Location, Device and every device component, and into CSV export columns -- so the API would advertise a composite key that no longer resolves, and a CSV round-trip would re-import against the wrong Location.

**Why not.** B2 with an unbounded staleness window. QuerySet.update() and .bulk_update() compile to a single SQL statement and never instantiate models, so they emit no post_save and cannot invalidate the cache. With no timeout that persists until some Location is saved. Low probability, high blast radius, and the probability depends on plugin or shell code we do not control -- not worth two points.

The within-request memo already captures essentially all of the benefit: the map is built once and read ~820 times per request. This hazard already exists in-tree for all six invalidate_*_cache receivers; worth addressing separately, either with a TTL backstop or by overriding update() and bulk_update() on RestrictedQuerySet.

### Enable LOCATION_NAME_AS_NATURAL_KEY

**18** · `C` · status `not-taken` · commit `3d59d0854`

`an existing supported setting`

Collapses Location's natural key from five hops to one.

| Instrument | Result |
|---|---|
| **wall clock** | −12% api.interface.depth1 |
| in process | -12% on api.interface.depth1 standalone; ~2 points on top of the whole-table map |

**Wall clock.** -12% on api.interface.depth1 standalone; ~2 points on top of the whole-table map

> **Caveat.** Composite keys change for Location and for every device component that includes a Location in its natural key. A CSV exported before the change no longer round-trips, and any integration storing a composite key must re-resolve it.

**Why not.** Tier C: changes API output on five of the six endpoints tested. Already a supported setting, so this is an operator decision rather than a code change -- worth documenting as a tuning option with its caveat.

Stacking it on the whole-table map adds only ~2 points, because the map already collapses most of what it avoids.

### Process-level per-user nav menu cache

**19** · `B2` `security-visible` · status `rejected` · commit `5b912a905`

`nautobot/core/context_processors.py`

Cache the built nav menu per user across requests rather than per request.

| Instrument | Result |
|---|---|
| **wall clock** | ≈−1% detail pages (~0.6ms) |
| in process | ~0.6ms, now that nav_menu runs once per request |

**Wall clock.** ~0.6ms, now that nav_menu runs once per request

> **Caveat.** ObjectPermission edits, group membership changes and is_superuser toggles would take effect only after cache expiry.

**Why not.** B2 and security-visible. A bounded stale window on a display string is tolerable; the same window on an authorisation decision is not. ~0.6ms does not buy that.

Rejected on blast radius, not on measurement. Included because the reasoning generalises: cached permission-derived output is a different class of risk from cached data.

### Drop the request argument in render_component_template

**20** · `C` · status `not-taken` · commit `5b912a905`

`nautobot/core/ui/utils.py:39`

Would skip all 70 RequestContext binds outright rather than memoizing their result.

| Instrument | Result |
|---|---|
| **wall clock** | ≈−2% detail pages (~1ms) |
| in process | ~1ms residual after the per-request memo |

**Wall clock.** ~1ms residual after the per-request memo

> **Caveat.** Rendered output changes for permission-restricted users.

**Why not.** Tier C for ~1ms once the memo is in place. Not worth it.

### Make tab badges reuse the panel paginator COUNT

**21** · `C` · status `not-taken` · commit `0ccf16a4e`

`nautobot/core/ui/object_detail.py`

The tab badge issues its own COUNT alongside the one django-tables2's paginator already computed.

| Instrument | Result |
|---|---|
| **wall clock** | no measurable change |
| in process | no measurable effect |

**Wall clock.** no measurable effect

> **Caveat.** The badge would show the permission-filtered count rather than the raw related count -- a visible number changing for non-superusers.

**Why not.** Tier C for no measured gain.

### Index maintenance on extras_objectchange, priced at a ~0 ceiling

**24** · `A` `M0` · status `not-taken`

`to be determined from pg_stat_user_indexes`

extras_objectchange carries at least eleven indexes, every one maintained on every insert, and it is the highest-insert-volume table in Nautobot. The write path is where the remaining cost sits: bulk.create.x100 is still 1,404 queries dominated by those inserts, and nothing on this branch has looked at index maintenance as a cost.

| Instrument | Result |
|---|---|
| **wall clock** | −1.7% ceiling, inside the ordering artifact |
| ceiling | dropping all 17 non-unique indexes: wall 3358 -> 3300ms median, -59ms (-1.7%), db_ms 776 -> 752ms, on bulk.create.interfaces.x100.deferred |
| observed | 18 non-primary indexes on extras_objectchange, 17 of them non-unique |

**Wall clock.** Three rounds each way, snapshot restore before every arm, arm order reversed between rounds: with 3434/3307/3358ms, without 3296/3414/3300ms.

The medians differ by 59ms, and in all three rounds the arm that ran SECOND was faster by 60-130ms regardless of which arm it was. The difference and the artifact are the same size, so the real cost of index maintenance here is indistinguishable from zero.

Consistent with the rest of the branch: db_ms is only ~780ms of a 3358ms operation, and index maintenance is a fraction of that. The cost on this path is Python-side.

**Why not.** Measured at a 1.7% ceiling that is the same magnitude as the run-ordering artifact, from dropping every non-unique index on the table. There is nothing to win, so the question of which indexes are safe to drop -- which our 3.6% endpoint coverage makes hard to answer -- does not need answering.

Scoped to non-unique indexes deliberately, and the reason is worth keeping. The first attempt tried to drop all 18 and failed: extras_objectchange_time_request_id_changed__f5f870cc_uniq backs a UNIQUE constraint, and PostgreSQL refuses to drop such an index directly. That is the right boundary rather than an obstacle -- a unique index is a constraint, not an optimization, so dropping one removes a guarantee and is a behaviour change rather than index tidying.

It also corrects the count this record used to carry. It said eleven indexes; that was the never-scanned subset from a pg_stat reading. The table has 18 non-primary indexes, 17 non-unique.

Adding indexes remains a dead end here and that stays measured rather than assumed: 325,152 queries totalling 4.6 seconds, mean 0.014ms, and query count fell 57% while total DB time went 601ms to 602ms. Both directions on this table are now priced.

### Batch get_prev_change for deferred change logging

**27** · `B1` · status `not-taken`

`nautobot/extras/context_managers.py:275 - nautobot/extras/models/change_logging.py:202`

get_snapshots() calls get_prev_change(), one SELECT per ObjectChange, and the deferred flush loop calls it once per record. It only fires for non-CREATE actions, which matches the measurements exactly: bulk.create pays nothing while bulk.update.deferred (1,472 queries, 49 changes) and bulk.delete (2,019 queries, 50 changes) both do. The code already carries a TODO saying so, and it is upstream issue #6303.

| Instrument | Result |
|---|---|
| **wall clock** | −3.1% ceiling, bulk update |
| measured prize | 49 get_prev_change calls costing 101.3ms of a 3278.7ms bulk.update.x100.deferred (3.1%); 50 calls costing 96.9ms of 3535.0ms on bulk.delete.x100 (2.7%); 0 calls on bulk.create.deferred, since it only fires for non-CREATE actions; 2 calls costing 4.1ms on delete.device (0.9%) |

**Wall clock.** The prize was measured before the fix was attempted, by wrapping get_prev_change to count and time its calls while running the same operations Tier 1W runs. Batching cannot recover all of it -- the batch is still one query -- so the realistic saving is ~3.0% of a bulk update or delete.

This also corrects how the target was described. get_snapshots() was called the largest remaining SQL item on the write path, which is true by query count -- 100 queries per 100-object operation -- and false by time. Query count misranking a target, again: the same lesson as the rejected FK pre-warming, arrived at from the other direction.

**Why not.** Measured at a 3.1% ceiling on bulk.update and 2.7% on bulk.delete, against an implementation that has to handle an object changed twice within one request (the flush loop iterates oldest-first, so the second change's prior change is the first) and cannot use the clean DISTINCT ON form, which is PostgreSQL-only in Django while the loop above it carries a comment about MySQL sort behaviour. Not worth that for 3%.

Upstream issue #6303 remains open and the TODO in context_managers.py:273 is accurate about the mechanism -- one query per object change. What this record adds is the size of the prize, so nobody spends the effort twice to find out.

Worth revisiting if the write path ever becomes latency-critical, or if the ObjectChange insert and index-maintenance costs (finding 24) are removed first, since 3% of a smaller number is a larger share.

### Replace Tier 1W's rolled-back transactions with restore-based isolation

**29** · `A` · status `not-taken`

`perf/tier1w_writes.py - perf/probe_commit_cost.py`

Tier 1W runs each operation inside transaction.atomic() and raises to unwind it, which buys identical starting state -- the bulk triple depends on it, since all three create the same 100 rows with the same names -- at the cost of never committing. With a dedicated measurement host and a 20-second snapshot restore, restore-based isolation is affordable and would measure real committed writes.

| Instrument | Result |
|---|---|
| **wall clock** | −0.9% commit vs rollback, inside variance |
| queries | rollback 1323, commit 1321 -- a 2-query difference, the ROLLBACK/COMMIT statement itself |

**Wall clock.** bulk.create.interfaces.x100.deferred, three rounds each way with a snapshot restore before every arm so each started from byte-identical state, and arm order reversed between rounds. rollback 3389/3312/3407ms, commit 3343/3358/3367ms. Medians 3389 against 3358 -- a 31ms difference on a 3.3-second operation, inside the run-to-run spread.

The first attempt at this measurement was discarded, and the reason is the useful part. It ran three rollback reps then one committed rep, so the committed arm was fourth and warmest, and reported commit 5% FASTER. Reversing the arm order flipped the sign: in both round 1 and round 3 the second arm won by ~46ms regardless of which arm it was. That is pure ordering effect, and it validated the arm-reversal rule within an hour of the rule being written.

**Why not.** Not worth changing, because the thing it would fix is not costing anything measurable. Rollback under-reports wall clock by 0.9% on a bulk create, inside variance, so Tier 1W's existing figures stand. Rollback also keeps Tier 1W fast enough to sit inside run_experiment.sh --writes as an inner-loop gate, where restore-based isolation would add ~70 seconds per arm.

What rollback omits is qualitative rather than quantitative, and that part is still true: transaction.on_commit callbacks never fire, and Nautobot uses them in five places including custom-field job enqueueing at extras/customfields.py:744. An experiment that changed commit-time behaviour -- deferring constraint checks, batching index maintenance to commit -- would measure exactly zero under rollback. Nothing on this branch has done that. Finding 24 is the near miss: index maintenance happens in-transaction, so rollback does see it.

It also does not follow that rollback is fine for the write screening matrix. REST writes cross the process boundary, so a rolled-back transaction in the harness cannot isolate them at all. This result narrows that build rather than blocking it: the in-process ORM half can keep using rollback for speed, and only the REST half needs restore-based orchestration.


## Current state — all 20 accepted fixes

Measured against the 24,091-object dataset on a pristine tree, and reflecting the tree as it stands. Only the 9 scenarios whose count changed are listed; the other 29 are unchanged, which is itself the point -- the list views were already efficient.

| Scenario | Queries | Duplicates |
|---|---|---|
| `api.interface.depth1` | 1,229 → 30 (-98%) | 1,198 → 3 |
| `api.device.list.depth1` | 112 → 11 (-90%) | 101 → 0 |
| `ui.device.detail` | 108 → 52 (-52%) | 55 → 3 |
| `ui.device.detail.worst` | 104 → 54 (-48%) | 49 → 3 |
| `ui.rack.detail` | 99 → 63 (-36%) | 45 → 11 |
| `ui.prefix.detail` | 79 → 49 (-38%) | 34 → 7 |
| `ui.device.interfaces` | 71 → 39 (-45%) | 34 → 1 |
| `ui.location.detail` | 68 → 39 (-43%) | 27 → 3 |
| `ui.objectchange.detail` | 24 → 17 (-29%) | 9 → 2 |
| **All 38 scenarios** | **2,235 → 695 (-68.9%)** | **1,604 → 85** |

Write path. Queries and config reads are deterministic; config reads are Redis round-trips
through Constance.

| Operation | Queries | Config reads | Wall |
|---|---|---|---|
| `bulk.create.x100.loop` | 1,604 → 1,404 (−12%) | 3,600 → 2 (−99.9%) | −28% |
| `bulk.create.x100.deferred` | 1,507 → 1,307 (−13%) | 3,600 → 2 (−99.9%) | −28% |
| `bulk.update.x100.deferred` | 1,675 → 1,472 (−12%) | 1,764 → 2 (−99.9%) | — |
| `bulk.delete.x100` | 2,222 → 2,019 (−9%) | 1,779 → 3 (−99.8%) | — |
| `create.interfaces.x50` | 804 → 704 (−12%) | 1,800 → 2 (−99.9%) | — |
| `create.interface` | 20 → 18 (−10%) | 36 → 2 | — |
| `create.device` | 34 → 34 | 15 → 3 | — |
| `bulk.create.x100.bulk_create` (floor) | 3 → 3 | 0 → 0 | — |

**Config reads are the signal on this path, not queries.** The natural-key lookup cache now
spans a whole transaction rather than one object, so a 100-object batch performs 2 Constance
reads instead of 3,600. Query count moves 9–13% because most of the remaining SQL is the
ObjectChange insert and the per-record `get_snapshots()` SELECT, neither of which any accepted
change touches.

Part of the write-path gain is inherited from the read path: change logging serializes through
the same API serializer, so prefetching nested natural keys cut `bulk.create.loop` from 1,604
to 1,404 queries before any write-specific change was made.

> **Correction.** The commit message for `5f351dc5b` credits serializer reuse with
> `bulk.update` 1,542 → 1,472 and `bulk.delete` 2,089 → 2,019. Those two query improvements
> were already present in the preceding run — the commit compared against a stale *before*
> file rather than the immediately preceding one. Its config-read result (98 → 2, 101 → 3)
> stands and is the change's real effect. No cumulative figure above is affected.

### Round-two reference, under uwsgi at concurrency 1

The absolute figures every future experiment is compared against, on the dedicated
measurement host. No before/after column: this is a reference, and it replaces a Tier 2
instrument that was measuring something else (see below).

| Scenario | r1 | r2 | r3 | median | spread |
|---|---:|---:|---:|---:|---:|
| `api.interface.depth1` | 1908 | 1884 | 1881 | **1884 ms** | 1.4% |
| `api.device.list.depth1` | 952 | 940 | 950 | **950 ms** | 1.2% |
| `api.interface.list` | 798 | 790 | 806 | **798 ms** | 2.1% |
| `api.device.list.deep` | 718 | 720 | 715 | **718 ms** | 0.8% |
| `api.device.list` | 726 | 718 | 714 | **718 ms** | 1.7% |
| `ui.device.interfaces` | 672 | 668 | 668 | **668 ms** | 0.7% |
| `ui.prefix.detail` | 369 | 370 | 369 | **369 ms** | 0.4% |
| `ui.rack.detail` | 366 | 370 | 365 | **366 ms** | 1.5% |
| `api.cable.list` | 359 | 360 | 360 | **360 ms** | 0.4% |
| `api.prefix.list` | 352 | 358 | 356 | **356 ms** | 1.7% |
| `api.location.list` | 318 | 321 | 318 | **318 ms** | 1.1% |
| `ui.device.detail` | 318 | 310 | 318 | **318 ms** | 2.5% |
| `ui.device.list.deep` | 312 | 312 | 312 | **312 ms** | 0.2% |
| `ui.device.detail.worst` | 312 | 316 | 311 | **312 ms** | 1.4% |
| `ui.device.list.filtered` | 314 | 304 | 308 | **308 ms** | 3.2% |
| `ui.device.list.page100` | 307 | 312 | 308 | **308 ms** | 1.8% |
| `ui.device.list` | 300 | 297 | 303 | **300 ms** | 2.0% |
| `ui.interface.list` | 293 | 294 | 294 | **294 ms** | 0.3% |
| `ui.location.list` | 263 | 256 | 269 | **263 ms** | 4.9% |
| `api.ipaddress.list` | 268 | 253 | 255 | **255 ms** | 5.7% |
| `ui.prefix.list.page100` | 250 | 250 | 250 | **250 ms** | 0.4% |
| `ui.location.detail` | 249 | 251 | 248 | **249 ms** | 1.4% |
| `ui.prefix.list` | 241 | 237 | 238 | **238 ms** | 1.7% |
| `ui.ipaddress.list` | 234 | 234 | 234 | **234 ms** | 0.2% |
| `api.device.detail.depth1` | 231 | 234 | 231 | **231 ms** | 1.3% |
| `ui.home` | 224 | 224 | 224 | **224 ms** | 0.0% |
| `ui.devicetype.list` | 215 | 222 | 214 | **215 ms** | 3.3% |
| `ui.rack.list` | 213 | 220 | 212 | **213 ms** | 4.0% |
| `api.device.detail` | 210 | 208 | 210 | **210 ms** | 1.2% |
| `ui.cable.list` | 192 | 185 | 192 | **192 ms** | 3.9% |
| `ui.circuit.list` | 192 | 192 | 192 | **192 ms** | 0.3% |
| `ui.vlan.list` | 178 | 174 | 180 | **178 ms** | 3.7% |
| `ui.tag.list` | 166 | 170 | 165 | **166 ms** | 2.7% |
| `ui.role.list` | 156 | 151 | 159 | **156 ms** | 5.1% |
| `ui.status.list` | 154 | 152 | 152 | **152 ms** | 1.6% |
| `ui.objectchange.list` | 149 | 150 | 151 | **150 ms** | 1.3% |
| `ui.objectchange.detail` | 136 | 142 | 137 | **137 ms** | 4.4% |
| `ui.search` | 81 | 83 | 81 | **81 ms** | 2.5% |

All 38 scenarios probed 200 before timing, none skipped. Spread across rounds: median **1.6%**, max **5.7%**.

### In-process reference, same host

Absolute figures with no HTTP layer, for the endpoints where serialization dominates.

| Scenario | r1 | r2 | r3 | median | spread |
|---|---:|---:|---:|---:|---:|
| `api.interface.depth1` | 1484 | 1449 | 1458 | **1458 ms** | 2.4% |
| `api.device.list.depth1` | 866 | 902 | 905 | **902 ms** | 4.4% |
| `api.interface.list` | 755 | 735 | 733 | **735 ms** | 3.1% |
| `api.device.list` | 650 | 686 | 678 | **678 ms** | 5.3% |
| `ui.device.interfaces` | 624 | 607 | 606 | **607 ms** | 3.0% |
| `api.cable.list` | 338 | 333 | 334 | **334 ms** | 1.3% |
| `api.prefix.list` | 325 | 320 | 324 | **324 ms** | 1.6% |
| `ui.rack.detail` | 333 | 323 | 323 | **323 ms** | 3.2% |
| `api.location.list` | 295 | 286 | 289 | **289 ms** | 3.0% |
| `ui.device.list` | 288 | 277 | 276 | **277 ms** | 4.2% |
| `ui.interface.list` | 277 | 271 | 271 | **271 ms** | 2.4% |
| `ui.device.detail` | 266 | 267 | 266 | **266 ms** | 0.5% |
| `api.ipaddress.list` | 235 | 232 | 230 | **232 ms** | 2.0% |
| `ui.cable.list` | 170 | 169 | 167 | **169 ms** | 1.8% |

Spread across rounds: median **2.7%**, max **5.3%** across 14 endpoints. Reflects the tree as it stands: every experiment measures wall clock, whatever its primary signal, because this host is precise enough that skipping it would only hide a result.

## What measurement overturned

**Query counts are page-bounded, not dataset-bounded.** At 11× the data the counts barely
moved, which retroactively validates the small dataset as an instrument for finding N+1s.

**Removing queries did not reduce database time.** 1,278 fewer queries, and total DB time
across the run went 601 ms to 602 ms. On this dataset the ORM round trip and the Python
wrapped around it are the cost; the SQL itself was never in the way.

**Three of my own conclusions were wrong and got corrected by measurement.** I claimed the
hierarchy endpoints scale with dataset size; page-size sensitivity testing showed that is true
for exactly one of five, and the UI list endpoints are entirely fixed cost. I put `nav_menu` at
40ms per request from a cProfile figure; true wall clock was ~16ms. And `ui.prefix.detail`
appeared to regress 11% until three repeat runs produced 329 / 447 / 347ms on identical code.

**Five hypotheses formed by reading code were wrong**, each corrected by instrumentation:
`Breadcrumbs.as_pair` was blamed for calling `.ancestors()` four times and calls it once;
`prepare_cloned_fields` was thought to run twice and runs once; an attribution of
`api.interface.depth1` named three small items and missed the item worth 70%; and the
ObjectChange double-serialization was assumed expensive on the redundant half, which is 12×
cheaper than the half that stays. Measure first; read code to explain a measurement, never to
predict one.

### Query count ranked the fixes in the wrong order

> **The single largest win removed zero queries.**
>
> Reusing nested serializers instead of rebuilding one per object is worth **−28.7%** on its
> own and changes no SQL at all. Memoizing the nav menu is worth **−24%** on detail pages and
> removes no query, no Redis read and no SQL — pure Python call overhead. On
> `api.interface.depth1`, a separately-measured change that removed 99 queries moved wall clock
> by **+0.3%**, and the rejected FK pre-warming removed 666 queries while running **62%
> slower**.
>
> Query count is the cheap, deterministic signal. It is the gate, not the objective.

### Four instruments, because each fix class is invisible to the others

| Instrument | Catches | Blind to |
|---|---|---|
| SQL query count | N+1s, missing prefetch | Python and Redis work |
| Redis backend reads | config and cache round-trip storms | SQL and pure CPU |
| Wall clock, in-process | everything Django does | the HTTP layer — reproduces to a median 1.7% spread across rounds, 7.4% worst case |
| Wall clock, HTTP at concurrency 1 | everything, including HTTP and WSGI | nothing — median 1.6% spread, 5.7% worst case, and agrees with in-process to 1.08× |

The Redis counter exists because one endpoint was making **1,938 Redis round-trips against 977
SQL queries** — twice as many cache calls as database calls, entirely invisible to SQL-shaped
tooling.

Two rounds of alternating A/B were not enough twice over: a 2ms "regression" and an 11%
"regression" both dissolved on a third round. Four of the fourteen commits could quote no
wall-clock number at all, because the box was busy; their evidence is a deterministic counter
instead.

### The HTTP instrument was measured wrong twice before it was measured right

Tier 2 originally ran 30 requests at concurrency 4 and reported the 95th percentile. On a host
that holds in-process timings to ±4%, that configuration spread **25.3% at the median and
41.4% at worst** across three rounds of identical code. The p95 was drawn from roughly two
samples in the tail, against three uwsgi workers sharing two physical cores, in integer
milliseconds. It was noise wearing a percentile.

At concurrency 1 reporting the median, the same 38 endpoints on the same code spread **1.6% at
the median and 5.7% at worst** — a sixteenfold improvement in precision, from changing the
instrument rather than the hardware.

The result that matters is not the precision. It is that the two independent instruments now
**corroborate** each other: HTTP at concurrency 1 runs a median **1.08×** in-process across
fourteen shared endpoints, range 1.04–1.19×, which is what HTTP parsing, WSGI and response
transfer should cost on top of identical Django work. Under the old configuration they
disagreed by 4–6× and nothing could say which was right.

Before that they disagreed in a way that was physically impossible, and it went unnoticed:
`ui.device.interfaces` reported a 66ms HTTP p95 against a 607ms in-process median. An API token
authenticates DRF only, so every UI endpoint had been answering **403** with a 299KB
permission-denied page that renders in ~50ms. cassowary counts an answered request as a
success, and the driver recorded no status code at all, so the run reported 38 endpoints and
zero failures while 27 of them timed an error page. Tier 2 now probes each endpoint once before
timing it, carries the status alongside the timing, and refuses to time anything that does not
answer 200.

Nor was the server the one anybody deploys. The dev image's CMD is `nautobot-server runserver`
— one threaded, GIL-bound process — while production runs uwsgi. Every round-one p95 described
a server nobody ships.

## What is next

### Remaining application-code targets

- **Row-scaling N+1s that memoization cannot help.** `PowerFeed.utilization` and the prefix
  hierarchy column both grow linearly with row count. Every fix on this branch so far removes
  *repeated* work; these need a different shape.
- **`get_snapshots()` is not the target it looked like.** It does issue one SELECT per
  ObjectChange — 100 queries per 100-object operation — which made it look like the largest
  remaining item on the write path. Timed, those calls are **3.1%** of a bulk update and 2.7%
  of a bulk delete. Query count misranking a target again, and the reason finding 27 was
  measured before it was built rather than after. Upstream #6303 stays open and correct about
  the mechanism; what is now on record is the size of the prize.
- **Residual `api.interface.depth1` cost is 292 queries**, down from 1,229. What is left is
  GenericForeignKey destination fetches and reverse one-to-one device-bay lookups on the nested
  Device serializer — the same mechanism as the accepted fixes, not yet applied to those paths.
- **`api.prefix.list` spends 13.7ms planning a query that executes in 1.3ms**, re-planned every
  request — about 21% of that endpoint's time. Explicitly not an index: an index makes planning
  worse. The lever is prepared-statement reuse or narrowing the serializer's `select_related`
  fan-out.

### Two things worth more than the next code fix

> **The largest measured gap is environment, not code.**
>
> Against `next.demo.nautobot.com` at the same API version: `/api/dcim/devices/?limit=50` takes
> 1,051ms there versus 231ms unpatched here, and `/api/ipam/prefixes/?limit=100` — an endpoint
> this branch barely changes, 58 → 55ms — takes 274ms, **4.7× slower**. That control isolates
> the difference as environment. TLS handshake was ~70ms, so it is not the network.
>
> One caveat on the magnitude: the "here" figures are in-process medians while the demo figures
> were taken over HTTPS against uwsgi, so the two sides used different instruments. Too large a
> gap to be all instrument, but it should be re-measured now that both sides can be taken at
> concurrency 1 under uwsgi.

**Read coverage was 3.6%, and the screening pass is now built.** This section used to claim a
surface of 330 API list endpoints and coverage of "roughly 5%". Both figures were wrong. The URL
resolver reports **166 API list endpoints**, of which **6** are named in `workload.yml` — real
coverage of **3.6%**. `api.interface.depth1`, now a 76% query reduction, was found by guessing
that interfaces is the biggest table.

`perf/screen_reads.py` replaces the guessing. It enumerates every REST list endpoint from the
resolver at run time — never from a list in a file, so it cannot rot as models come and go — and
measures `list`, `list?depth=1`, `detail` and `detail?depth=1` for each: 518 measurements in 84
seconds, normalized to cost per *returned object* rather than per request. It is a ranking
instrument, not a gate; the inner loop stays at 38 scenarios. Two of its top five were worse per
object than anything the inner loop had ever measured, and it has already produced two accepted
fixes (findings 30 and 31). `dcim.cabletocabletermination?depth=1` at 19.5 queries per object
over 6,556 rows is the largest target it found that nothing has yet touched.

**Writes have no equivalent instrument.** Tier 1W is 11 hand-picked ORM operations across four
models, which is a narrower sample than reads had before the screen, and every write finding on
this branch came out of it. A write screening matrix is designed in `perf/README.md` and not yet
built.

## Caveats

- **The full test suite passes.** `invoke tests --no-parallel` ran all **17,504 tests in
  2h 15m** against the tree at `2d1326e86`: **OK, 663 skipped, 1 expected
  failure, zero failures and zero errors**. This closes what had been the branch's largest
  open item — a whole-suite run had never completed, and the one attempt died in Django's
  parallel runner with a pickling error *and exited 0*. Serial was the fix, so a worker
  exception would surface instead of being swallowed. The run built docs rather than passing
  `--skip-docs-build`, which is what makes `test_get_docs_url` fail across ~35 dcim model
  tests even on a clean tree; it does not appear here. Note it used `--keepdb
  --cache-test-fixtures`, so a result that ever smells like stale fixture state should be
  re-run with `--no-keepdb`. **It does not cover the current tree.** Findings 30 and 31 landed
  after that run; each has a targeted suite behind it — 5,308 and 4,027 tests, no failures — but
  no whole-suite run has been taken against the tree as it now stands.
- **No changelog fragments, deliberately.** Nothing here reaches a release in its current
  shape: anything upstreamed would go through review and very likely change, so a fragment
  written now would describe a change that no longer exists. The commit messages and the
  findings records carry more than a fragment would, and each finding's caveat is already
  written the way a release note would have to write it. The same applies to documenting the
  new `nautobot.apps.tables.TemplateColumn` export and the django_tables2 upgrade note --
  both are real requirements for landing, recorded against their findings, and written
  against the final shape rather than this one.
- **Absolute wall-clock figures are not comparable across the two machines used here.** Round
  one ran on an Apple Silicon workstation; round two on an Intel i5-8259U with turbo disabled,
  which is 4–6× slower in absolute terms and far more precise. Relative comparisons hold;
  absolute ones do not.
- **Fourteen accepted changes, ten of which introduce request-scoped state.** No single one is
  unjustified, and each is measured. The aggregate is still a lot of new caching for a reviewer
  to absorb at once, and it deserves to be read as a set rather than as fourteen unrelated
  diffs.
- **Improvements are not additive.** Several changes reduce natural-key work by different
  means, so their individual gains overlap rather than sum. Only the cumulative row is a sum.
- **Small-dataset history.** Round one measured 2,113 objects, where most list views paginate
  at 25–50 rows and look fast regardless. Those figures are kept for provenance, not for
  judging real-world value.
- **Silk middleware remains in the request chain.** It is inert without a session flag and
  constant across runs, so it does not distort relative comparisons, but it is present in every
  absolute number here.
- **UI response bodies are not byte-stable** between identical requests, so HTML endpoints are
  compared on query count and status rather than content.

---

Measured against `nautobot/next` at 3.3.0a0 on an isolated stack with pinned resources. Harness, workload definition, findings and baseline data are on the `perf/experiments` branch under `perf/`; every scenario and operation above is reproducible with `perf/run_experiment.sh`.

This file is generated. Edit `perf/findings/*.yml` for numbers and `perf/report.template.md` for narrative, then run `perf/build_report.py`. `--check` exits non-zero when the two have drifted apart.
