# Optimization opportunities in Nautobot core

Measured read and write path optimizations, each one applied to a branch and priced. Every
number came from a reproducible harness against a fixed dataset on one dedicated host. The
instruments, the baselines and the working behind each change are in
[`perf/methodology.md`](methodology.md).

| | |
| --- | --- |
| Measured against | `next` at `3f77053a7` · 3.3.0a0 |
| Dataset | databot `enterprise-campus / large` for reads, `datacenter / large` for writes |
| Changes proposed | 68 |
| Accepted on this branch | **52** |
| Rejected | 15 |
| Reverted after landing | 1 |
| Experiments recorded | 88, of which 20 measured the harness itself |

> Generated by `perf/scripts/build_report.py` from `perf/findings/` and `perf/baselines/`. Do not edit this file.
>
> Sources: tree `07bf0f5d9` with 10 dirty source path(s), at generation time; readloop from `tier1-57-stock-next.json` + `tier1-57-branch-next.json` + `tier2-57-stock-next.json` + `tier2-57-branch-next.json`; readscreen from `perf/baselines/screen-reads-ab-next.json`; writes from `perf/baselines/screen-writes-ab-next.json`; apply hand-entered in `perf/baselines/cumulative.json`, tree `33f4cb8930f611786f2125dee8b4d3491726a50ce1674d6066dc1b56dec7fa3e`; per-change figures from `perf/findings/*.yml`.

## Cumulative effect

Stock `next` against this branch, one box, arms alternated, both trees proved different by content hash before each run. Every endpoint is weighted equally, so these are totals over a workload rather than a prediction of what any one user gets back.

| Measurement | Coverage | Queries | Database time | Wall clock |
|---|---|---|---|---|
| Read loop, 57 scenarios | 57 scenarios on both arms | 3,619 -> 901 (−75.1%) | - | 63.8 s -> 30.0 s (−52.9%) |
| Read screen, every REST read endpoint | 516 of 518 measurements | 15,035 -> 4,668 (−69.0%) | 6,766 ms -> 3,965 ms (−41.4%) | 67.2 s -> 28.7 s (−57.3%) |
| Write screen, every POST endpoint | 254 of 257 measurements | 20,529 -> 17,044 (−17.0%) | 10,651 ms -> 8,869 ms (−16.7%) | 52,397 ms -> 36,309 ms (−30.7%) |
| Whole-workflow apply, 11,572 objects | 11,572 objects created per arm, across 68 models | 804,796 -> 635,388 (−21.0%) | 31,859 ms -> 24,556 ms (−22.9%) | 1,838 s -> 1,037 s (−43.6%) |

- **Read loop, 57 scenarios**: median of five alternating rounds per arm, both arms proved distinct by content hash -- stock `0ee05ec157bcf7a8`, branch `33f4cb8930f61178` -- and each hash reproduced on every arm of every round. Query counts were bit-identical across all rounds on both arms (3,619 and 901): zero unstable counts in any run.

**Wall clock is the median of each scenario across the five rounds, then summed** -- not the median of the five round totals. Summing rounds first lets the single largest endpoint drive the result, and `api.interface.depth1` at ~11s is large enough to do it.

Dispersion across rounds is **0.74% on stock and 0.95% on branch**, as the interquartile range of the round totals against their median. Max-min is wider, 3.96% and 1.56%, but it grows with round count by construction and is not comparable with the three-round figures this row used to carry. **Per scenario the picture is looser: the median scenario's relative standard deviation across rounds is 5.21% on stock and 5.25% on branch.** The totals are stable because 57 scenarios' errors cancel, not because any single endpoint reproduces tightly -- so this cumulative figure carries weight that an individual per-endpoint wall-clock number taken from it does not.

**Re-measured 2026-09-22, after the branch was rebased onto `next` at 3f77053a7.** The stock arm moved with it, so both arms now carry the 22 queries finding 88 attributes to upstream #9501 -- which is why the query row reads -75.1% where it read -75.6%, the branch's absolute saving being unchanged at 2,718 queries. The scenario set is hand-picked and largely what this branch optimised, so it describes these pages fairly rather than predicting an arbitrary one; the read screen row is the unbiased check; wall clock is the sum of per-endpoint medians over the 57 endpoints answering on both arms, so it is a workload total rather than a per-page figure; 57 of 57 improved, 0 unchanged, **none worse**; the median moved -46.0%; and **25% of the total saving is one item**, `api.interface.depth1`.
- **Read screen, every REST read endpoint**: median of three alternating rounds per arm with the arm ORDER reversed between rounds, enumerated from the URL resolver at run time rather than from a scenario list, and matched on `(id, kind)` -- the kinds make 518 measurements, and keying on id alone collapses them to 165 and silently changes every total. Arms proved distinct by content hash on every swap.

**Direction, not just the net: 51 measurements improved, 332 unchanged, 133 worse.** The 133 are finding 62's trade made visible -- a prefetch replacing a JOIN is more round trips, each cheaper -- and that finding's own note says the query counter reads it as a regression and must not be the gate. They cost 248 queries against 10,615 saved, 2.3%, and the same rounds put database time down 41.4%.

This row exists to check whether the read loop's result generalises beyond the 57 scenarios this branch optimised. It is REST-only -- no UI page appears in it -- and it is a screening instrument read as a ranked list, not a gate; 51 of 516 measurements improved on query count, 332 unchanged, 133 worse; query savings run **20.6% to 98.0%** per measurement, median 72.2%; on wall clock 94 of 96 improved, 1 unchanged, 1 up to +21.9%; the median moved -47.3%; and **7% of the total saving is one item**, `dcim-api:cable-list list.depth1`; 420 measurements below 100ms are excluded, where a per-measurement wall figure cannot be trusted for sign (finding 43).
- **Write screen, every POST endpoint**: median of three alternating rounds per arm, matched on `(id, kind)` rather than id alone -- the three kinds make 257 measurements from 105 endpoints, and keying on id alone collapses them. Arms proved distinct by content hash on every swap.

178 measurements improved, 66 unchanged, 10 worse. **Queries and wall clock come apart on the write path and both belong here**: -17.0% queries against -30.7% wall. Gating on queries alone undervalues the write-path work by roughly 1.8x, because those wins are Python -- serializer reuse, natural-key memoization, a tag cache -- rather than SQL; 178 of 254 measurements improved on query count, 66 unchanged, 10 worse; query savings run **1.9% to 51.5%** per measurement, median 8.6%; on wall clock 126 of 126 improved, 0 unchanged, **none worse**; the median moved -20.7%; and **12% of the total saving is one item**, `dcim-api:interfaceredundancygroupassociation-list create.x10`; 128 measurements below 100ms are excluded, where a per-measurement wall figure cannot be trusted for sign (finding 43).
- **Whole-workflow apply, 11,572 objects**: **the whole-workflow figure: one `databot apply` per arm, creating 11,572 objects across 68 models, three alternating rounds, median.** The database is dropped and recreated from an empty template before every arm, so each run builds the dataset from nothing.

The client runs on a second host, not on the box under measurement: it builds every payload itself and is a sustained CPU load, and the perf overlay commits all eight of the measurement host's threads. Measured cost of moving it off-box is +0.16ms per TCP connect against ~198ms average per request.

Wall clock is tight across rounds -- stock 1839/1838/1832s, branch 1037/1040/1032s, spans of 0.4% and 0.8% -- and row counts were identical on all six arms (536 devices, 1,616 interfaces, 1,648 cables), which is what says each arm applied the same work. Query counts come from `pg_stat_statements` reset per arm, so they cover the whole run rather than one request.

The dataset file defines 11,578 objects and databot reports creating 11,572; the six-object difference was not chased, and the figures here describe what was created. These figures are hand-entered from the run's result lines; no source file drives them, so verify_report.py cannot recompute this row; measured against tree `33f4cb8930f611786f2125dee8b4d3491726a50ce1674d6066dc1b56dec7fa3e`.

## Tiers

Tiers price the adoption cost rather than filter on it. Nothing here is disqualified for being
expensive; it is labelled so the price is visible. A flag alongside a tier carries the part of
the risk the tier cannot express.

| Tier | Adoption cost |
|---|---|
| `A` | no observable change |
| `B1` | state scoped to a request, transaction or instance |
| `B2` | state outliving its scope, needs invalidating |
| `C` | changes observable behaviour |
| `M0` | metadata-only migration |
| `M1` | bounded single-pass migration |
| `M2` | unbounded migration |

| Flag | What it adds to the tier |
|---|---|
| `security-visible` | Touches an authorisation decision rather than a displayed value, so a stale or shared result is a permissions bug. |
| `third-party-coupled` | Reimplements or depends on internals of a dependency, so an upgrade can change behaviour rather than break a signature. Correct against the pinned version, and a differential test now renders the same cell both ways so a divergence fails a test instead of producing wrong output. |
| `unmeasurable-on-this-dataset` | The endpoint returns zero rows on both snapshots, so no timing and no endpoint probe here can show the defect. Gated instead on a deterministic counter over rows synthesised inside a rolled-back transaction: the mechanism is demonstrated, the value on a populated instance is not. |

All commits referenced below can be found on the [`perf/recommended`](https://github.com/kevsmith/nautobot/tree/perf/recommended) branch of this repo.

## Accepted (52)

Measured, kept, and applied to the tree. Each Reason is that change measured on its own, and several reduce the same cost by different routes -- they do not sum, and the cumulative table above is the measured total.

| # | Change | Tier | Reason | Commit |
|---:|---|---|---|---|
| 01 | [Make the natural-key fallback lazy, so it is not computed and discarded per row](methodology.md#make-the-natural-key-fallback-lazy-so-it-is-not-computed-and-discarded-per-row) | `A` | −25.1% api.interface.list | `545190347` |
| 02 | [Prefetch nested natural keys, and fetch each cable path once](methodology.md#prefetch-nested-natural-keys-and-fetch-each-cable-path-once) | `B1` | −26% api.interface.depth1 | `70ca95a00` |
| 03 | [Render each detail-page component once per request, not three times](methodology.md#render-each-detail-page-component-once-per-request-not-three-times) | `B1` | −186 queries across two detail pages; no wall-clock A/B was run | `b44116b7b` |
| 04 | [Memoize natural-key lookups for the duration of one serialization](methodology.md#memoize-natural-key-lookups-for-the-duration-of-one-serialization) | `B1` | −89% Constance reads per create loop; −13% bulk create | `9e47e3bbc` |
| 05 | [Extend the natural-key lookup cache across a whole read request](methodology.md#extend-the-natural-key-lookup-cache-across-a-whole-read-request) | `B1` | −48% api.interface.list, under load | `6a7c0a63e` |
| 06 | [Stop paying Redis round-trips for tree display and config lookups](methodology.md#stop-paying-redis-round-trips-for-tree-display-and-config-lookups) | `B2` | −27% api.location.list | `717551dfe` |
| 07 | [Compile each TemplateColumn once instead of once per table cell](methodology.md#compile-each-templatecolumn-once-instead-of-once-per-table-cell) | `B1` `third-party-coupled` | −18.5% ui.device.interfaces | `2f4781034` |
| 08 | [Cut the residual duplicate queries on object detail pages](methodology.md#cut-the-residual-duplicate-queries-on-object-detail-pages) | `B1` | −46 queries on detail pages; ~−9% on an independent A/B | `cd0fe8ee6` |
| 09 | [Build nested serializers once, and share one instance per cabled row](methodology.md#build-nested-serializers-once-and-share-one-instance-per-cabled-row) | `B1` | −45.8% api.interface.depth1 | `b43be4113` |
| 10 | [Walk tree ancestor chains in blocks instead of one query per level](methodology.md#walk-tree-ancestor-chains-in-blocks-instead-of-one-query-per-level) | `A` | −8 queries on detail pages; below what wall clock could resolve | `755ad3771` |
| 11 | [Use the caching TemplateColumn everywhere, not just device tables](methodology.md#use-the-caching-templatecolumn-everywhere-not-just-device-tables) | `A` `third-party-coupled` | consistency: 67 call sites left on the uncached column | `48e9a7397` |
| 12 | [Build the nav menu once per request instead of once per component](methodology.md#build-the-nav-menu-once-per-request-instead-of-once-per-component) | `A` | −24% ui.device.detail, −25% ui.rack.detail | `7ecae12d4` |
| 13 | [Reuse one API serializer per model when change-logging a transaction](methodology.md#reuse-one-api-serializer-per-model-when-change-logging-a-transaction) | `B1` | −28% bulk create | `864ff1b50` |
| 14 | [Resolve natural keys from a whole-table map for small reference models](methodology.md#resolve-natural-keys-from-a-whole-table-map-for-small-reference-models) | `B1` | −15% api.interface.depth1 | `6c1ddd84c` |
| 22 | [Stop writing ObjectChange.object_data, with the column made nullable](methodology.md#stop-writing-objectchangeobject-data-with-the-column-made-nullable) | `C` `M0` | storage: object_data is 11.8% of the changelog table | `5f7fd6e41` |
| 25 | [Join parent_bay when serializing nested Devices](methodology.md#join-parent-bay-when-serializing-nested-devices) | `A` | −5.8% api.interface.depth1 | `8cd802d3e` |
| 26 | [Prefetch cable paths for peer terminations that are not on the page](methodology.md#prefetch-cable-paths-for-peer-terminations-that-are-not-on-the-page) | `B1` | −14.4% api.interface.depth1 | `352d06fc4` |
| 30 | [Prefetch both ends of an interface connection, which the serializer renders in full](methodology.md#prefetch-both-ends-of-an-interface-connection-which-the-serializer-renders-in-full) | `B1` | −61.3% api.interface-connections at limit 50, −49.6% at limit 25 | `3a3fdee74` |
| 34 | [Prefetch the cable walk for power- and console-connections, the two endpoints finding 30 left](methodology.md#prefetch-the-cable-walk-for-power--and-console-connections-the-two-endpoints-finding-30-left) | `B1` | −51.0% power-connections at limit 50, −42.7% at limit 25 | `de8bc5ece` |
| 36 | [Prefetch the cable's terminations for cable-to-cable-terminations at depth 1](methodology.md#prefetch-the-cables-terminations-for-cable-to-cable-terminations-at-depth-1) | `B1` | −75.6% at limit 50, −68.1% at limit 25 | `2fa7bf5ad` |
| 42 | [Route natural_key() through the ancestor walk Nautobot had already optimized](methodology.md#route-natural-key-through-the-ancestor-walk-nautobot-had-already-optimized) | `A` | consistency: natural_key() bypassed the optimized ancestor walk | `a45bc9a60` |
| 48 | [Defer cable path rebuilds in the REST create path, the fourth instance of one shape](methodology.md#defer-cable-path-rebuilds-in-the-rest-create-path-the-fourth-instance-of-one-shape) | `A` | −18 queries on a two-ended REST cable create; wall clock inconclusive | `42d68a223` |
| 50 | [Where a list view's 283ms goes, and it is template rendering rather than the view](methodology.md#where-a-list-views-283ms-goes-and-it-is-template-rendering-rather-than-the-view) | `A` | attribution: 91.6% of an empty list view is template rendering, not the view | - |
| 51 | [Resolve the navbar-favorites URLs once per process, not once per menu item](methodology.md#resolve-the-navbar-favorites-urls-once-per-process-not-once-per-menu-item) | `B2` | −43.0% on a chrome-only response; 387 URL reversals per page become 18 | `d44504c62` |
| 53 | [Select_related the device list's primary IPs, which its own table cannot see](methodology.md#select-related-the-device-lists-primary-ips-which-its-own-table-cannot-see) | `A` | attribution: 100 ipam_ipaddress queries on a 100-row page, all from one property | `83010f904` |
| 54 | [Route TenantColumn through the caching TemplateColumn](methodology.md#route-tenantcolumn-through-the-caching-templatecolumn) | `A` `third-party-coupled` | attribution: 100 template compilations per 100-row page, one per cell, from one column | `3cecc5162` |
| 55 | [Stop compiling SQL for querysets that are empty by construction](methodology.md#stop-compiling-sql-for-querysets-that-are-empty-by-construction) | `A` `third-party-coupled` | attribution: 99 SQL compilations discarded per device-list request, 0 executed from them | `505a9027b` |
| 56 | [Prefetch the home page changelog panel's generic foreign key](methodology.md#prefetch-the-home-page-changelog-panels-generic-foreign-key) | `A` | attribution: 25 of the home page's 79 queries are one panel rendering 15 rows | `97b993d26` |
| 58 | [Cache the rendered sidenav item fragment per user, template, registry and permission set](methodology.md#cache-the-rendered-sidenav-item-fragment-per-user-template-registry-and-permission-set) | `B2` `security-visible` | finding 57: no hot spot in a fixed 20.4ms, so the levers are fewer nodes or fewer renders | `5189079f0` |
| 59 | [Tags are free on the read path, and the tagged dataset exists for the write path](methodology.md#tags-are-free-on-the-read-path-and-the-tagged-dataset-exists-for-the-write-path) | `A` | dataset A/B on one arm: 9,390 tag assignments cost 0 queries and +10.7KB per 100 rows | - |
| 60 | [Finding 22 subsumes finding 31, whose tag cache nothing on the branch reads](methodology.md#finding-22-subsumes-finding-31-whose-tag-cache-nothing-on-the-branch-reads) | `A` | serialize_object is called 50x on stock and 0x on the branch for the same 50-object write | - |
| 61 | [Drop finding 31, whose mechanism finding 22 made inert, and shed a tier C change](methodology.md#drop-finding-31-whose-mechanism-finding-22-made-inert-and-shed-a-tier-c-change) | `A` | dropping finding 31 removes 2 queries of 6,637 and moves nothing else | - |
| 62 | [Prefetch depth-0 FK serializer fields instead of JOINing them](methodology.md#prefetch-depth-0-fk-serializer-fields-instead-of-joining-them) | `A` | attribution: Device widens 32 -> 212 columns at depth 0 for columns DRF never reads | `cab58fd08` |
| 63 | [Auto-prefetch GenericForeignKey fields in the REST optimizer](methodology.md#auto-prefetch-genericforeignkey-fields-in-the-rest-optimizer) | `A` | attribution: ~1 query per row per GFK, and exactly 2.19 on the model carrying two | `1455556b1` |
| 64 | [Memoize TreeModel.display within the request scope](methodology.md#memoize-treemodeldisplay-within-the-request-scope) | `A` | attribution: 1.000 cache-backend GET per rendered row, and 97 -> 5 after | `4ba849d89` |
| 65 | [Prefetch the cable peer on the FrontPort and RearPort API viewsets](methodology.md#prefetch-the-cable-peer-on-the-frontport-and-rearport-api-viewsets) | `A` `unmeasurable-on-this-dataset` | attribution on synthesised rows: 1.000 q/row before, 0.000 after, at depth 0 | `5008da2d4` |
| 66 | [Let Job.task_queues honor a prefetch, and prefetch it on the Job viewsets](methodology.md#let-jobtask-queues-honor-a-prefetch-and-prefetch-it-on-the-job-viewsets) | `A` | attribution: exactly 1.000 query per row, and flat in the row count after | `14b0c20e8` |
| 67 | [Memoize the route shape of serializer URLs, so reverse() runs once per route](methodology.md#memoize-the-route-shape-of-serializer-urls-so-reverse-runs-once-per-route) | `A` | attribution: 1,031 reversals on a 100-row page, 9.9 per row, 78.2ms of reverse() | `b30968dd9` |
| 68 | [Memoize the route shape of get_absolute_url and dynamic form data-urls](methodology.md#memoize-the-route-shape-of-get-absolute-url-and-dynamic-form-data-urls) | `A` | attribution: 542 -> 6 reversals on a 100-row page, and redundant reversals to zero | `787c32afc` |
| 69 | [Prefetch connected endpoints on the Device LLDP neighbors tab](methodology.md#prefetch-connected-endpoints-on-the-device-lldp-neighbors-tab) | `A` | attribution: 875 queries on one page, 17.857 per interface, 484 in two shapes | `b89fc35f5` |
| 70 | [Sort ContentTypesColumn in Python so it keeps its prefetch cache](methodology.md#sort-contenttypescolumn-in-python-so-it-keeps-its-prefetch-cache) | `A` | attribution: exactly 1.000 content-type query per rendered row | `a15cb3efc` |
| 71 | [Keep the to-many hop when a table column's accessor walks a ForeignKey first](methodology.md#keep-the-to-many-hop-when-a-table-columns-accessor-walks-a-foreignkey-first) | `A` | attribution: 1.000 q/row on the one affected column, 0.000 after | `51b25c317` |
| 72 | [Add an explicit columns= kwarg to BaseTable](methodology.md#add-an-explicit-columns-kwarg-to-basetable) | `A` | enabling change: makes a hidden column's per-row cost measurable at all | `872947fba` |
| 73 | [Prefetch relationship associations and their peers, and compare relationships by id](methodology.md#prefetch-relationship-associations-and-their-peers-and-compare-relationships-by-id) | `A` | attribution: 4.200 queries per rendered row on one relationship column | `4fbd24fad` |
| 74 | [Let models declare what their display reads, and prefetch it in tables](methodology.md#let-models-declare-what-their-display-reads-and-prefetch-it-in-tables) | `A` | attribution: 1.000 q/row on DeviceTable.device_type in isolation, 0.000 after | `d528c25c7` |
| 75 | [Extract BaseTable.replace_queryset from add_conditional_prefetch](methodology.md#extract-basetablereplace-queryset-from-add-conditional-prefetch) | `A` | enabling change: the seam the per-table commits are written against | `3e7d7e240` |
| 76 | [Batch the per-row hierarchy lookups on Prefix and Location tables](methodology.md#batch-the-per-row-hierarchy-lookups-on-prefix-and-location-tables) | `A` | attribution: 3.000 q/row on prefixes and 1.973 on locations, both to 0.000 | `68ea05b49` |
| 77 | [Prefetch what table columns read through properties and templates](methodology.md#prefetch-what-table-columns-read-through-properties-and-templates) | `A` | audit: 55 offending columns across 4 tables, the 6 outside the interface tables to zero | `10efc7958` |
| 78 | [Make cable and connection tables carry their own queryset optimizations](methodology.md#make-cable-and-connection-tables-carry-their-own-queryset-optimizations) | `A` | audit: the 1.750 q/row floor on every interface-table column, and 8.000 on CableTable | `5c9a5bbd1` |
| 85 | [Render a static choice list's options once per process instead of per request](methodology.md#render-a-static-choice-lists-options-once-per-process-instead-of-per-request) | `A` | byte-identical output, proved against django's own Select.render, with the cost fitted | `c68554961` |
| 86 | [Fetch the filter drawer when it is opened instead of rendering it into every page](methodology.md#fetch-the-filter-drawer-when-it-is-opened-instead-of-rendering-it-into-every-page) | `C` | three arms over three alternating rounds, against an inert control that moved −0.5% | `10edaaa95` |
| 88 | [Upstream #9501 adds a Job lookup to every UI list view, and runs it twice on device lists](methodology.md#upstream-9501-adds-a-job-lookup-to-every-ui-list-view-and-runs-it-twice-on-device-lists) | `A` | Tier 1 over two alternating rounds, plus SQL-shape capture across three refs | - |

Every commit above is on `perf/recommended` at `git@github.com:kevsmith/nautobot`.

## Rejected (15)

Measurement or blast-radius analysis ruled these out. Listed because a rejected optimization prices an option someone would otherwise retry.

| # | Change | Tier | Reason |
|---:|---|---|---|
| 15 | [Pre-warm natural-key FK chains with one select_related](methodology.md#pre-warm-natural-key-fk-chains-with-one-select-related) | `A` | +62% api.interface.depth1 |
| 16 | [Stop double-serializing every change record by writing an empty object_data](methodology.md#stop-double-serializing-every-change-record-by-writing-an-empty-object-data) | `C` | Tier C on two public API contracts |
| 17 | [Cross-request Redis-backed natural-key map with signal invalidation](methodology.md#cross-request-redis-backed-natural-key-map-with-signal-invalidation) | `B2` | unbounded staleness: bulk_update emits no invalidation signal |
| 18 | [Enable LOCATION_NAME_AS_NATURAL_KEY](methodology.md#enable-location-name-as-natural-key) | `C` | Tier C; an operator setting rather than a code change |
| 19 | [Process-level per-user nav menu cache](methodology.md#process-level-per-user-nav-menu-cache) | `B2` `security-visible` | authorization decisions in a process-level cache, for ~0.6ms |
| 20 | [Drop the request argument in render_component_template](methodology.md#drop-the-request-argument-in-render-component-template) | `C` | Tier C for ~1ms once the per-request memo is in place |
| 21 | [Make tab badges reuse the panel paginator COUNT](methodology.md#make-tab-badges-reuse-the-panel-paginator-count) | `C` | no measurable change |
| 23 | [Backfill object_data_v2 for historical ObjectChange rows](methodology.md#backfill-object-data-v2-for-historical-objectchange-rows) | `A` `M2` | correctness: v1 rows cannot reconstruct v2 natural keys |
| 24 | [Index maintenance on extras_objectchange, priced at a ~0 ceiling](methodology.md#index-maintenance-on-extras-objectchange-priced-at-a-0-ceiling) | `A` `M0` | −1.7% ceiling, inside the ordering artifact |
| 27 | [Batch get_prev_change for deferred change logging](methodology.md#batch-get-prev-change-for-deferred-change-logging) | `B1` | −3.1% ceiling, bulk update |
| 41 | [Natural-key map on three more traversed-into models](methodology.md#natural-key-map-on-three-more-traversed-into-models) | `A` | −0.4% across the write surface |
| 49 | [savepoint=False in defer_cable_path_rebuilds() — measured, works, rejected on the trade](methodology.md#savepointfalse-in-defer-cable-path-rebuilds-measured-works-rejected-on-the-trade) | `B1` | not measured — inconclusive at a 4-statement effect |
| 57 | [The nav menu's residual 20ms has no hot spot, only 3,629 template nodes](methodology.md#the-nav-menus-residual-20ms-has-no-hot-spot-only-3629-template-nodes) | `A` | attribution: 3,629 node renders averaging ~7us, no node type above 121us/render |
| 79 | [A for_nested_serialization flavour of the cable-peer and connection prefetches](methodology.md#a-for-nested-serialization-flavour-of-the-cable-peer-and-connection-prefetches) | `A` | not measured: rejected on query count, which moved the wrong way |
| 83 | [Deferring the filter drawer is worth 46ms on a list view, measured by removing it](methodology.md#deferring-the-filter-drawer-is-worth-46ms-on-a-list-view-measured-by-removing-it) | `C` | the drawer removed outright on a throwaway arm, three alternating rounds |

## Reverted after landing (1)

Accepted and applied, then removed once a later finding made the mechanism inert. These are not rejections: each was measured, correct, and worth taking at the time. They are listed separately because a reader totalling the branch's effect must not count them, and because the reason one finding can strand another is worth seeing.

| # | Change | Tier | Reason |
|---:|---|---|---|
| 31 | [Fix the tag cache in serialize_object, which can never fire for an untagged object](methodology.md#fix-the-tag-cache-in-serialize-object-which-can-never-fire-for-an-untagged-object) | `C` | correctness: the tag cache could never fire; −6.9% bulk create |


## Methodology

An isolated stack (`DEBUG=False`, `PLUGINS = []`, pinned CPU and memory, served under uwsgi)
against databot `enterprise-campus / large / seed 42` (24,091 objects) for reads and
`datacenter / large` (11,578 rows) for the whole-workflow write measurements. Every aggregate
above is a stock-versus-branch delta taken on one host with the arms alternated and both trees
proved different by content hash before each arm, computed over measurements present on both
sides. The read figure comes from a 57-scenario loop that measures each list view twice: the
document, and the separate request the browser fires to render its rows. The write figures come
from a screen enumerating every POST endpoint the URL resolver exposes, plus one end-to-end
apply that has no read equivalent. A whole test suite of 17,505 tests passed against the same
tree content hash these measurements were taken against. Absolute figures are not comparable to
any other machine.

## Observations

- Query count cannot see this work. The request that renders a list view's rows got faster on
  **18 of 18 pages, a median 18.6%**, while its query count stayed identical on every one of
  them. The time removed is almost exactly proportional to how much rendering a page does
  (**correlation 0.97** between a page's cost and the milliseconds saved), which is what
  removing a per-row overhead looks like. The rejected FK pre-warm did the reverse: 666 fewer
  queries, 62% slower.
- The database is not the bottleneck. PostgreSQL executed 325,152 queries in 4.6 seconds across
  a full baseline run, so every second of user-visible latency measured here is Python-side and
  addressable in application code.
- Five findings exist only because an affordance was added and its call sites were never
  updated, the one pattern here with a repeatable cause and a far smaller search space to screen
  than 166 endpoints by cost.

---

Measured against `nautobot/next` at `3f77053a7` (3.3.0a0) on an isolated stack with pinned resources. Harness, workload definition, findings and baseline data are on the `perf/experiments` branch under `perf/`; every scenario and operation above is reproducible with `perf/scripts/run_experiment.sh`. Instruments, baselines and per-finding working are in `perf/methodology.md`.

Both files are generated. Edit `perf/findings/*.yml` for numbers and `perf/report.template.md` or `perf/methodology.template.md` for narrative, then run `perf/scripts/build_report.py`. `--check` exits non-zero when they have drifted apart.
