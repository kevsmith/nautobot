# Optimization opportunities in Nautobot core

Measured read and write path optimizations, each one applied to a branch and priced. Every
number came from a reproducible harness against a fixed dataset on one dedicated host. The
instruments, the baselines and the working behind each change are in
[`perf/methodology.md`](methodology.md).

| | |
| --- | --- |
| Measured against | `next` at `ce01a0464` · 3.3.0a0 |
| Dataset | databot `enterprise-campus / large` for reads, `datacenter / large` for writes |
| Changes proposed | 41 |
| Accepted on this branch | **29** |
| Rejected | 12 |
| Experiments recorded | 56, of which 15 measured the harness itself |

> Generated by `perf/scripts/build_report.py` from `perf/findings/` and `perf/baselines/`. Do not edit this file.
>
> Sources: tree `dd23568aa` with 3 dirty source path(s), at generation time; readloop from `tier1-57-stock.json` + `tier1-57-branch.json` + `tier2-57-stock.json` + `tier2-57-branch.json`; readscreen from `perf/baselines/screen-reads-ab.json`; writes from `perf/baselines/screen-writes-ab.json`; apply hand-entered in `perf/baselines/cumulative.json`, tree `9c9c64dbb`; per-change figures from `perf/findings/*.yml`.

## Cumulative effect

Stock `next` against this branch, one box, arms alternated, both trees proved different by content hash before each run. Every endpoint is weighted equally, so these are totals over a workload rather than a prediction of what any one user gets back.

| Measurement | Coverage | Queries | Database time | Wall clock |
|---|---|---|---|---|
| Read loop, 57 scenarios | 57 scenarios on both arms | 3,597 -> 1,729 (−51.9%) | - | 35,252 ms -> 20,777 ms (−41.1%) |
| Read screen, every REST read endpoint | 516 of 518 measurements | 15,035 -> 4,681 (−68.9%) | 7,023 ms -> 4,800 ms (−31.7%) | 67.4 s -> 32.3 s (−52.0%) |
| Write screen, every POST endpoint | 254 of 257 measurements | 22,578 -> 19,027 (−15.7%) | 11,645 ms -> 10,009 ms (−14.1%) | 57,921 ms -> 42,920 ms (−25.9%) |
| Whole-workflow apply, 11,572 objects | 11,572 objects across 68 models, row counts identical on both arms | 845,430 -> 673,249 (−20.4%) | 33,872 ms -> 26,634 ms (−21.4%) | 1,948 s -> 1,194 s (−38.7%) |

- **Read loop, 57 scenarios**: median of three alternating rounds per arm on one host, both trees proved different by content hash before every arm, the stock arm hashing next's own `e308df687`. The scenario set includes the 18 row-rendering requests finding 44 showed were never measured, and their wall clock falls a median -28.6% on -22.1% queries -- **four of the 18 now change query count** (every device-list variant, finding 53), where an earlier version of this row claimed the branch changed not one query on any of them. That claim was true when written and is false now. The document half of a list view has moved too: a median -25.5% against the -1.3% recorded before; wall clock is the sum of per-endpoint medians over the 57 endpoints answering on both arms, so it is a workload total rather than a per-page figure; 54 of 54 improved, 0 unchanged, **none worse**; the median moved -27.2%; and **38% of the total saving is one item**, `api.interface.depth1`; 3 measurements below 100ms are excluded, where a per-measurement wall figure cannot be trusted for sign (finding 43).
- **Read screen, every REST read endpoint**: median of three alternating rounds per arm, enumerated from the URL resolver at run time rather than from a scenario list -- 518 measurements across 166 endpoints. **This row exists to check whether the read loop's gain generalises**, because the loop's 57 scenarios are largely the ones this branch optimised. It does: the unbiased figure is larger, not smaller. **But 471 of 516 comparable measurements are unchanged on query count** -- the aggregate is carried by 43 that improved, and the wall saving is spread across roughly eight endpoints (top five = 29%), every one of them a `list.depth1` nested serialisation. Two measurements regressed on queries, both `dcim-api:cabletocabletermination-list` (list 5 -> 8, detail 4 -> 7), against **-753 queries and -94.5% across that model's four kinds** -- finding 36's prefetch buying heavily on depth1 and costing three queries on the flat paths, which is the trade `perf/README.md` describes rather than a defect. Its wall reads +24.4% on one kind. Other wall regressions run +0.1% to +3.0%. **Coverage caveat:** 66 of the 166 endpoints return zero rows and another 47 return fewer than ten (`perf/dataset-gaps.md`), so this is breadth over a dataset that does not populate everything -- an empty endpoint measures its fixed cost but cannot show a per-row defect. It is REST-only and in-process, so it speaks for neither the UI nor what a user waits for; the read loop above is the row that does; 43 of 516 measurements improved on query count, 471 unchanged, 2 worse; query savings run **20.6% to 98.0%** per measurement, median 71.0%; on wall clock 92 of 97 improved, 2 unchanged, 3 up to +24.4%; the median moved -37.3%; and **7% of the total saving is one item**, `dcim-api:cable-list list.depth1`; 419 measurements below 100ms are excluded, where a per-measurement wall figure cannot be trusted for sign (finding 43).
- **Write screen, every POST endpoint**: median of three alternating rounds per arm. Matched on `(id, kind)` rather than id alone -- the three kinds (`create.x1`, `create.x10`, `update.x1`) make 257 measurements from 105 endpoints, and keying on id alone silently collapses them to 105. **This is the first run of this screen since the harness moved into `perf/scripts/`**: the invocation path was stale, so the script had not run at all, which also meant its arms had never actually alternated despite its own header requiring it. Both defects were fixed before this run, and the unbiased result reproduces the biased one to within about one percentage point on all three metrics -- so the ordering defect was real and its effect on this instrument was not; 192 of 254 measurements improved on query count, 62 unchanged, **none worse**; query savings run **1.3% to 45.0%** per measurement, median 7.8%; on wall clock 129 of 134 improved, 2 unchanged, 3 up to +1.3%; the median moved -16.4%; and **12% of the total saving is one item**, `dcim-api:interfaceredundancygroupassociation-list create.x10`; 120 measurements below 100ms are excluded, where a per-measurement wall figure cannot be trusted for sign (finding 43).
- **Whole-workflow apply, 11,572 objects**: four arms in a genuine stock/branch/branch/stock alternation, each from a database reset to the same empty template, the client driven from albert so it cannot compete with the stack it is measuring. Every arm created exactly 11,572 objects and ended with identical row counts -- 536 devices, 1,616 interfaces, 1,648 cables -- with zero client failures, so the arms provably did the same work. **Of the 754 seconds saved, 7.2 are database execution.** Figures are the median of two arms per side; within-side agreement is 0.08-0.26% on wall clock and 0.45-0.95% on queries. **The wall-clock figure is no longer provisional**: the earlier row rested on one stock arm run branch-branch-stock, and the two further stock arms it asked for are here with the ordering reversed. Reproducibility across nights is essentially exact on the deterministic half -- the second stock arm landed within 19 queries of the previously recorded stock arm, 0.002%. Both stock arms hashed next's own `e308df687`: `arm_control.sh` now removes files a path-scoped checkout cannot delete, so the earlier note about the stock arm hashing `0e1d03a4e` no longer applies; measured against tree `9c9c64dbb`.

## Tiers

Tiers price the adoption cost rather than filter on it. Nothing here is disqualified for being
expensive; it is labelled so the price is visible. A flag alongside a tier carries the part of
the risk the tier cannot express.

| Tier | Adoption cost |
|---|---|
| `A` | no observable change |
| `B1` | state scoped to a request, transaction or instance |
| `B2` | state outliving its scope, needs invalidating |
| `C` | changes observable behaviour |
| `M0` | metadata-only migration |
| `M1` | bounded single-pass migration |
| `M2` | unbounded migration |

| Flag | What it adds to the tier |
|---|---|
| `security-visible` | Touches an authorisation decision rather than a displayed value, so a stale or shared result is a permissions bug. |
| `third-party-coupled` | Reimplements or depends on internals of a dependency, so an upgrade can change behaviour rather than break a signature. Correct against the pinned version, and a differential test now renders the same cell both ways so a divergence fails a test instead of producing wrong output. |

All commits referenced below can be found on the [`perf/recommended`](https://github.com/kevsmith/nautobot/tree/perf/recommended) branch of this repo.

## Accepted (29)

Measured, kept, and applied to the tree. Each Reason is that change measured on its own, and several reduce the same cost by different routes -- they do not sum, and the cumulative table above is the measured total.

| # | Change | Tier | Reason | Commit |
|---:|---|---|---|---|
| 01 | [Make the natural-key fallback lazy, so it is not computed and discarded per row](methodology.md#make-the-natural-key-fallback-lazy-so-it-is-not-computed-and-discarded-per-row) | `A` | −25.1% api.interface.list | `559cabf72` |
| 02 | [Prefetch nested natural keys, and fetch each cable path once](methodology.md#prefetch-nested-natural-keys-and-fetch-each-cable-path-once) | `B1` | −26% api.interface.depth1 | `4d016d955` |
| 03 | [Render each detail-page component once per request, not three times](methodology.md#render-each-detail-page-component-once-per-request-not-three-times) | `B1` | −186 queries across two detail pages; no wall-clock A/B was run | `a80ef1d98` |
| 04 | [Memoize natural-key lookups for the duration of one serialization](methodology.md#memoize-natural-key-lookups-for-the-duration-of-one-serialization) | `B1` | −89% Constance reads per create loop; −13% bulk create | `5c12cc9de` |
| 05 | [Extend the natural-key lookup cache across a whole read request](methodology.md#extend-the-natural-key-lookup-cache-across-a-whole-read-request) | `B1` | −48% api.interface.list, under load | `be9b3bdb5` |
| 06 | [Stop paying Redis round-trips for tree display and config lookups](methodology.md#stop-paying-redis-round-trips-for-tree-display-and-config-lookups) | `B2` | −27% api.location.list | `ddb8bc1bb` |
| 07 | [Compile each TemplateColumn once instead of once per table cell](methodology.md#compile-each-templatecolumn-once-instead-of-once-per-table-cell) | `B1` `third-party-coupled` | −18.5% ui.device.interfaces | `1ee6226bd` |
| 08 | [Cut the residual duplicate queries on object detail pages](methodology.md#cut-the-residual-duplicate-queries-on-object-detail-pages) | `B1` | −46 queries on detail pages; ~−9% on an independent A/B | `05bb2c9b1` |
| 09 | [Build nested serializers once, and share one instance per cabled row](methodology.md#build-nested-serializers-once-and-share-one-instance-per-cabled-row) | `B1` | −45.8% api.interface.depth1 | `be22f09bb` |
| 10 | [Walk tree ancestor chains in blocks instead of one query per level](methodology.md#walk-tree-ancestor-chains-in-blocks-instead-of-one-query-per-level) | `A` | −8 queries on detail pages; below what wall clock could resolve | `436f1ef6c` |
| 11 | [Use the caching TemplateColumn everywhere, not just device tables](methodology.md#use-the-caching-templatecolumn-everywhere-not-just-device-tables) | `A` `third-party-coupled` | consistency: 67 call sites left on the uncached column | `3eda79d26` |
| 12 | [Build the nav menu once per request instead of once per component](methodology.md#build-the-nav-menu-once-per-request-instead-of-once-per-component) | `A` | −24% ui.device.detail, −25% ui.rack.detail | `e388b1fde` |
| 13 | [Reuse one API serializer per model when change-logging a transaction](methodology.md#reuse-one-api-serializer-per-model-when-change-logging-a-transaction) | `B1` | −28% bulk create | `9924ece48` |
| 14 | [Resolve natural keys from a whole-table map for small reference models](methodology.md#resolve-natural-keys-from-a-whole-table-map-for-small-reference-models) | `B1` | −15% api.interface.depth1 | `dfc4631f9` |
| 22 | [Stop writing ObjectChange.object_data, with the column made nullable](methodology.md#stop-writing-objectchangeobject-data-with-the-column-made-nullable) | `C` `M0` | storage: object_data is 11.8% of the changelog table | `cb1c6c454` |
| 25 | [Join parent_bay when serializing nested Devices](methodology.md#join-parent-bay-when-serializing-nested-devices) | `A` | −5.8% api.interface.depth1 | `ad1e84161` |
| 26 | [Prefetch cable paths for peer terminations that are not on the page](methodology.md#prefetch-cable-paths-for-peer-terminations-that-are-not-on-the-page) | `B1` | −14.4% api.interface.depth1 | `13273fc7f` |
| 30 | [Prefetch both ends of an interface connection, which the serializer renders in full](methodology.md#prefetch-both-ends-of-an-interface-connection-which-the-serializer-renders-in-full) | `B1` | −61.3% api.interface-connections at limit 50, −49.6% at limit 25 | `60a5f0950` |
| 31 | [Fix the tag cache in serialize_object, which can never fire for an untagged object](methodology.md#fix-the-tag-cache-in-serialize-object-which-can-never-fire-for-an-untagged-object) | `C` | correctness: the tag cache could never fire; −6.9% bulk create | `7d2851d6e` |
| 34 | [Prefetch the cable walk for power- and console-connections, the two endpoints finding 30 left](methodology.md#prefetch-the-cable-walk-for-power--and-console-connections-the-two-endpoints-finding-30-left) | `B1` | −51.0% power-connections at limit 50, −42.7% at limit 25 | `c51de5381` |
| 36 | [Prefetch the cable's terminations for cable-to-cable-terminations at depth 1](methodology.md#prefetch-the-cables-terminations-for-cable-to-cable-terminations-at-depth-1) | `B1` | −75.6% at limit 50, −68.1% at limit 25 | `15a15f77f` |
| 42 | [Route natural_key() through the ancestor walk Nautobot had already optimized](methodology.md#route-natural-key-through-the-ancestor-walk-nautobot-had-already-optimized) | `A` | consistency: natural_key() bypassed the optimized ancestor walk | `85fbb16c4` |
| 48 | [Defer cable path rebuilds in the REST create path, the fourth instance of one shape](methodology.md#defer-cable-path-rebuilds-in-the-rest-create-path-the-fourth-instance-of-one-shape) | `A` | −18 queries on a two-ended REST cable create; wall clock inconclusive | `547fd4e9d` |
| 50 | [Where a list view's 283ms goes, and it is template rendering rather than the view](methodology.md#where-a-list-views-283ms-goes-and-it-is-template-rendering-rather-than-the-view) | `A` | attribution: 91.6% of an empty list view is template rendering, not the view | - |
| 51 | [Resolve the navbar-favorites URLs once per process, not once per menu item](methodology.md#resolve-the-navbar-favorites-urls-once-per-process-not-once-per-menu-item) | `B2` | −43.0% on a chrome-only response; 387 URL reversals per page become 18 | `adcda5384` |
| 53 | [Select_related the device list's primary IPs, which its own table cannot see](methodology.md#select-related-the-device-lists-primary-ips-which-its-own-table-cannot-see) | `A` | attribution: 100 ipam_ipaddress queries on a 100-row page, all from one property | `34de5da16` |
| 54 | [Route TenantColumn through the caching TemplateColumn](methodology.md#route-tenantcolumn-through-the-caching-templatecolumn) | `A` `third-party-coupled` | attribution: 100 template compilations per 100-row page, one per cell, from one column | `5c4d04c70` |
| 55 | [Stop compiling SQL for querysets that are empty by construction](methodology.md#stop-compiling-sql-for-querysets-that-are-empty-by-construction) | `A` `third-party-coupled` | attribution: 99 SQL compilations discarded per device-list request, 0 executed from them | `e5f12e7e6` |
| 56 | [Prefetch the home page changelog panel's generic foreign key](methodology.md#prefetch-the-home-page-changelog-panels-generic-foreign-key) | `A` | attribution: 25 of the home page's 79 queries are one panel rendering 15 rows | `d5bd860ef` |

Every commit above is on `perf/recommended` at `git@github.com:kevsmith/nautobot`.

## Rejected (12)

Measurement or blast-radius analysis ruled these out. Listed because a rejected optimization prices an option someone would otherwise retry.

| # | Change | Tier | Reason |
|---:|---|---|---|
| 15 | [Pre-warm natural-key FK chains with one select_related](methodology.md#pre-warm-natural-key-fk-chains-with-one-select-related) | `A` | +62% api.interface.depth1 |
| 16 | [Stop double-serializing every change record by writing an empty object_data](methodology.md#stop-double-serializing-every-change-record-by-writing-an-empty-object-data) | `C` | Tier C on two public API contracts |
| 17 | [Cross-request Redis-backed natural-key map with signal invalidation](methodology.md#cross-request-redis-backed-natural-key-map-with-signal-invalidation) | `B2` | unbounded staleness: bulk_update emits no invalidation signal |
| 18 | [Enable LOCATION_NAME_AS_NATURAL_KEY](methodology.md#enable-location-name-as-natural-key) | `C` | Tier C; an operator setting rather than a code change |
| 19 | [Process-level per-user nav menu cache](methodology.md#process-level-per-user-nav-menu-cache) | `B2` `security-visible` | authorization decisions in a process-level cache, for ~0.6ms |
| 20 | [Drop the request argument in render_component_template](methodology.md#drop-the-request-argument-in-render-component-template) | `C` | Tier C for ~1ms once the per-request memo is in place |
| 21 | [Make tab badges reuse the panel paginator COUNT](methodology.md#make-tab-badges-reuse-the-panel-paginator-count) | `C` | no measurable change |
| 23 | [Backfill object_data_v2 for historical ObjectChange rows](methodology.md#backfill-object-data-v2-for-historical-objectchange-rows) | `A` `M2` | correctness: v1 rows cannot reconstruct v2 natural keys |
| 24 | [Index maintenance on extras_objectchange, priced at a ~0 ceiling](methodology.md#index-maintenance-on-extras-objectchange-priced-at-a-0-ceiling) | `A` `M0` | −1.7% ceiling, inside the ordering artifact |
| 27 | [Batch get_prev_change for deferred change logging](methodology.md#batch-get-prev-change-for-deferred-change-logging) | `B1` | −3.1% ceiling, bulk update |
| 41 | [Natural-key map on three more traversed-into models](methodology.md#natural-key-map-on-three-more-traversed-into-models) | `A` | −0.4% across the write surface |
| 49 | [savepoint=False in defer_cable_path_rebuilds() — measured, works, rejected on the trade](methodology.md#savepointfalse-in-defer-cable-path-rebuilds-measured-works-rejected-on-the-trade) | `B1` | not measured — inconclusive at a 4-statement effect |


## Methodology

An isolated stack (`DEBUG=False`, `PLUGINS = []`, pinned CPU and memory, served under uwsgi)
against databot `enterprise-campus / large / seed 42` (24,091 objects) for reads and
`datacenter / large` (11,578 rows) for the whole-workflow write measurements. Every aggregate
above is a stock-versus-branch delta taken on one host with the arms alternated and both trees
proved different by content hash before each arm, computed over measurements present on both
sides. The read figure comes from a 57-scenario loop that measures each list view twice: the
document, and the separate request the browser fires to render its rows. The write figures come
from a screen enumerating every POST endpoint the URL resolver exposes, plus one end-to-end
apply that has no read equivalent. A whole test suite of 17,505 tests passed against the same
tree content hash these measurements were taken against. Absolute figures are not comparable to
any other machine.

## Observations

- Query count cannot see this work. The request that renders a list view's rows got faster on
  **18 of 18 pages, a median 18.6%**, while its query count stayed identical on every one of
  them. The time removed is almost exactly proportional to how much rendering a page does
  (**correlation 0.97** between a page's cost and the milliseconds saved), which is what
  removing a per-row overhead looks like. The rejected FK pre-warm did the reverse: 666 fewer
  queries, 62% slower.
- The database is not the bottleneck. PostgreSQL executed 325,152 queries in 4.6 seconds across
  a full baseline run, so every second of user-visible latency measured here is Python-side and
  addressable in application code.
- Five findings exist only because an affordance was added and its call sites were never
  updated, the one pattern here with a repeatable cause and a far smaller search space to screen
  than 166 endpoints by cost.

---

Measured against `nautobot/next` at `ce01a0464` (3.3.0a0) on an isolated stack with pinned resources. Harness, workload definition, findings and baseline data are on the `perf/experiments` branch under `perf/`; every scenario and operation above is reproducible with `perf/scripts/run_experiment.sh`. Instruments, baselines and per-finding working are in `perf/methodology.md`.

Both files are generated. Edit `perf/findings/*.yml` for numbers and `perf/report.template.md` or `perf/methodology.template.md` for narrative, then run `perf/scripts/build_report.py`. `--check` exits non-zero when they have drifted apart.
