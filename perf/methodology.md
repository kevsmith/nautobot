# Nautobot performance experiments: methodology and evidence

Every number in [`perf/report.md`](report.md), with the instrument that produced it and the
working behind each finding.

> Generated by `perf/scripts/build_report.py` from `perf/findings/` and `perf/baselines/`. Do not edit this file.
>
> Sources: tree `de3f3324b` with 2 dirty source path(s), at generation time; readloop from `tier1-57-stock.json` + `tier1-57-branch.json` + `tier2-57-stock.json` + `tier2-57-branch.json`; writes from `perf/baselines/screen-writes-ab.json`; apply hand-entered in `perf/baselines/cumulative.json`, tree `bb57841bf`; per-change figures from `perf/findings/*.yml`.

## The instruments, and a warning about the word "tier"

Two unrelated taxonomies share the word. Risk tiers (`A`, `B1`, `B2`, `C`, `M0`-`M2`) price
what a change costs to adopt, and are what the report's Tiers table describes. Instrument
tiers (Tier 1, Tier 1W, Tier 2) are harness scripts, numbered in the order they were built,
with no hierarchy of authority implied. Prefer the names below, which say what each one
measures.

| Name | Script | Measures | Cost |
|---|---|---|---|
| Read query profile (Tier 1) | `tier1_queries.py` | SQL per read scenario, in-process | seconds, deterministic |
| Write query profile (Tier 1W) | `tier1w_writes.py` | SQL per write operation, in a rolled-back transaction | seconds, deterministic |
| HTTP latency (Tier 2) | `tier2_latency.py` | wall clock over real HTTP against uwsgi | minutes, needs a quiet host |
| In-process latency | `bench_endpoints.py` | wall clock with no HTTP layer | minutes, needs a quiet host |
| Read screen | `screen_reads.py` | every REST list endpoint, ranked per object | minutes |
| Write screen | `screen_writes.py` | every POST endpoint, ranked per created object | minutes |

Tier 2's docstring still describes it as the slow, noisy confirmation of a Tier 1 result. At
concurrency 1 it holds a 1.6% median spread and agrees with in-process timing to 1.08x, which
makes it a peer instrument. The scripts keep their names because dozens of finding records cite
them as historical fact, and renaming the committed baseline files would break the provenance
those records depend on. A rename is on the queue.

## Test environment

### The stack

An isolated stack on port 8180 with `DEBUG=False`, no Apps enabled (`PLUGINS = []`), and pinned
CPU and memory, so results reflect production-shaped code paths and every finding is
attributable to core. Served under uwsgi rather than `runserver`, because the dev image's CMD
is one threaded, GIL-bound process and production is not.

Those settings are not incidental. They live in `development/docker-compose.perf.yml` and
`development/nautobot_config.py`, and a checkout without them measures a different program.
`DEBUG=True` alone loads the debug toolbar, enables SQL logging, and has Django append every
query to `connection.queries` for the life of the process.

### The dataset

databot's `enterprise-campus / large / seed 42`: **24,091 objects**, comprising 2,902 devices,
8,925 interfaces, 3,278 cables, 110 locations, 595 prefixes, 2,937 IP addresses and 36,552
existing ObjectChange records, applied over REST in 1,013 seconds.

A `datacenter / large / seed 42` dataset (11,578 rows) is also kept, and is what the
whole-workflow write measurements use. Its shape is very different, patch-panel heavy, with
864 front ports and 864 rear ports that the campus dataset has none of, which is why per-model
costs transfer between the two but weightings do not.

Round one ran against `enterprise-campus / small / seed 42` (2,113 objects). Those figures are
history and are marked as such wherever they appear.

### The workload

Hand-maintained rather than auto-generated. Endpoints are named by Django view name and
resolved through `reverse()`, and objects are selected by strategy at run time, never a
baked-in primary key, so the workload survives a reseed and fails loudly if a view is renamed.

The inner loop is 38 read scenarios and 13 write operations. Two screening instruments cover
the surface the inner loop does not: `perf/scripts/screen_reads.py` enumerates every REST list
endpoint from the URL resolver and measures 518 read scenarios, and
`perf/scripts/screen_writes.py` does the same for the 152 endpoints that accept POST. Both
normalize to cost per object, and both rank rather than gate.

### Each fix class is invisible to the other instruments

| Instrument | Catches | Blind to |
|---|---|---|
| SQL query count | N+1s, missing prefetch | Python and Redis work |
| Redis backend reads | config and cache round-trip storms | SQL and pure CPU |
| Wall clock, in-process | everything Django does | the HTTP layer; reproduces to a median 1.7% spread across rounds, 7.4% worst case |
| Wall clock, HTTP at concurrency 1 | everything, including HTTP and WSGI | nothing; median 1.6% spread, 5.7% worst case, and agrees with in-process to 1.08x |

Query profiling drives each scenario through the Django test client and counts SQL. It is
deterministic: the same counts reproduced exactly on a second machine with a different CPU
architecture and host OS, which is what makes it usable as a regression gate. Write-path
profiling runs each operation inside a real change context and a rolled-back transaction, so
change logging and signals behave as they would for a live write.

The Redis counter exists because one endpoint was making **1,938 Redis round-trips against 977
SQL queries**, twice as many cache calls as database calls, entirely invisible to SQL-shaped
tooling.

Three alternating rounds is the minimum for any wall-clock claim. Two rounds were not enough
twice over: a 2 ms "regression" and an 11% "regression" both dissolved on a third round.

## Baseline: read path

Ten most expensive read scenarios of 38, on the large baseline (pristine tree, 24,091 objects).
Duplicates are repeated query shapes after literal normalization.

| Scenario | Queries | Duplicate |
|---|---:|---:|
| `api.interface.depth1` | 1,229 | 1,198 |
| `api.device.list.depth1` | 112 | 101 |
| `ui.device.detail` | 108 | 55 |
| `ui.device.detail.worst` | 104 | 49 |
| `ui.rack.detail` | 99 | 45 |
| `ui.home` | 79 | 32 |
| `ui.prefix.detail` | 79 | 34 |
| `ui.device.interfaces` | 71 | 34 |
| `ui.location.detail` | 68 | 27 |
| `api.interface.list` | 23 | 3 |

All 38 endpoints returned 200, and every query count repeated exactly across runs. List views
are efficient at 9-10 queries each; the cost concentrates in nested API serialization and in
detail pages.

`api.interface.list` issues 23 queries and was still the third-slowest endpoint on the branch:
no N+1, purely CPU-bound work per row. No query-count instrument can see that cost, which is
what shaped the rest of the exercise.

## Baseline: write path

Each operation measured inside a change context and rolled back, on the large baseline.
"Changes" counts ObjectChange records created.

| Operation | Queries | Wall | Changes |
|---|---:|---:|---:|
| `create.device` | 34 | 19.6 ms | 1 |
| `update.device.name` | 35 | 19.9 ms | 1 |
| **`update.device.noop`** | **35** | 19.9 ms | 1 |
| `delete.device` | 174 | 68.2 ms | 2 |
| `create.interface` | 20 | 15.1 ms | 1 |
| `create.ipaddress` | 22 | 12.9 ms | 1 |
| `create.tag` | 10 | 8.3 ms | 1 |
| `bulk.create.x100.loop` | 1,604 | 1,255 ms | 100 |
| `bulk.create.x100.deferred` | 1,507 | 1,115 ms | 100 |
| **`bulk.create.x100.bulk_create`** | **3** | 9.4 ms | 0 |
| `bulk.update.x100.deferred` | 1,675 | 1,152 ms | 49 |
| `bulk.delete.x100` | 2,222 | 1,191 ms | 50 |

`update.device.noop`, a save that changes no field, costs the same 35 queries as a real update,
because change logging runs regardless. The bulk triple isolates what change logging costs: the
same 100 rows take 1,604 queries with logging inline, 1,507 with logging deferred, and **3**
with signals and validation bypassed entirely.

Deferral is the mechanism Nautobot's own bulk-edit views use, and it removes only 6% of
queries.

## Wall-clock references

The absolute figures every future experiment is compared against, on the dedicated measurement
host. No before/after column: these are references, and they replace a Tier 2 instrument that
was measuring something else.

**Under uwsgi at concurrency 1.**

| Scenario | r1 | r2 | r3 | median | spread |
|---|---:|---:|---:|---:|---:|
| `api.interface.depth1` | 1908 | 1884 | 1881 | **1884 ms** | 1.4% |
| `api.device.list.depth1` | 952 | 940 | 950 | **950 ms** | 1.2% |
| `api.interface.list` | 798 | 790 | 806 | **798 ms** | 2.1% |
| `api.device.list.deep` | 718 | 720 | 715 | **718 ms** | 0.8% |
| `api.device.list` | 726 | 718 | 714 | **718 ms** | 1.7% |
| `ui.device.interfaces` | 672 | 668 | 668 | **668 ms** | 0.7% |
| `ui.prefix.detail` | 369 | 370 | 369 | **369 ms** | 0.4% |
| `ui.rack.detail` | 366 | 370 | 365 | **366 ms** | 1.5% |
| `api.cable.list` | 359 | 360 | 360 | **360 ms** | 0.4% |
| `api.prefix.list` | 352 | 358 | 356 | **356 ms** | 1.7% |
| `api.location.list` | 318 | 321 | 318 | **318 ms** | 1.1% |
| `ui.device.detail` | 318 | 310 | 318 | **318 ms** | 2.5% |
| `ui.device.list.deep` | 312 | 312 | 312 | **312 ms** | 0.2% |
| `ui.device.detail.worst` | 312 | 316 | 311 | **312 ms** | 1.4% |
| `ui.device.list.filtered` | 314 | 304 | 308 | **308 ms** | 3.2% |
| `ui.device.list.page100` | 307 | 312 | 308 | **308 ms** | 1.8% |
| `ui.device.list` | 300 | 297 | 303 | **300 ms** | 2.0% |
| `ui.interface.list` | 293 | 294 | 294 | **294 ms** | 0.3% |
| `ui.location.list` | 263 | 256 | 269 | **263 ms** | 4.9% |
| `api.ipaddress.list` | 268 | 253 | 255 | **255 ms** | 5.7% |
| `ui.prefix.list.page100` | 250 | 250 | 250 | **250 ms** | 0.4% |
| `ui.location.detail` | 249 | 251 | 248 | **249 ms** | 1.4% |
| `ui.prefix.list` | 241 | 237 | 238 | **238 ms** | 1.7% |
| `ui.ipaddress.list` | 234 | 234 | 234 | **234 ms** | 0.2% |
| `api.device.detail.depth1` | 231 | 234 | 231 | **231 ms** | 1.3% |
| `ui.home` | 224 | 224 | 224 | **224 ms** | 0.0% |
| `ui.devicetype.list` | 215 | 222 | 214 | **215 ms** | 3.3% |
| `ui.rack.list` | 213 | 220 | 212 | **213 ms** | 4.0% |
| `api.device.detail` | 210 | 208 | 210 | **210 ms** | 1.2% |
| `ui.cable.list` | 192 | 185 | 192 | **192 ms** | 3.9% |
| `ui.circuit.list` | 192 | 192 | 192 | **192 ms** | 0.3% |
| `ui.vlan.list` | 178 | 174 | 180 | **178 ms** | 3.7% |
| `ui.tag.list` | 166 | 170 | 165 | **166 ms** | 2.7% |
| `ui.role.list` | 156 | 151 | 159 | **156 ms** | 5.1% |
| `ui.status.list` | 154 | 152 | 152 | **152 ms** | 1.6% |
| `ui.objectchange.list` | 149 | 150 | 151 | **150 ms** | 1.3% |
| `ui.objectchange.detail` | 136 | 142 | 137 | **137 ms** | 4.4% |
| `ui.search` | 81 | 83 | 81 | **81 ms** | 2.5% |

All 38 scenarios probed 200 before timing, none skipped. Spread across rounds: median **1.6%**, max **5.7%**.

**In-process, same host**, with no HTTP layer, for the endpoints where serialization dominates.

| Scenario | r1 | r2 | r3 | median | spread |
|---|---:|---:|---:|---:|---:|
| `api.interface.depth1` | 1484 | 1449 | 1458 | **1458 ms** | 2.4% |
| `api.device.list.depth1` | 866 | 902 | 905 | **902 ms** | 4.4% |
| `api.interface.list` | 755 | 735 | 733 | **735 ms** | 3.1% |
| `api.device.list` | 650 | 686 | 678 | **678 ms** | 5.3% |
| `ui.device.interfaces` | 624 | 607 | 606 | **607 ms** | 3.0% |
| `api.cable.list` | 338 | 333 | 334 | **334 ms** | 1.3% |
| `api.prefix.list` | 325 | 320 | 324 | **324 ms** | 1.6% |
| `ui.rack.detail` | 333 | 323 | 323 | **323 ms** | 3.2% |
| `api.location.list` | 295 | 286 | 289 | **289 ms** | 3.0% |
| `ui.device.list` | 288 | 277 | 276 | **277 ms** | 4.2% |
| `ui.interface.list` | 277 | 271 | 271 | **271 ms** | 2.4% |
| `ui.device.detail` | 266 | 267 | 266 | **266 ms** | 0.5% |
| `api.ipaddress.list` | 235 | 232 | 230 | **232 ms** | 2.0% |
| `ui.cable.list` | 170 | 169 | 167 | **169 ms** | 1.8% |

Spread across rounds: median **2.7%**, max **5.3%** across 14 endpoints. Reflects the tree as it stands: every experiment measures wall clock, whatever its primary signal, because this host is precise enough that skipping it would only hide a result.

## Findings about the harness itself

15 of the 53 experiments changed the harness rather than Nautobot: new instruments, and measurements of the instruments themselves. They are recorded to the same standard because a measurement is only as good as the thing that took it.

### Screening pass over every read endpoint, and what it found

**28** · `A` · commit `fddc2fc80` · `perf/scripts/screen_reads.py`

The inner loop measures 38 hand-picked scenarios. This enumerates every REST list endpoint from the URL resolver at run time and measures list, list?depth=1, detail and detail?depth=1 for each, normalizing to cost per returned object so a 5-row model and a 6,556-row one are comparable. 518 measurements across 166 list endpoints in 84 seconds, against the tree with all sixteen accepted changes applied -- so these are residual costs, not costs the branch has already removed.

| Instrument | Result |
|---|---|
| **wall clock** | not applicable: an instrument rather than a change |
| coverage | 6 of 166 API list endpoints were named in workload.yml; 160 had never been measured |
| queries | top residual costs per returned object, page of 25: dcim.cabletocabletermination?depth=1 19.5 q/obj (487 queries, 473 duplicates, 1226ms, 6556 rows); dcim.interfaceconnections 14.4 q/obj (359 queries, 345 duplicates, 777ms, 2780 rows, and a clean 9 + 14n fit against page size); vpn.vpntunnelendpoint?depth=1 9.8; circuits.circuit?depth=1 7.3; dcim.interfaceredundancygroupassociation?depth=1 6.6; ipam.ipaddresstointerface?depth=1 5.9 |

**Tests.** n/a; reads only, no mutation, safe to re-run

Two of these are worse per object than anything the inner loop has ever measured, and neither was in it.

**dcim.cabletocabletermination at depth 1** is the largest: 19.5 queries per returned object with 473 duplicates on a page of 25. For comparison, api.interface.depth1 at its original worst was 1,229 queries for 100 objects -- 12.3 per object -- and that endpoint absorbed most of this branch's effort.

**dcim.interfaceconnections is a clean N+1: 9 fixed queries plus 14 per object.** Page-size sensitivity gives an exact linear fit -- 23 queries at limit 1, 79 at 5, 149 at 10, 359 at 25, 709 at 50 -- so the cost is per-row work rather than pagination overhead. On 2,780 rows that is the largest untouched N+1 the screen found.

**Correction to an earlier reading of this record.** It said interfaceconnections showed 'a different cost class' because its query count is identical at depth 0 and depth 1, and that none of the accepted fixes would touch it. Both halves were wrong. The endpoint simply ignores `depth`: diffing the two responses shows the only difference is the pagination `next` link echoing `depth=1`, exactly 8 bytes at every page size. Identical counts across depth therefore say nothing about cost class. It is the ordinary per-row N+1 class this branch has been fixing all along, which makes it more tractable rather than less. powerconnections and consoleconnections share the shape at 5.4 q/obj and presumably the same explanation.

Caveats carried from the design. This weights every endpoint equally, which is a selection-free sample rather than a usage-weighted one -- nobody may list cabletocabletermination at depth 1 in practice, and that is a product question rather than a measurement one. Per-object cost is misleading on pages of one or two objects, where fixed overhead dominates, so the ranking above is restricted to pages of ten or more. And dcim.connected-device returns 400 without its required peer_device and peer_interface parameters, which is correct behaviour for a lookup endpoint rather than a failure.

It is a screening instrument, not a regression gate. 518 measurements do not belong in the inner loop, which stays at 38 scenarios.

### Replace Tier 1W's rolled-back transactions with restore-based isolation

**29** · `A` · `perf/scripts/tier1w_writes.py - perf/scripts/probe_commit_cost.py`

Tier 1W runs each operation inside transaction.atomic() and raises to unwind it, which buys identical starting state -- the bulk triple depends on it, since all three create the same 100 rows with the same names -- at the cost of never committing. With a dedicated measurement host and a fast snapshot restore, restore-based isolation is affordable and would measure real committed writes.

| Instrument | Result |
|---|---|
| **wall clock** | −0.9% commit vs rollback, inside variance |
| queries | rollback 1323, commit 1321 -- a 2-query difference, the ROLLBACK/COMMIT statement itself |

**Wall clock.** bulk.create.interfaces.x100.deferred, three rounds each way with a snapshot restore before every arm so each started from byte-identical state, and arm order reversed between rounds. rollback 3389/3312/3407ms, commit 3343/3358/3367ms. Medians 3389 against 3358 -- a 31ms difference on a 3.3-second operation, inside the run-to-run spread.

The first attempt at this measurement was discarded, and the reason is the useful part. It ran three rollback reps then one committed rep, so the committed arm was fourth and warmest, and reported commit 5% FASTER. Reversing the arm order flipped the sign: in both round 1 and round 3 the second arm won by ~46ms regardless of which arm it was. That is pure ordering effect, and it validated the arm-reversal rule within an hour of the rule being written.

**Why not.** Not worth changing, because the thing it would fix is not costing anything measurable. Rollback under-reports wall clock by 0.9% on a bulk create, inside variance, so Tier 1W's existing figures stand. Rollback also keeps Tier 1W fast enough to sit inside run_experiment.sh --writes as an inner-loop gate.

**The second half of this reason was wrong, and finding 33 falsified it.** It said restore-based isolation would add ~70 seconds per arm. That priced the restore at restore_snapshot.sh, which is 49 seconds and does four things per iteration a measurement loop does not need. A template clone returns the database to baseline in 1.3 seconds plus one discarded warmup request, so the true cost is under 3 seconds per arm, not 70.

The finding stands anyway, because cost was never the primary reason. The primary reason is the first half and it is unchanged -- commit against rollback is -0.9% on a bulk create, inside variance, so there is nothing quantitative to gain by switching. What the cheap clone changes is that the option is now available for free if the qualitative gap below ever matters.

What rollback omits is qualitative rather than quantitative, and that part is still true: transaction.on_commit callbacks never fire, and Nautobot uses them in five places including custom-field job enqueueing at extras/customfields.py:744. An experiment that changed commit-time behaviour -- deferring constraint checks, batching index maintenance to commit -- would measure exactly zero under rollback. Nothing on this branch has done that. Finding 24 is the near miss: index maintenance happens in-transaction, so rollback does see it.

It also does not follow that rollback is fine for the write screening matrix. REST writes cross the process boundary, so a rolled-back transaction in the harness cannot isolate them at all. This result narrows that build rather than blocking it: the in-process ORM half can keep using rollback for speed, and only the REST half needs restore-based orchestration. With finding 33 that orchestration costs 1.3 seconds an iteration, so the matrix can afford it for every operation rather than only where it is unavoidable.

### Tier 1W's wall clock is biased by a GC pause that three alternating rounds cannot cancel

**32** · `A` · commit `d52169a34` · `perf/scripts/tier1w_writes.py - perf/scripts/probe_f31_wall.py`

tier1w_writes.py runs a warmup plus three reps of each operation and reports the wall clock of the LAST rep. A gen-2 cyclic garbage collection costs about 200ms in this process and recurs on a period set by allocation count -- which is deterministic for a given tree. So the pause lands on the reported rep for one arm and not the other, and it does so in every independent run of that arm. The arm-reversal rule cancels monotonic drift; it cannot cancel a bias that is not random.

| Instrument | Result |
|---|---|
| **wall clock** | not applicable: a measurement artifact rather than a change |
| incident | finding 31 read create.device at 89/89/89ms unpatched and 241/250/248ms patched, three rounds, both orderings -- a 179% regression that reproduced perfectly and was not real. The same operation measured over nine reps costs 83.7ms and 79.8ms. db_ms was flat at ~24ms in both arms, which is what said the cost was not in the database. |
| mechanism | 24 consecutive calls, gc counters read between each: 329ms on the cold first call, 122ms typical, and one 323ms call carrying a gen-2 collection. Median 122ms, min 111ms, max 329ms. |
| exposure | ~200ms on operations tier1w reports between 25ms and 300ms, where it is up to 8x the true cost; ~6-7% on the ~3s bulk operations. |

**Wall clock.** The fix is an instrument, not an edit to tier1w: perf/scripts/probe_f31_wall.py runs nine reps of each write operation and reports the median, min, max and per-rep timings, so one pause cannot move the answer. Observed round-to-round spread on that instrument is 6-13% max-to-min within a round, and the round medians agree to about 2.6% across rounds -- which is the noise floor finding 31's controls independently reported.

**Tests.** n/a; a measurement artifact, no product code involved

Query counts are untouched by this. The collector issues no SQL, so every deterministic counter this branch has published stands, including the ones taken with tier1w. Only its per-operation wall_ms is affected.

Two earlier findings are worth re-reading in this light, and both survive. Finding 24 measured dropping all 17 non-unique ObjectChange indexes at −59ms on a 3358ms operation and called the real cost indistinguishable from zero; a ~200ms artifact only strengthens that conclusion. Finding 29 measured commit against rollback at 31ms on a 3.3-second operation and called it inside variance; same. Neither claimed a win that this would erase. This retires the idea that any tier1w wall figure smaller than a few hundred milliseconds meant anything.

tier1w_writes.py is deliberately left alone. Changing it to report a median would silently redefine every wall figure in the committed baselines, and its job is the deterministic counters, which are correct. The wall-clock instrument is separate and says so.

Found by investigating a regression rather than averaging it away. The honest reading of 89ms against 248ms, three rounds, both orderings, is that the change made create.device three times slower -- and the branch's own rule is to explain a tripwire rather than accept or dismiss it.

### Reset the database by cloning a template, not by replaying the snapshot

**33** · `A` · `perf/scripts/reset_db.sh - perf/scripts/probe_clone_warmup.sh`

restore_snapshot.sh takes 49 seconds, and four of its five phases are things a measurement loop does not need every iteration. CREATE DATABASE ... TEMPLATE copies the files instead, which returns the database to baseline in 1.3 seconds with the app left running. Per-operation isolation stops being something to design around and becomes something to buy every iteration.

| Instrument | Result |
|---|---|
| **wall clock** | not applicable: an instrument rather than a change |
| phases | restore_snapshot.sh measured by phase: container stop 5.9s, drop/create 0.5s, psql replay 19.1s, container up 12.8s, migrate no-op 9.6s -- 49s total. The clone replaces all but the drop/create. |
| clone | 1.41 / 1.33 / 1.23s over three runs; template build is a one-time 3.7s |
| warmup | cold-cache penalty is confined to the first request after a clone and nothing after it. Position 1 median 981.8ms against a 706.4ms warm control (+275.4ms); position 2 onward is indistinguishable, deltas within ±40ms on a ~710ms operation. Per-round position 1 was 719.6 / 981.8 / 1138.9ms -- highly variable, which fits connection re-establishment plus buffer warming rather than a fixed cost. |

**Controls.** app survives the swap with no restart: DROP DATABASE ... WITH (FORCE) evicts the pooled connections and Django reconnects on the next request despite CONN_MAX_AGE=300, two consecutive 200s immediately after a clone. celery_worker and celery_beat both return to healthy after a clone with zero errors in the logs. Every request in the warmup probe is status-checked, so a clone that left the app unable to reach the database would fail the probe rather than read as a fast 500.

**Tests.** n/a; harness only, no product code

The warmup convention that follows from the measurement: discard exactly one request after a clone. One throwaway request costs about a second and removes the whole effect, and without it every model in the write matrix would carry a variable few-hundred-millisecond bias -- in the instrument built specifically to make those numbers trustworthy.

restore_snapshot.sh remains the authority and the slow path. It is what establishes the state the template copies, and what to run when the template is missing or stale.

**The staleness hazard is the reason this needs a guard rather than just a script.** The template is a database at a fixed schema. restore_snapshot.sh defends against schema drift by always running migrate, which is 9.6 of its 49 seconds; a clone that skipped that check would silently hand back a schema behind the code. reset_db.sh fingerprints the migration files in the tree -- names and contents, since names alone would miss a file edited in place -- and refuses to clone when the fingerprint does not match the one recorded on the template. The fingerprint is stored as a comment on the template database rather than in a table, because database comments are not copied by CREATE DATABASE ... TEMPLATE, so the clones stay free of anything the harness added. Verified to fire on both a new migration file and one edited in place, exiting non-zero in each case and recovering afterwards.

It refuses rather than falling back to the slow path. A reset that takes 1s most of the time and 49s occasionally would put a 48-second spike inside a measurement loop at an unpredictable point.

### A container restart biases the measurement that follows it, and arm alternation does not cancel it

**35** · `A` · `perf/scripts/sync.sh - perf/scripts/probe_ui_only.py`

perf/scripts/dc.sh restart returns when the container has started, not when it is ready. uwsgi then forks three workers that each import Nautobot, and the nautobot container is pinned to two physical cores (cpuset 0,1,4,5). An in-process probe launched into that window competes with those imports for the cores it is being measured on. The result is bimodal: the same view reads either ~99ms or ~165ms depending on where the probe lands, the mode is set at process start and then holds for the whole arm, and three alternating rounds does not cancel it.

| Instrument | Result |
|---|---|
| **wall clock** | not applicable: an instrument defect rather than a change |
| no restart | 18 measurements of ui.interface-connections across 6 consecutive runs with no restart between them -- 94.5-106.6ms, a 1.13x spread, query count stable at 8 every time |
| with restart | 99-177ms on the same probe when each run followed a restart, bimodal rather than spread, and the query count went unstable too -- 8, 12 and 13 at the same page size |
| same arm twice | arm B six times gave 99.1-176.7ms; arm A six times gave 98.5-174.0ms. Both arms reach both modes, so the arm is not the variable. |

**Controls.** found on an endpoint that was a control in the finding 34 experiment, whose code path that change never touches. It read +73% with an identical query count, consistently, across three alternating rounds.

**Tests.** n/a; harness only, no product code

How it was nearly believed. In the finding 34 A/B the control read +73% in the patched arm at all three page sizes, with 8 queries in both arms, in all three rounds. Consistency read as signal. It was not -- rerunning the same arm six times reproduces the full 99-177ms range without changing a line of code, and the first A/B simply landed arm B in the fast mode three times and arm A in the slow mode three times.

This is the second time on this branch that a deterministic-looking bias survived arm alternation. Finding 32 was the first, a gen-2 GC pause recurring on a period fixed per tree, so it lands on the reported rep for one arm and not the other in every independent run. Both share a shape worth naming -- arm reversal cancels drift, and cancels nothing keyed to something other than time.

Two fixes, and the first is the real one. An in-process probe never needed the restart. Tier 1 and Tier 1W spawn a fresh Python process per run and pick up synced code without one; only uwsgi-served measurement needs a restart, because uwsgi holds imported modules. The A/B that produced this was restarting between arms for no reason and paying a bimodal artifact for it.

Second, sync.sh --restart now waits for the container to report healthy and then for the load average to fall under the ceiling before returning, and fails rather than handing back a host that is not measurable. Verified -- it refused during an unrelated test run at loadavg 1.71.

What this does not settle is whether earlier numbers on this branch are affected. Any wall-clock figure taken through an in-process probe shortly after a restart carries the risk. Query counts are unaffected, which is most of the branch evidence, and the affected figures would have to be re-taken to know.

### Second read screening pass, and the ranking's blind spot

**37** · `A` · `perf/scripts/screen_reads.py`

Re-run of the screen against 4a925fb43, the tree carrying findings 30, 31, 34 and 36, and directly comparable to the first run: same instrument, same page size of 25, same dataset (8,925 interfaces, 2,902 devices, 3,278 cables), 518 measurements over 166 list endpoints in 82s. Ten of the 518 changed their query count and 508 are byte-identical to the first run, so the four fixes are surgical and the residual ranking below is the first one's ranking with its top two removed.

| Instrument | Result |
|---|---|
| **wall clock** | not applicable: an instrument rather than a change |
| removed | total across the 518 measurements 6,345 -> 4,687 queries (-26.1%) and 3,242 -> 1,596 duplicates. dcim.cabletocabletermination?depth=1 487 -> 15; dcim.interfaceconnections 359 -> 17; dcim.powerconnections 135 -> 12; dcim.consoleconnections 134 -> 11. Both of finding 28's headline targets are gone. |
| surgical | only 10 of 518 measurements moved, all on the four fixed endpoints. Nothing else on the read surface changed by a single query, which is the evidence that the four prefetches have no collateral effect. |
| regression | cabletocabletermination at depth 0 went 5 -> 8 queries on a page of 25 and 4 -> 7 on detail, with db time 169 -> 190ms. Finding 36's prefetches are unconditional and depth-0 serialization never reads them. Fixed cost (three queries: cable__terminations, and the cable_paths__destination pair for the one non-null termination FK on the page), not per-row. |
| residual | top cost per returned object, pages of >=10, unchanged from the first run to two decimals: vpn.vpntunnelendpoint?depth=1 9.76 q/obj (244 q, 226 dup, 37 rows); circuits.circuit?depth=1 7.32 (183 q, 38 rows); dcim.interfaceredundancygroupassociation?depth=1 6.60 (132 q, 20 rows); vpn.vpntermination?depth=1 6.52 (163 q, 37 rows); dcim.cable?depth=1 6.36 (159 q, 145 dup, 3,278 rows); ipam.ipaddresstointerface?depth=1 5.92 (148 q, 130 dup, 2,937 rows) |
| invisible to the ranking | dcim.device list is 8 queries, zero duplicates, and 438ms of database time on a page of 25 -- 55ms per query, the largest db-time item anywhere on the read side. Reproducible: 432ms in the first run, 441ms at depth 1. Per object it is 0.32 q/obj, which puts it nowhere near the ranking. Queries per object cannot see a small number of expensive queries. |
| coverage | 166 list endpoints attempted, 166 measured, 2 non-200 (dcim.connected-device, 400 without its required parameters, which is correct behaviour). But only 49 returned a full page of >=10 objects. 47 returned 1-9 rows and 70 returned zero -- including cluster, virtualmachine, vminterface, module and the whole modules/templates family, tag, service and rir. A zero-row endpoint reads as cheap and is actually unmeasured. |

**Controls.** nine models (jobs, contactassociation, objectchange, users, one wireless assignment) returned different response bytes between the two runs. Row counts and query counts are identical for all nine, so the drift is field-level -- timestamps, last_login, job metadata -- and does not affect comparability.

**Tests.** n/a; reads only, no mutation

The queue asked this pass to answer one question: does anything on the read side still outrank the write path for attention. It does not, and the ranking above is why.

The four highest per-object costs sit on tables of 20 to 38 rows, where one page is the entire table. Fixing them would remove a few hundred queries from a request nobody has evidence anyone makes. Only two residuals sit on tables large enough for the cost to compound -- dcim.cable?depth=1 (3,278 rows, 145 duplicates per page) and ipam.ipaddresstointerface?depth=1 (2,937 rows, 130 duplicates) -- and both are the same per-row N+1 mechanism this branch has now fixed four times, with a known shape and a bounded payoff per endpoint. Against that, the write path has one measured 37% win at whole-workflow scale that nothing has attributed, and zero screening coverage. The queue order stands.

Two things this run found that the ranking method itself would never surface.

**dcim.device spends 438ms in the database over 8 queries with no duplicates.** Every read fix on this branch has been a query-count fix, and this endpoint has almost no queries to remove. It is the largest read-side db-time item measured and the instrument ranks it 100th. A screen that normalizes to queries per object is structurally blind to a slow query, and the fix for that is to rank on db time as well -- one line, since db_ms is already recorded.

**The screen measures 166 endpoints and exercises 49.** 70 of them return zero rows against this dataset, so they contribute a measurement that says nothing while counting as coverage. This is the same failure the queue's item 3 requires the write matrix to avoid, present in the instrument that raised the objection. Either seed the empty models or report attempted / exercised / measured separately, and stop quoting 166.

On the regression. Finding 36 removed 472 queries from the depth=1 path and added 3 to the depth=0 path, which is a trade worth making at roughly 150:1 and was not visible in that experiment because the A/B measured depth=1 only. Making the prefetch conditional on depth is possible -- the viewset can read the requested depth -- but it is three fixed queries on an endpoint nobody has shown is hot at depth 0, so it is recorded rather than queued.

**Correction, from re-reading the committed run while building the coverage accounting.** Three numbers above do not survive a check against `perf/baselines/screen-reads.json`. dcim.device's list endpoint is **432.0 ms** over 8 queries with 0 duplicates, not 438. It is not the largest db-time item in that file: dcim.cabletocabletermination at depth 1 costs 574 ms and dcim.device at depth 1 costs 434 ms. And it ranks 334th of 518 records by queries per object, not 100th -- the earlier figure was taken from a differently-scoped list. The conclusion the paragraph draws is unaffected: a screen normalizing to queries per object cannot see an endpoint whose cost is a few expensive queries, and db_ms was already recorded, so the second ranking is one line. **"The screen measures 166 endpoints and exercises 49" needs its terms separated too.** Of 166 endpoints, 96 return at least one row, 49 return a full page of 10 or more, and 70 return none. 49 is the full-page count rather than the exercised count, and quoting it as coverage understates by 47 endpoints exactly as quoting 166 overstates by 70. The coverage block now reports all four numbers separately.

### Write screening matrix, every POST endpoint ranked on cost per created object

**38** · `A` · `perf/scripts/screen_writes.py - perf/scripts/payloads.py`

The read screen's counterpart, built to the same rules: enumerate from the URL resolver at run time, normalize to cost per object, and never let an unmeasured model read as a cheap one. 152 of the resolver's 166 API list endpoints accept POST; 105 were measured across create.x1, create.x10 and update.x1, for 257 measurements in 211 seconds. The headline is the comparison it makes possible: the *median* marginal cost of creating one object is 12 queries, against a median read cost of 0.28 queries per returned object, and the cheapest create on the entire surface (3.0) is more expensive per object than 47 of the 49 read endpoints that return a full page.

| Instrument | Result |
|---|---|
| **wall clock** | not applicable: an instrument rather than a change |
| surface | 166 API list endpoints, 152 of which accept POST and 14 of which are read-only. Writability is read from the DRF router's own action map on the URL callback, not inferred from the viewset class, so the resolver stays the authority for writes as it is for reads. |
| coverage | 152 attempted / 130 payloads built / 105 payloads accepted / 105 measured. Of the 105, **82 were built from field metadata alone** and 23 needed an entry in the hand-maintained exception list in payloads.py. The 47 unmeasured models break down as: 21 rejected by model validation no generated payload can satisfy ("A Location of type Wing must have a parent Location", "Racks may not associate to locations of type Office"), 19 unbuildable because this dataset has no rows of a required related model (cluster, moduletype, cloudnetwork, virtualserver, savedview), 4 rejected on a uniqueness constraint over foreign keys whose tables hold fewer than ten rows, 2 HTTP 500 (see note), and 1 with no derivable value. |
| ranking | marginal queries per additional object, (x10 - x1) / 9, over the 98 models whose bulk create was accepted: ipam.ipaddresstointerface 65.6 (x1 77, x10 667, 627 duplicates, 372ms db); dcim.interfaceredundancygroupassociation 46.2 (55 / 471, 430 dup); dcim.cable 44.9 (54 / 458, 381 dup, one SELECT on dcim_cabletocabletermination repeated 280 times) -- **but see the correction below, the real figure is ~220**; dcim.device 40.0 (49 / 409, 364 dup, 309ms db); ipam.prefixlocationassignment 36.4; ipam.vlanlocationassignment 30.1; ipam.prefix 30.0; extras.configcontext 30.0. dcim.interface is 23.8. Distribution: min 3.0, median 12.0, max 65.6. |
| dataset independence | the screen was re-run against a completely different dataset (datacenter/large, 11,578 rows, against enterprise-campus/large's 24,098) and the per-object costs are identical to the decimal for 9 of 11 top models -- cable 44.9, interface 23.8, device 40.0, prefix 30.0 all unchanged. Only ipam.ipaddresstointerface moved, 65.6 to 69.0. So the ranking transfers between datasets and only the weights change. Coverage rose from 85% to 92% of datacenter rows because that dataset has rearports and virtual device contexts, which the campus one has none of: dcim.interfacevdcassignment became measurable at 91.2 marginal queries per object, the most expensive create on the surface. |
| fixed cost | create.x1 is 9 queries at its cheapest, 20 at the median and 86 at its worst, so a single-object POST is mostly fixed overhead and the x1 figure alone would rank the surface almost uselessly. update.x1 is 15 / 24 / 77 over 51 models, worst dcim.cable. |
| determinism | 256 of 257 measurements had identical query counts across all three reps. The one exception is ipam.vlanlocationassignment create.x10 at [307, 308]. |
| isolation verified | row counts on dcim_device, dcim_interface, dcim_cable, ipam_prefix, ipam_ipaddress, ipam_namespace and extras_objectchange are byte-identical before and after a full run: 2902 / 8925 / 3278 / 595 / 2937 / 1 / 36552. 257 measured writes, nothing left behind. |

**Controls.** Rollback isolation was measured against the alternative rather than assumed from finding 29. Both modes were run over the five measured dcim.device* models, three rounds each way with the arms alternated (rb, rs, rb, rs, rs, rb), 13 operations compared on medians.

**Query counts differ by exactly -2 on all 13 operations, in every round.** That is the SAVEPOINT/RELEASE pair the outer transaction adds and nothing else -- the same 2-query difference finding 29 measured on bulk create at a completely different scale. The write path itself does not change shape when the transaction commits.

**Wall clock: the committed arm is +3.0% median (range -2.2% to +11.4%).** That is an upper bound on what rollback under-reports, not an estimate of it, because the reset arm carries the residual cold-buffer cost of the clone that precedes it. Finding 29 measured -0.9% on bulk create with a restore before every arm; this is the same answer at the same order of magnitude.

Rollback is available at all only because the requests go through the Django test client and stay in-process. Finding 29's "REST writes cross the process boundary" is true of a real HTTP client against uwsgi and not of this one.

**Change logging is not hidden by the rollback**, and this was checked rather than inherited: Nautobot records ObjectChanges from synchronous post_save receivers, ObjectChangeMiddleware flushes them inside the same request, and there is no transaction.on_commit anywhere on that path. What rollback does omit is the five on_commit callbacks elsewhere in Nautobot, including custom-field job enqueueing at extras/customfields.py:744.

**--isolation reset needed three corrections before it was a control rather than a trap**, and each is a way a write screen can produce confident wrong numbers. It cannot shell out to perf/scripts/reset_db.sh, because this runs inside the container and that script drives docker compose from the host -- so the clone is issued over a second psycopg2 connection, carrying a reimplementation of the migration fingerprint that --verify-reset checks against the shell one (they agree byte for byte). force_login writes a django_session row, so the client has to be logged in again after every clone or every subsequent request is anonymous. And the post-clone throwaway request has to be rolled back even in reset mode: a committed throwaway collides with the measured request on every unique name, which reads as a rejected payload.

**Tests.** n/a; harness only, no product code

Three things this cost more than expected, each of which is now a property of the instrument rather than a fact about one run.

**A rolled-back transaction restores the database, not the process.** The first full run had 47 of 252 measurements with *unstable query counts*, nearly all updates. The cause is process-level caches -- the natural-key field lookups and the tag cache -- which are cold only on the first pass and survive the rollback that resets everything else. One discarded warmup run per operation takes the unstable count from 47 to 1. This is the same discipline finding 33 established for a database clone, arriving from the opposite direction: there the cold thing was PostgreSQL's shared buffers, here it is the Python process, and in both cases the fix is one throwaway request and the cost of skipping it is a number that moves for reasons unrelated to the change.

**DRF's `required` is not the model's `blank`.** A model field with `blank=False` and no default is still `required=False` on the serializer whenever the column is nullable, and `full_clean` then rejects the value the payload left out. Five models -- configcontext, configcontextschema, secret, rackreservation, service -- were being reported as rejected payloads while the builder was doing exactly what the serializer told it. Reading the model field alongside the serializer field is what fixed them.

**A many-related field's child is not always a plain relation.** `content_types` is a many-related field over `ContentTypeField`, whose value is the string "app_label.model"; the first version handed it a UUID and nine models -- status, role, tag, webhook, customfield, metadatatype, jobbutton, cloudresourcetype, objectpermission -- were unmeasurable as a result. Dispatching on the child field rather than assuming a primary key recovered all nine.

**The exception list is counted, deliberately.** 23 of the 105 measured models only got there via an entry in `MODEL_SEEDS`, and the coverage summary reports "82 from field metadata alone, 23 needing a seed entry" rather than one number. The list holds two kinds of knowledge and nothing else: `force`, which names optional fields a `clean()` requires ("Either device or module must be set" -- 14 models, including dcim.interface), and `values`, which supplies the three IPAM addresses that have to fall inside a real prefix in the right namespace. A model is not added to make a number look better.

**A product defect, found as a byproduct.** POST to `vpn.vpnprofilephase1policyassignment` or `vpn.vpnprofilephase2policyassignment` returns HTTP 500 -- "got unexpected keyword arguments: `_custom_field_data`". Both models are plain `BaseModel` and support no custom fields; both serializers are `NautobotModelSerializer`, which puts `custom_fields` in validated_data with a default and passes it to the model constructor. The payload never mentions custom fields, so this is unconditional: those two endpoints cannot be written to at all. Correctness, not performance, and out of scope for this branch -- recorded here because nothing else on the branch would have found it.

**Correction, from finding 40: dcim.cable at 44.9 is wrong by roughly 5x.** The generated payload leaves termination_a_type and termination_b_type null, because both are optional and this builder populates required fields only -- and a cable connected to nothing skips every termination check and cable-path walk. A cable with real terminations costs ~220 queries. This is the "minimal payloads measure the floor" caveat below landing on a top-four model, and it is the first evidence of how large that gap can be. Any model whose expensive work hangs off optional relations is understated here by an unknown factor, and the ranking should be read as a lower bound per model rather than an estimate.

**What the ranking cannot see, stated in advance this time.** It is queries per object, so it is blind to a small number of expensive queries exactly as the read screen was -- finding 37 had to discover that after the fact with dcim.device. `db_ms` is recorded per measurement, so the same second ranking is available here without re-running anything. And the payloads are minimal: only required fields are populated, so every figure is the *floor* cost of a create. Tags, custom field data and relationships are omitted, and they are write work. A populated create costs more than anything reported here.

### The −37% write-path win is real, diffuse, and mostly not SQL

**39** · `A` · `perf/scripts/screen_writes.py - whole-workflow databot apply`

Queue item 1, answered. Kevin measured a datacenter dataset apply at 495s against perf/verified versus 786s against stock next, on his own hardware, single runs. Reproduced here as a controlled A/B on the measurement host: same box, same dataset (datacenter/large, seed 42, 11,578 rows), arms alternated, and the two trees proved different by content hash before each run (a586fc5cf against e308df687). The win is real. It is spread across 40 models rather than concentrated in one finding, and 99.6% of the time it saves is not database execution time.

| Instrument | Result |
|---|---|
| **wall clock** | −33.7% on a whole datacenter apply (−31.3% on the earlier pair, before the client fix in finding 40) |
| applies | stock next 2114s / 2111s; perf/verified 1450s, plus a 1453s pilot before the protocol existed. Within-arm spread is 0.2% on both arms, two orders of magnitude below the effect. Every run created exactly 9,972 objects and ended with identical row counts. |
| headline | −31.3% wall clock (1450s against 2112.5s median), −13.2% queries (756,365 against 871,171, counted server-side by pg_stat_statements), −8.0% server execution time (33,733ms against 36,648ms). |
| where the time goes | of the 662 seconds saved, server-side execution accounts for 2.9 -- 0.44%. The branch's write-path wins are Python, not SQL, which is exactly what findings 13, 4 and 31 are: not rebuilding an API serializer per changed object, not re-resolving natural keys, and a tag cache that was never firing. Query count is a poor proxy for the value of this work. |
| diffuse | the write screen run against both arms on the same dataset -- 260 measurements present on both sides, identical coverage -- gives −12.9% queries, −23.9% wall, −12.3% db time, with **zero measurements worse on perf** and 153 of 260 improved. Largest: dcim.interfacevdcassignment −47.5%, ipam.ipaddresstointerface −29.3%, the device-component family (powerport, poweroutlet, consoleport, consoleserverport) −36.6% each, dcim.interface −24.1%. 40 models contribute; the top three are 75% of the query saving. |
| after the client fix | re-run against a rebuilt databot whose write timeout is 120s rather than 30s, so neither arm hits the cable fallback (0 in both logs, against 16 in every arm before): stock next 1967s / 850,278 queries / 34,445ms, perf/verified 1304s / 723,075 queries / 31,100ms. **−33.7% wall, −15.0% queries, −9.7% server execution.** This is the figure to quote -- same box, same dataset, same client, neither arm distorted.

**Not the same measurement as Kevin's −37%, and an earlier version of this finding said it was.** His run uses a different datacenter dataset on different hardware, so the two cannot be divided into a hardware ratio and neither can be used to corroborate the other's magnitude. They are two independent results pointing the same direction. His own reproducibility is good -- 487.69s against the 495s he first measured, 1.5% apart -- but that is a statement about his box, not about this one. |
| retracted | an earlier version of this finding carried an "adjusted −40.5%", built by subtracting 16 x 30s of supposedly-discarded work from both arms. It was wrong twice over. The timed-out batches were not discarded -- the server committed them and the client confirmed rather than re-created them (see finding 40) -- so there was nothing to subtract, and the measured cost of the client fix is 146s on the perf arm (1450 -> 1304), not 480s. The arithmetic looked clean, which is exactly why it got published. Superseded by the measured pair above. |

**Controls.** Arms alternated (pilot A, B, A, B) and the tree proved different before each run by hashing nautobot/**/*.py, not by trusting the checkout -- the rule that exists because a git stash A/B once silently measured identical code six times. Only nautobot/ is swapped, since perf/ and development/docker-compose.perf.yml do not exist on next; all 37 differing files are modifications, no adds or deletes, so a path-scoped checkout is an exact swap both ways. The checkout is unstaged immediately, because a staged reversion left lying around is how five fixes were undone once. Each arm starts from a byte-identical empty migrated database cloned from a template, the container is restarted (uwsgi has no autoreloader, so without it the next request runs the previous arm's code), and the run waits for loadavg to fall below 0.7 before starting -- finding 35, where three uwsgi workers importing Nautobot on two pinned cores biased a whole arm bimodally.

**Tests.** n/a; measurement only, no product code

The queue proposed answering this by reverting finding 13 alone and re-timing. That would have given one number and a yes/no. Running the write screen against both arms instead gives the per-model breakdown above for the same machine time, and the answer it produces is not the one the yes/no would have suggested: no single finding accounts for the win, so there is no finding-13-shaped thing for the write matrix to go hunting.

**The number that took longest to see.** 662 seconds saved and 2.9 of them are database execution. Every instrument on this branch until now has gated on query count, and query count would have rated this work at −13% when it is worth −31%. The read side does not behave this way -- there, db time and query count track each other closely (finding 38). The write path is where they come apart, and any future write experiment should report both or it will undervalue this class of fix.

**The inference this finding originally carried was wrong.** The first pair of applies both hit a client-side timeout on 16 cable batches. I modelled that as 480 seconds of work thrown away on each arm and subtracted it, producing a tidy "adjusted −40.5%". Two things were wrong: the work was not thrown away (the server committed and the client confirmed), and the real cost was 146s rather than 480s. The measured post-fix pair is −33.7%. An adjustment computed from a counted constant still rests on a story about what the constant means, and that story was never checked. A re-run was affordable here, and it was the better choice than an adjustment.

### A cable create costs 220 queries, and what that broke downstream

**40** · `A` · `perf/scripts/probe_cable_bulk.py - perf/scripts/capture_proxy.py`

Creating one cable with real terminations costs ~220 queries and 0.34 seconds, linear from 25 to 96 cables per request with no batching advantage in queries at all. That is five times what the write screen reports for dcim.cable, because the screen's generated payload leaves both terminations null and a cable connected to nothing skips every termination check and cable-path walk. The cost is also why every datacenter apply logged 16 failed bulk creates: 100 cables takes 35.6s over HTTP against databot's 30s client timeout.

| Instrument | Result |
|---|---|
| **wall clock** | not applicable: a cost measurement and a client-side defect, with no product change |
| per cable | 100 real cables in one array POST: 22,003 queries, 37.1s in-process. The curve is flat per object -- 25 cables 4,750q/8.5s, 50 cables 9,474q/17.0s, 96 cables 18,169q/31.1s and 31.7s -- so 0.34s and ~190-220 queries per cable regardless of batch size. Batching cables saves round trips, not database work. |
| screen understates it | screen_writes.py reports dcim.cable at 44.9 marginal queries per object. That payload has termination_a_type and termination_b_type null, because both are optional and the builder populates required fields only. The real figure is roughly 5x, and dcim.cable is a top-four model in the datacenter apply, so its share is understated everywhere finding 38 quotes it. |
| the timeout | databot's httpx client defaulted to timeout=30.0 (discovery/fetch.py:80). At 0.356s per cable over HTTP the break-even batch is 84 cables and databot batches at 100, so the failure was deterministic rather than intermittent: exactly 16 fallbacks in all three arm logs, 1,648 cables being 16 full batches plus a 48-cable remainder that fits. |
| what it actually cost | 146 seconds, measured -- 1450s before the client fix against 1304s after, on the same tree and dataset. **Not the ~480s an earlier version of this finding claimed.** The timed-out batches were not wasted: the server finished its ~35s and committed, and databot -- which write-aheads every row to the state sidecar before POSTing -- then found all 100 present and confirmed them instead of re-creating them. That is the "skipped 1,600 (resumed)" in every pre-fix arm log, and 1,600 is exactly 16 batches x 100. The cost is the difference between giving up at 30s and the confirmation round-trips, not the work itself. |
| not a nautobot fault | a capture proxy between databot and Nautobot recorded **zero 5xx from Nautobot** across a whole cable phase, and logged BrokenPipeError writing back to databot. Reproduced exactly with curl --max-time 30 on the same payload: exit 28 at 30.0019s, and the server logs the identical hr_write(): Broken pipe. Nautobot completes the request and commits it; the client has stopped listening by then, so only the response write fails. |

**Controls.** Every probe POST runs in a rolled-back transaction and the database is reset from the template afterwards. The 25/50/96 curve was taken in one process without resets between sizes, since nothing is committed. Payloads are rebuilt from cables that already exist, so terminations are real occupied-checked pairs rather than generated ones, and the source cables are deleted inside the same transaction so the terminations are free.

**Tests.** n/a; probes only, no product code

Three wrong turns, and each was wrong in a way worth writing down.

**The first reproduction succeeded and proved nothing.** A 100-cable array POST built by the payload generator returned 201 in 7.8s, in-process and over HTTP. It was measuring a cable with no terminations. The tell was in the response -- termination_a_type: null -- and it was visible the whole time.

**The second reproduction also succeeded, and the timing is what redirected it.** Real terminations gave 201 in 35.6s over HTTP. A request that takes 35s and succeeds, against a production failure that gave up at what the uwsgi log called 8.5s, looked like two different events. It was the same event: uwsgi's msecs figure is not measured from request start, and keying on it cost an hour. The client-side number -- curl exit 28 at exactly 30.0019s against a 30.0s default -- is what settled it, and it was available from the start.

**Reading the server's logs could not have worked.** Django logs "Internal Server Error" for these and Nautobot's exception middleware renders the traceback into a 246-byte body, so neither the container log nor databot's warning ever says what broke. The capture proxy exists because the only copy of the evidence was in flight between two processes.

**A fourth wrong turn, caught by Kevin rather than by me.** I read the clean final row counts -- exactly 1,648 cables, no duplicates -- as evidence that the timed-out transactions rolled back. They are equally consistent with commit-then-confirm, which is what the "skipped 1,600 (resumed)" line said all along and what actually happened. Having decided on rollback, I then costed the timeouts as 480s of discarded work and published an adjusted figure built on it. The row count was real evidence for a claim it did not distinguish between.

**Consequences.** databot now defaults to a 120s write timeout with a --write-timeout flag. 120 rather than 60 because 60s only covers a machine 1.7x slower than this one and a CI runner on shared vCPUs is easily that; the safe batch is roughly timeout x 0.8 / 0.36s / slowdown. (An earlier version justified the margin by calling this host "~2x slower than the one Kevin measured on". That ratio came from dividing two applies of *different datasets* and was never a measurement -- withdrawn, and the sizing argument stands without it.) The timeout value stops mattering if the client halves the batch and retries on timeout instead of dropping straight to per-row -- recorded, not queued.

**For Nautobot, the standing number is 220 queries per cable.** That is not a client problem and no fix on this branch touches it. It is the most expensive per-object create measured anywhere on the write surface, and it is now the strongest candidate at the top of the write ranking.

### Per-measurement wall clock on the write screen has a magnitude floor, and reps do not raise it

**43** · `A` · `perf/scripts/screen_writes.py - perf/scripts/compare_screen.py`

Building the report's cumulative section surfaced 21 measurements that improved on query count and read *slower* on wall clock, worst +42.5%, in a comparison whose aggregate is −23.9%. The write screen already runs three reps after a discarded warmup and reports medians, so those figures are medians rather than single samples and more reps cannot be the fix. Banding the 153 improved measurements by their absolute cost shows why.

| Instrument | Result |
|---|---|
| **wall clock** | not applicable: this measures the instrument rather than a change to Nautobot |
| bands | baseline wall 25-100ms: n=55, median −4.7%, worst +42.5%, 17 of 55 slower. 100-400ms: n=62, median −15.7%, worst +5.4%, 4 of 62 slower. 400ms and above: n=36, median −38.1%, worst −1.9%, **0 of 36 slower**. |
| floor | every one of the 21 wall-slower measurements has a baseline under 265ms, median 48.9ms, against a median of 200.8ms across all 153 improved. The sign of a per-measurement wall figure is unreliable below roughly 100ms and reliable above roughly 400ms. |
| queries unaffected | all 153 improved measurements fell on query count, between 1.8% and 47.5%, median 6.8%. Query counts are deterministic, so the spread there is the result rather than the noise. |

**Controls.** computed from the two committed arms of finding 39 (`screen-writes-dc-next.json` against `screen-writes-dc.json`), both taken at reps=3 with medians, so the bands compare like with like and no new measurement was required

**Tests.** n/a; harness only, no product code

This is finding 32's conclusion arriving on a second instrument. There it was Tier 1W -- a recurring GC pause meant no wall figure below a few hundred milliseconds meant anything, and three alternating rounds could not cancel it. Here it is the write screen, the mechanism is whatever jitter a 40ms request carries rather than a GC pause specifically, and the boundary lands in the same place. Two instruments, independently, put the usable floor at a few hundred milliseconds.

**The practical consequence is a reporting rule rather than a code change.** A per-measurement wall figure should not be published without its absolute magnitude alongside it, and a range across measurements should not be published at all on this axis: quoting "wall clock ranged from −59.8% to +42.5%" would advertise a 42% regression that is noise, on a comparison whose aggregate is −23.9%. The aggregate is unaffected -- it sums absolute milliseconds across 260 measurements, where the small ones contribute little and their jitter cancels.

**What the banding also shows is not noise.** The median improvement rises monotonically with the cost of the operation: −4.7%, −15.7%, −38.1% across the three bands. The expensive half of the write surface is where this branch does its work, which is the half a user waits on. That belongs in a conversation about where the wins are rather than in the report, where a band table needs the reader to already know the test environment.

### The harness measured the shell of every UI list view and never the rows

**44** · `A` · `perf/workload.yml - perf/scripts/tier1_queries.py - perf/scripts/workload.py`

A Nautobot list view builds its table over queryset.none() unless the request carries HX-Request (nautobot/core/views/renderers.py:96). The document therefore renders a table with no rows in it, and the browser fetches the real one on a follow-up request fired by hx-trigger="load" in generic/object_list.html:152. The harness never sent that header -- zero occurrences across workload.yml, workload.py, tier1_queries.py and bench_endpoints.py -- so all 20 ui.*.list scenarios measured a page that rendered nothing. ui.interface.list asks for per_page=100 and rendered none of the 100.

| Instrument | Result |
|---|---|
| **wall clock** | not applicable: this measures the instrument rather than a change to Nautobot |
| anatomy | measured in Firefox against the perf instance, per-request TTFB from the Performance API. A list view is two requests to the same URL: the document renders the shell and an htmx XHR renders the rows. dcim/devices/?per_page=50 fired 2 requests, 50 rows rendered by the second; dcim/interfaces/?per_page=100 fired 2, 100 rows by the second. **The request count and which request renders the rows are the result here.** The timings taken alongside them (document 471ms then XHR 692ms; 453ms then 1195ms) put roughly 60 to 70% of each page's server time in the unmeasured request, but they are indicative only and no figure should be quoted from them -- see controls. |
| detail pages are not split | device detail is one request with no htmx follow-up, so the 108 -> 52 query reduction recorded for it covers the whole page and needs no correction. Location detail is the document plus one small /api/dcim/locations/<id>/stats/ fetch, a different shape from the list-view split and also unmeasured. Request counts, not timings. |
| what it invalidates | finding 07's control ("ui.device.list and ui.interface.list both within noise across three rounds") and finding 12's ("ui.interface.list flat") were taken on empty tables and say nothing about list views. Finding 11 converted 67 TemplateColumn call sites across the ipam, circuits and dcim tables and carries no wall-clock number at all; every bit of that work happens in the unmeasured request. |
| not affected | query counts and wall clock for API endpoints, for the 7 UI detail pages, and for the write path. The 2,235 -> 695 read aggregate is unaffected as an arithmetic claim -- those scenarios measured what they measured -- but it does not cover list-view row rendering at all, which was never in the denominator. |

**Controls.** The load-independent facts are the request count per page, the URLs, and the initiator types; those are what this finding claims. Every timing was taken while the measurement host ran a full test suite at loadavg 1.5 to 2.0, against arm_control.sh's gate of 0.7 -- the harness would have refused to measure under these conditions, and finding 35 is why the gate exists. The document-versus-XHR ratio is more reliable than the absolutes since both requests met the same contention, but a longer request has more opportunity to be descheduled than a shorter one, so even the ratio is indicative. Real timings come from the on-host driver under the quiesce gate with the arms alternated.

**Tests.** n/a; harness only, no product code

Found because Kevin's dev-tools spot checks said list views were faster and the harness said their query counts had not moved. I read the query counts as "not faster" and said so, which is precisely the error this branch exists to warn about -- and worse than the usual form of it, because the instrument was not merely blind to the improvement, it was pointed at a different request.

**The reachable conclusion was available in the code and I did not go looking for it.** The renderers.py:96 short-circuit is one line, `queryset if htmx_request else queryset.none()`, and it explains why every list view sat at 9 to 13 queries whatever the page size and whatever the dataset. That constancy was in the baseline the whole time and got read as "list views are already efficient" rather than as "these numbers do not depend on the data, so they cannot be measuring the data".

**The fix keeps both halves.** 18 ui.*.list.rows twins now carry HX-Request: true, generated from the existing list scenarios so none was missed, and the workload goes from 39 to 57 scenarios. The shell scenarios stay, because a page load pays for both requests and dropping either one just moves the blind spot. render_cumulative now sums the intersection of the two arms rather than each file whole -- otherwise the next stock run would have compared 57 scenarios against a 38-scenario baseline and rendered the difference as a regression.

**A browser was the only instrument in play that could have caught this.** Tier 1 and the read screen both drive the Django test client at one URL per scenario; Tier 2 drives HTTP but from the same URL list. None of them knows a page is two requests. The general rule is that an instrument built from a list of URLs cannot discover a request it was not told about, so something that observes a real client belongs in the harness -- not for timing, where it has no reps, no arm alternation and no quiesce gate, but for establishing what a page actually asks for.

### Query capture silently returns zero once the connection's query log saturates

**45** · `A` · `perf/scripts/tier1_queries.py - perf/scripts/compare.py`

The stock arm of a 57-scenario Tier 1 run reported `query_count: 0` for its last six scenarios. Not failures: status 200, no error, a full response body, and a content hash byte-identical to the branch arm's for the same URL. The branch arm measured those same six at the same positions correctly. `CaptureQueriesContext` records `len(connection.queries_log)` on entry and exit and slices between them, and that log is a `deque(maxlen=9000)` shared for the life of the process. Once it saturates the length stops growing, so every subsequent capture computes `final - initial = 0`.

| Instrument | Result |
|---|---|
| **wall clock** | not applicable: this measures the instrument rather than a change to Nautobot |
| signature | six zero-query records, contiguous, at indices 51 to 56 of 57. Contiguity is what identifies the mechanism: a one-time connection recycle at CONN_MAX_AGE=300 would also reset the log and empty one slice, but the next scenario would re-baseline against the shorter log and measure correctly. A saturated deque is a permanent condition, so everything after it reads zero. |
| bias direction | the arm that executes more queries saturates first, and that is always the baseline arm -- stock reached scenario 51 having executed roughly 7,647 queries against the branch arm's 5,472. So the failure under-reports the *before* side of every comparison and biases the result in the branch's favour. |
| why it was silent | status 200, a plausible 165KB body, and **`query_count_stable: True`** -- all three reps agreed on zero, so every stability check the harness owns passed it. It was caught only because the two arms disagreed in an impossible direction: six API endpoints appearing cheaper on stock than on the branch that optimized them. |
| committed baselines are clean | all twelve committed baselines scanned for the signature -- status 200, non-empty body, zero queries -- and **zero records match**. The 38-scenario runs never accumulated enough to saturate; it took today's 57-scenario workload on the arm that executes the most queries. No published number is affected. |
| threshold not pinned | stock executed ~7,647 queries before the first zero counting three requests per scenario, or 10,196 counting four. The limit is 9,000, so the exact crossing was not proven and the fix deliberately does not depend on it. |
| graduated not binary | the trailing zeros are the visible tip. Re-running the same arm with the log cleared per capture shows the failure degrades progressively as the deque fills: api.interface.depth1 reported **495 queries against a true 1,067** -- understated by 572, 46% of its value -- while still looking like an ordinary measurement, and only the six scenarios after it went fully to zero. Stock arm total 2,549 against a corrected 3,166, so **19.5% of the queries executed were never counted**. A run can therefore be materially wrong before it produces any record obviously impossible enough to notice. |

**Controls.** the six were re-measured on the branch arm at the same positions and returned 6 to 11 queries each, which is what isolates position-and-history from endpoint. Dataset drift was ruled out against `expected-counts.txt` (2902 / 8925 / 3278 / 2937, exact match) and the arm swap was ruled out by content hash -- stock `0e1d03a4e` against branch `90dbd96ff`.

**Tests.** n/a; harness only, no product code

Three fixes, and only the first is the bug.

**Clear the log before each capture.** A measurement should not depend on how much ran before it. This makes run length irrelevant and is correct whichever trigger applied, which matters because the arithmetic above does not pin the threshold.

**Raise the per-connection limit from 9,000 to 18,000.** Defence in depth for the residual case the first fix does not cover: a single request exceeding the limit on its own. `bulk.delete.x100` already issues 2,222 queries, so 9,000 was about four times the largest operation measured here -- thinner headroom than a larger dataset or a new bulk operation deserves. Set on the connection inside the harness rather than in product settings, so the instrument's configuration stays in the instrument.

**Refuse an implausible record.** No authenticated Django page serves a 200 with a body in zero queries; there is a session lookup and a user fetch before the view runs. That combination is an instrument failure rather than a fast page, and it now fails loudly in `tier1_queries.py` and is refused by `compare.py`'s availability check. This is the fix that generalises: the first two close one hole each, and this one closes the class. The next variant of a silent-zero will announce itself instead of waiting for someone to notice that two arms disagree in an impossible direction.

**The partial undercounts are the dangerous half.** A zero is at least absurd on its face, which is how this was caught. An endpoint reporting 495 of its 1,067 queries is not absurd at all -- it is a plausible number, stable across reps, on a status-200 response, and nothing in the harness or in a reader's intuition would flag it. The plausibility guard catches the zeros; nothing catches a 46% undercount except clearing the log, which is why that fix is the one that matters rather than the guard.


**The instrument passed its own stability check while reporting nothing.** That is the part worth remembering. `query_count_stable` compares reps against each other, so three reps agreeing on a wrong answer reads as confidence. A stability check cannot detect a systematic failure -- only a plausibility check can, and the harness had none until now.

### The branch's list-view win was entirely in the request the harness never issued

**46** · `A` · `perf/scripts/tier2_latency.py - perf/workload.yml`

First stock-versus-branch wall-clock A/B over the 57-scenario workload, on one host, arms alternated, both trees proved different by content hash, cassowary at n=30 c=1 per endpoint. It answers a question the harness could not previously ask: finding 44 established that every ui.*.list scenario measured a page rendering no rows, so the branch's effect on list views had never been measured at all. It is substantial, and it is invisible to every deterministic signal the harness owns.

| Instrument | Result |
|---|---|
| **wall clock** | −31.9% summed median server time across 57 endpoints |
| the split | the document half of a list view is unchanged -- median **−1.3%** across 18 scenarios, range −5.7% to +2.6%, which is noise. The row-rendering half improves on **18 of 18**, median **−18.6%**, best −33.1% (dcim.cable), worst −7.6%. Largest absolute movers: ui.interface.list.rows 1272 -> 902ms, ui.prefix.list.rows 1556 -> 1226ms, ui.device.list.rows 1041 -> 786ms. |
| no queries moved | **Tier 1 measured all 18 row-rendering scenarios as identical across the two arms** -- same query count, same duplicate count, to the query. So this is an 18.6% median wall-clock improvement with zero SQL difference. Every deterministic signal this harness owns reports the branch as having done nothing to list views. |
| chrome baseline | ui.chrome.404 -- full chrome, static content, 3 queries -- is **82ms**. Zero-row list documents run 151 to 256ms, so per-model list machinery on top of chrome is 68 to 174ms, a 2.5x spread. The cheap end is extras models (objectchange, role, status at 68 to 70ms); the dear end is dcim and ipam (location 174, prefix 154). **The per-model term dominates, not the chrome.** |
| aggregate | summed per-endpoint median server time across all 57 endpoints answering on both arms: **33,606ms -> 22,892ms, −31.9%**. 25 endpoints moved more than 10%. Largest: api.interface.depth1 6420 -> 1514ms (−76.4%), api.interface.list 1626 -> 779ms (−52.1%), api.device.list.depth1 1853 -> 1153ms (−37.8%). |
| what the saving scales with | across the 18 row-rendering scenarios, the milliseconds removed correlate with the page's own cost at **pearson +0.97, spearman +0.95** -- the branch removes a roughly constant fraction of whatever rendering a page does. By cost band the median improvement is −9.4% below 300ms, −19.1% from 300 to 900ms, and −24.5% above 900ms. That is the signature of a per-row overhead being removed rather than a fixed cost, and it is orthogonal to database access, which is why query count is identical on every one. |

**Controls.** both arms through arm_control.sh: path-scoped checkout, container restart because uwsgi has no autoreloader, then a wait for loadavg below 0.7. Tree hashes recorded per arm and proved different -- branch 90dbd96ff against stock 0e1d03a4e. Every endpoint probed for a 200 before timing, none skipped, zero failures reported by cassowary on either arm. ui.chrome.404 carries expected_status 404 and is exempted from the 200 gate rather than excluded from the run.

**Tests.** n/a; measurement only, no product code

This is the read-path counterpart to finding 39, and it lands the same way. There, a −33.7% write win was spread across 40 models with no single finding accountable, and query count would have rated the work at −13%. Here, an 18.6% median win on the request users spend most of their time waiting for is invisible to query count entirely -- not undervalued, invisible, because the counts are identical to the query.

**It also settles a contradiction that was open for most of a day.** Two readings of the chrome cost disagreed: a browser session under load put the 404 at 274ms and a zero-row list document at 357ms, making chrome the dominant term; the committed quiet-box numbers put ui.search at 81ms with identical chrome, making the per-model term dominant. On a gated box the 404 is **82ms**, within a millisecond of ui.search, so the committed reading was right and the browser figures were inflated 3.3x by a test suite competing for the same cpuset. The lesson is not that the browser was the wrong instrument -- it found finding 44, which nothing else could have -- but that a measurement taken outside the quiesce gate cannot arbitrate between two that were taken inside it.

**What this does not say.** The aggregate sums per-endpoint medians with every endpoint weighted equally, which is the same method the write screen uses, so the two rows in the report are comparable to each other. It is not traffic-weighted, and nothing in this harness knows the traffic mix. A −31.9% summed figure is not a claim that any particular user gets 31.9% of their time back.


**A mechanism I proposed and the data did not support.** I attributed this to findings 07 and 11 -- the caching TemplateColumn -- on the strength of the biggest movers being dcim and ipam tables, which is what finding 11 converted. Tested properly by counting TemplateColumn-derived columns per table class and correlating against improvement, it comes out at **pearson +0.09, spearman +0.05**: no relationship. The counter is also not trustworthy -- it reports zero columns for CableTable and InterfaceTable, which plainly have them, so those tables build columns somewhere a static walk cannot see. Either way the attribution is unproven, and what the data does support is the magnitude relationship above. This is the sixth hypothesis on this branch formed by reading code and killed by measuring, and the first one where the eyeballed pattern was a genuine correlation with the wrong variable: the big movers really are dcim and ipam tables, and they are also the expensive ones.

### The write screen's floor is the real cost for most models, and unreachable for the expensive ones

**47** · `A` · `perf/scripts/screen_writes.py - perf/scripts/payloads.py`

`screen_writes.py` populates required fields only, so every figure it reports is the floor cost of a create. The caveat said any model whose expensive work hangs off an *optional* relation is understated by an unknown factor. `--include-optional` re-runs the screen with every writable optional field populated, which bounds that factor -- for the models where a populated payload can be built at all.

| Instrument | Result |
|---|---|
| **wall clock** | not applicable: this measures the instrument rather than a change to Nautobot |
| the factor | across 73 models measured both ways at create.x10, the median understatement is **1.00x** -- for the typical model the floor is the cost. Mean 1.31x, max **4.96x**. 33 of 73 are understated by more than 1.1x, 10 by more than 1.5x, 6 by more than 2x, 4 by more than 3x. Largest: ipam.prefixlocationassignment 69 -> 342 queries (4.96x), ipam.vlanlocationassignment 69 -> 287 (4.16x), dcim.virtualchassis 108 -> 431 (3.99x), ipam.vlan 138 -> 423 (3.07x). |
| where it cannot reach | **27 of the 100 models the floor run measured could not be measured with populated payloads at all**, because a payload carrying every optional field could not be built or was rejected. They include the expensive ones the caveat was written about: dcim.cable, dcim.interface, dcim.powerfeed, dcim.interfacevdcassignment, dcim.poweroutlet, dcim.virtualdevicecontext. So this bounds the factor everywhere except where it matters most. |
| cable is still quantified elsewhere | finding 40 measured a genuinely connected cable at ~220 queries against the screen's floor figure of 44.9, which is 4.9x -- by attribution rather than by payload, and consistent with the top of the range measured here. |

**Controls.** both runs on the same branch tree and the same dataset, create.x10 only so the comparison is like for like, and rollback isolation in both so neither mutates state. Models present in only one run are reported separately rather than dropped, since their absence is the result.

**Tests.** n/a; harness only, no product code

The caveat this was meant to retire cannot be retired, and the shape of what is left is more useful than a clean answer would have been. "Understated by an unknown factor" becomes "not understated at all for the median model, up to 5x for four of 73, and still unknown for the 27 the method cannot reach" -- and those 27 are exactly the models anyone would care about, because a model with expensive optional relations is both the one most understated and the one hardest to build a payload for. The two properties have the same cause.

**What it does settle** is that the ranking is not systematically wrong. If the floor understated everything by a large factor the whole ordering would be suspect; a median of 1.00x says the ranking is sound for most of the surface, with four known outliers and a named list of models where it should not be trusted.

### The nav menu's cost is not a superuser artifact, and Django permissions are inert here

**52** · `A` · `perf/scripts/probe_nav_menu_perms.py — nautobot/core/context_processors.py _build_nav_menu`

Findings 50 and 51 were all measured as `perfbot`, a superuser. That mattered because `_build_nav_menu` runs ~176 permission checks and Django's PermissionsMixin.has_perm returns True from `is_active and is_superuser` *before consulting any backend* — so every check short-circuited, and "construction is 1.1ms, rendering is 44x more" could have held only for a user who pays nothing for permissions. Measured against a non-superuser seeing the same menu, it holds: construction doubles, rendering does not move.

| Instrument | Result |
|---|---|
| **wall clock** | not measured: an attribution, no change to measure |
| three users | superuser: 200, wall 275.2ms, build 1.17ms, nav_render 20.6ms, 176 wrapper calls, **0** backend has_perm calls, menu 15/42/123. Broad non-superuser (ObjectPermission granting view on all 191 content types): 200, wall 256.8ms, build **2.45ms**, nav_render **20.2ms**, 176 wrapper calls, **186** backend calls, menu 15/42/122. Limited non-superuser (6 permissions): 403, wall 20.9ms, build 1.39ms, nav_render 1.2ms, 14 wrapper calls, 118 backend calls, menu 1/1/2. |
| verdict | construction 1.17 -> 2.45ms, roughly doubling; rendering 20.6 -> 20.2ms, unmoved. The ratio narrows from 44x to about 8x and rendering still dominates. Nothing built on "rendering is the cost, not construction" needs revising, and the −43% of finding 51 is unaffected because URL reversals do not depend on who is asking. |
| why it stays cheap | `objperm_loads=1` in both non-superuser arms. ObjectPermissionBackend loads the ObjectPermission set once per user object and caches it on `_object_perm_cache`, so 186 backend checks are dict lookups against already-materialised data. `build_q=0`: the builder issues no queries during the request, because the permission load happens outside the timed section. |
| superuser shortcut | the superuser makes **zero** backend calls for 176 checks. The 186-vs-0 gap is the entire cost of being a normal user, and it is 1.3ms. |
| pruning | the limited user's menu prunes to 1 tab / 1 group / 2 items, and the permission tree short-circuits after 14 wrapper calls rather than 176 — a tab whose permissions fail never has its groups or items checked. So a *less* privileged user is cheaper, not dearer, and the expensive case is the broad non-superuser rather than the restricted one. |

**Wall clock.** Five reps per arm with one warm-up discarded, on a quiesced host through measure.sh. Single round, which is adequate here because the claim rests on the build and render splits (1.17 vs 2.45ms, 20.6 vs 20.2ms) rather than on total wall clock, and those are measured directly rather than by subtraction.

**Controls.** the broad non-superuser sees 15/42/122 against the superuser's 15/42/123, so the two render essentially the same menu and the render comparison is like-for-like. The limited arm is deliberately kept despite its 403: it shows that a smaller menu is a smaller cost, which is what rules out "non-superusers are cheaper" being mistaken for a saving.

**Tests.** not applicable — no product code changed.

**Two probe defects worth recording, because both produced confident wrong answers.**

First, granting `django.contrib.auth.Permission` rows does nothing for Nautobot's permission checks. `ObjectPermissionBackend.get_object_permissions()` reads the ObjectPermission model and nothing else, so a user granted 192 Django view permissions still saw a 1-tab menu and got a 403 — identical to a user with six. The broad arm only became broad once granted through ObjectPermission. Anyone measuring permission cost in this codebase will hit this.

Second, patching `type(user).has_perm` fails inside a request: the user is a SimpleLazyObject proxy with no `has_perm` on the class. `ObjectPermissionBackend.has_perm` is the only reliable funnel, and it has the advantage of counting checks however the user object is wrapped.

The measurement was prompted by finding 51's write-up flagging its own exposure, and it closes that gap rather than opening a new one. What it does not cover: a user whose ObjectPermissions carry *constraints*, which is the case where a check has to evaluate a queryset filter rather than a set membership. That is untested and is the remaining unknown in this area.


## Per-finding evidence

For each change in the report that carries working: how its wall clock was taken, what was held
flat as a control, which tests ran, and what the finding concluded beyond its own verdict.
Numbered to match the report.

### 01 · Make the natural-key fallback lazy, so it is not computed and discarded per row

**Wall clock.** api.interface.list 357.9 -> 268.2ms (-25.1%), median of 15 reps; api.interface.depth1 1479 -> 1254ms (-15.2%); ui.interface.list 64.2 -> 56.0ms (-12.9%)

**Controls.** api.prefix.list flat, as predicted -- its natural keys touch no config-dependent model

**Tests.** core test_api 61 OK

Removes no queries at all, which is why a query-count gate alone would have missed it. The first change on this branch that a query-count gate would have scored at zero.

### 02 · Prefetch nested natural keys, and fetch each cable path once

**Wall clock.** api.interface.depth1 1479 -> 1092ms (-26%); api.device.list.depth1 301 -> 238ms (-21%); api.interface.list 268 -> 250ms

**Tests.** dcim test_api 1673 OK

The cable-path memo is instance-scoped, so it is discarded with the object.

### 03 · Render each detail-page component once per request, not three times

**Wall clock.** not measured. The commit reports query counts and a byte-for-byte HTML diff only; no wall-clock A/B was run

**Controls.** Rendered HTML diffed byte-for-byte before and after; only the CSRF token differed

**Tests.** core test_ui 59 OK, core test_views 95 OK

Model-agnostic, which is why Device, Rack, Prefix and Location all showed the same shape while list views stayed at 9-10 queries. An App panel whose get_extra_context() must return a fresh object per call would now see one shared object.

### 04 · Memoize natural-key lookups for the duration of one serialization

**Wall clock.** ~1010 -> ~877ms on the 100-interface create loop, directionally consistent across repeats but within run-to-run variance. Quoted as supporting evidence, not as the result -- the deterministic config-read count is the result

**Tests.** core test_models 35 OK, core test_api 61 OK

Scoped to a single serialization deliberately: Location's value depends on live tree depth and Device's on Constance config, so a permanent class-level cache would go stale. A permanent variant measured +21.7% against +19.8% -- indistinguishable, and not worth the risk.

### 05 · Extend the natural-key lookup cache across a whole read request

**Wall clock.** api.interface.list 305 -> 158ms and api.interface.depth1 1114 -> 726ms, both halves of the same run under equal load. Supporting evidence only: the container was under ~260% CPU from concurrent profiling, so the config-read count is the result

**Controls.** api.prefix.list 0 -> 0 and api.cable.list 0 -> 0 -- a previously considered global cache had regressed prefix ~18% by adding Redis reads to models that needed none

**Tests.** core test_api 61 OK

Wall clock was unusable during this experiment -- the container was under ~260% CPU from concurrent profiling -- so the deterministic config-read count is the primary evidence.

### 06 · Stop paying Redis round-trips for tree display and config lookups

**Wall clock.** api.location.list 75.2 -> 54.9ms (-27%), interleaved over 7 rounds on a quiet machine. The fix also removed variance, since the Redis round-trips were the jitter source

**Controls.** api.prefix.list 5 -> 5 unchanged

**Tests.** core test_tree_queries 11 OK, test_config 30 OK, test_utils 73 OK

B2 because max_depth uses the cross-request Redis cache. The invalidate_max_depth_cache receiver was extended to evict the request-local entry too, and that line is necessary -- without it a tree change mid-request leaves a stale max_depth. Note the pre-existing hazard this shares: all six invalidate_*_cache receivers miss QuerySet.update() and .bulk_update(), which compile to one SQL statement and emit no signal.

### 07 · Compile each TemplateColumn once instead of once per table cell

**Wall clock.** ui.device.interfaces 147.5 -> 119.3ms (-18.5%), median of 12 reps over three alternating rounds on an idle machine

**Controls.** ui.device.list and ui.interface.list both within noise across three rounds

**Tests.** core test_tables 17 OK, dcim test_tables 34 OK

Compiled templates are immutable and hold no per-render state, so sharing one across requests and threads is safe -- new state, but nothing to invalidate. Flagged third-party-coupled: it reimplements django_tables2 3.0.1's render(), depending on the get_context_data() hook and the parent_context request assignment. A future version changing those would diverge silently. Revisit on any django_tables2 upgrade. An earlier two-round comparison suggested a ~2ms regression on two list views; a third round showed it was drift.

### 08 · Cut the residual duplicate queries on object detail pages

**Wall clock.** own A/B inconclusive and no number claimed from it -- ui.rack.detail swung 84 to 110ms across two runs of the same config, larger than the effect. An independent interleaved measurement at 125 samples per config put it at roughly -8.7ms on rack and -8.5ms on device (~-9%), consistent with prepare_cloned_fields measured directly at 6.5ms

**Tests.** core test_views 95 OK, test_ui 59 OK, test_tree_queries 11 OK

Corrects two earlier attributions: Breadcrumbs.as_pair was blamed for calling .ancestors() four times and calls it once; prepare_cloned_fields was suspected of running twice and runs once. My own A/B here was inconclusive -- ui.rack.detail swung 84ms to 110ms across two runs of the same config -- so no wall-clock number is claimed from it.

### 09 · Build nested serializers once, and share one instance per cabled row

**Wall clock.** api.interface.depth1 753.4 -> 404.8ms (-45.8%), median of 12 reps over two alternating rounds each way

**Controls.** api.device.list.depth1 flat at 209.8/212.7 vs 212.4/214.7

The largest single win on the branch, and the earlier attribution missed it entirely -- it named ~150 GFK fetches, ~100 device bays and ~77 cable paths, which were the small items, and missed the 70% item. Found by grouping queries by originating call site and checking instance provenance.

### 10 · Walk tree ancestor chains in blocks instead of one query per level

**Wall clock.** not measured. Worth 8 queries across three detail pages, below what wall clock could resolve on that machine

**Tests.** core test_tree_queries 11 OK, test_views 95 OK, test_dynamicgroups 92 OK

Took only the Tier A half of the proposed change. The proposal also primed the returned queryset's _result_cache, which is correct for count/exists/first/iteration and discarded by anything that clones -- a subtler contract than one query justified.

### 11 · Use the caching TemplateColumn everywhere, not just device tables

**Wall clock.** not measured for this commit -- the machine was under concurrent profiling load. The mechanism is identical to finding 07, measured there at -18.5% on ui.device.interfaces, and the saving scales with rows x TemplateColumns per table

| | |
|---|---|
| note | same mechanism and measured effect as 40c3af5fe; the saving scales with rows x TemplateColumns per table |

**Tests.** core test_tables 17 OK, dcim test_tables 34 OK, ipam test_tables 7 OK, extras test_tables 7 OK, ipam test_views 596 OK

No wall-clock number quoted: the machine was under concurrent profiling load, and timings here need an idle box and three alternating rounds. Deterministic signals unchanged as expected, since this removes Python work rather than queries.

### 12 · Build the nav menu once per request instead of once per component

**Wall clock.** ui.device.detail -24% and ui.rack.detail -25%, alternating rounds on an idle machine. An independent interleaved measurement at n=42 per cell put it at -14 to -17ms across six detail pages (-19.5% to -28.2%). Note the retracted figure: cProfile reported nav_menu at 40ms/request; true wall clock is ~16ms of an ~88ms request

**Controls.** ui.device.list 48.3/49.1 vs 48.9/49.5 and ui.interface.list flat

**Tests.** core test_navigations 6 OK, test_views 95 OK, test_ui 59 OK

Memoized on the request object, so the cache is scoped to one request and therefore one user -- no user-keyed map, no module-level dict, nothing outliving the response. Verified with two users interleaved four times each in one process: the superuser sees 131 distinct nav links, a permissionless user sees 10, both stable, no leakage. Corrects an earlier figure: this was reported at 40ms/request from cProfile, which inflates call-heavy code; true wall clock is ~16ms of an ~88ms request.

### 13 · Reuse one API serializer per model when change-logging a transaction

**Wall clock.** bulk.create.loop and .deferred both -28%, three alternating rounds each way. serialize_object_v2 in isolation -41% for 100 Interfaces over five paired rounds (553 -> 326ms)

| | |
|---|---|
| isolated | serialize_object_v2 -41% for 100 Interfaces over five paired rounds |

**Controls.** the cfg column confirms the two arms really differ, 2 against 200

**Tests.** extras test_changelog 35 OK, extras test_models 295 OK, core test_models 35 OK, core test_api 61 OK

A first attempt at this A/B used git stash to toggle, which silently did nothing because the change was already committed -- all six runs measured the same code, and the cfg counter is what caught it. Also: this commit credited itself with bulk.update 1542 -> 1472 and bulk.delete 2089 -> 2019, which were already present in the preceding run; it compared against a stale before-file. The config-read result is the real effect. Serialized output compared object-by-object across seven models, 172 objects, zero mismatches.

### 14 · Resolve natural keys from a whole-table map for small reference models

**Wall clock.** api.interface.depth1 -15%, three alternating rounds (328/352/347ms against 398/420ms). Controls flat: api.device.list 176/178/177 vs 174/180; api.prefix.list 56/61/56 vs 55/59

**Controls.** api.device.list and api.prefix.list both flat

**Tests.** core test_models 35 OK, test_csv 28 OK, test_api 61 OK, dcim test_api 1673 OK

Opt-in per model via natural_key_map_enabled, guarded at 5000 rows, built with values_list so no model instantiation, living in the existing request_cache() scope. Falls back to traversal on any miss. A cross-request Redis-backed variant was built and measured alongside and rejected -- see seq 17. Byte-identical responses on six endpoints; a sweep over 1310 objects produced identical natural_key, composite_key and natural_slug, and 60 objects round-tripped back to the same PK.

### 15 · Pre-warm natural-key FK chains with one select_related

**Wall clock.** -666 queries and api.interface.depth1 1092 -> 1767ms, +62%. The wall-clock measurement IS the result here: it is what rejected the change

The clearest demonstration on the branch that query count and wall clock can point in opposite directions. The variant that looked three times better by query count was the one that regressed past the original baseline in wall time. The effect grows with Location tree depth.

### 16 · Stop double-serializing every change record by writing an empty object_data

**Wall clock.** not measured. Deterministic query counts were the point; the change was never a candidate to ship

**Controls.** read path unaffected: Tier 1 total unchanged, all endpoints 200

**Tests.** breaks 13 tests in nautobot.extras.tests.test_changelog -- six failures and seven errors across ChangeLogAPITest, ChangeLogViewTest and ObjectChangeModelTest, including test_legacy_object_data

Kept in history with its measurements rather than deleted, because the number is the deliverable.

**Superseded, and by a route that landed.** Finding 22 takes the same win by making the column nullable rather than writing {} into it, and is accepted. What this version buys over that one is nothing: an empty dict makes the field present and lying, where null is the correct JSON for "not recorded". It stays on the record as the route that was tried first, not as a live option.

The ~12% figure in the reason above is stale in the direction of being smaller. Finding 22 re-measured the same prize on the current tree at −7.7% on the ORM path and −2.1% on the REST path, because finding 31 had since removed one of the two queries per record.

### 17 · Cross-request Redis-backed natural-key map with signal invalidation

**Wall clock.** +0 to 2 percentage points over the request-scoped variant, inside run-to-run variance

The within-request memo already captures essentially all of the benefit: the map is built once and read ~820 times per request. This hazard already exists in-tree for all six invalidate_*_cache receivers; worth addressing separately, either with a TTL backstop or by overriding update() and bulk_update() on RestrictedQuerySet.

### 18 · Enable LOCATION_NAME_AS_NATURAL_KEY

**Wall clock.** -12% on api.interface.depth1 standalone; ~2 points on top of the whole-table map

Stacking it on the whole-table map adds only ~2 points, because the map already collapses most of what it avoids.

### 19 · Process-level per-user nav menu cache

**Wall clock.** ~0.6ms, now that nav_menu runs once per request

Rejected on blast radius, not on measurement. Included because the reasoning generalises: cached permission-derived output is a different class of risk from cached data.

### 20 · Drop the request argument in render_component_template

**Wall clock.** ~1ms residual after the per-request memo

### 21 · Make tab badges reuse the panel paginator COUNT

**Wall clock.** no measurable effect

### 22 · Stop writing ObjectChange.object_data, with the column made nullable

**Wall clock.** not measured -- proposed, not yet run

| | |
|---|---|
| prize remeasured | the v1 serializer costs 454.4ms of a 3775.4ms bulk.create.loop (12.0%), 575.8ms of 4525.7ms deferred (12.7%), 4.5ms per call against v2's 14.4ms -- 3.2x, where finding 16 measured 12x. The ratio narrowed because finding 13 made v2 cheaper, so v1 is a larger share of what remains than when it was first measured |

**Controls.** read path unaffected: Tier 1 total 695, all 38 endpoints 200/302, gate PASS

**Tests.** extras test_models 295 OK, extras test_api 1475 OK, and **13 failures in extras test_changelog** -- 7 errors from assertions on oc.object_data[...] against None, 6 failures across test_legacy_object_data and test_get_snapshots on mixed-format snapshot pairs

**Taken on storage, not on speed, and the distinction matters.** The performance case for this change collapsed between being recorded and being implemented: −2.1% of queries and a wall clock indistinguishable from zero on the REST path, against an unchanged Tier C cost. Measured on write-path performance alone -- the axis this branch is chartered on -- the answer was no, and that analysis is kept above rather than deleted.

It is being taken because that was the wrong question. object_data is 11.8% of the changelog table and the table grows without bound where retention is long. Removing a snapshot that is written for every record and read by nothing is a storage and redundancy argument, and it stands whether or not it is fast. Pure-ORM bulk writes -- a Job, a data migration, nbshell -- also keep −7.7% queries and roughly −9% wall on a 100-object create loop, which is a real if narrow bonus.

**What the tests now cover, since the payload change is the whole cost.** The null case: new records carry object_data_v2 and a null object_data, and diff against each other through v2 (test_object_data_not_written_for_new_records). The non-null case: records whose only snapshot is v1 still render and still diff v1-against-v1. And the upgrade boundary, which happens once per object and is the only genuinely new behaviour -- a current record whose predecessor is legacy reports no prechange rather than diffing two incomparable shapes. The legacy fixtures are now built explicitly with serialize_object rather than obtained by nulling half of a current record, because that trick stops working the moment object_data is no longer written and would have produced records in a state Nautobot has never created.

**Two production readers the original cost analysis missed, both checked and both safe.** extras/webhooks.py:210 already reads object_data_v2 first and falls back to object_data, so webhook payloads are unchanged. extras/signals.py:152 caches v1 into pre_object_data, but get_snapshots consumes it as `pre_object_data_v2 or pre_object_data` and v2 is still populated.

**The general lesson, separate from the verdict.** A recorded prize is a measurement of a tree that no longer exists. This one was taken before finding 31 landed, and finding 31 removed half of it -- so one finding removed half of another's prize and nothing in the ledger flagged it. Anything parked for later should be re-measured before it is implemented, not just before it is proposed. The commit that first measured this said as much ("a recorded prize can go stale") and re-measured finding 16's number before proceeding; the same discipline applied one step later would have caught this without a re-implementation.

What the nullable route buys over 43db1b018, which wrote {} into the NOT NULL column, is not the 2 queries, since both save those. It is that {} makes the field present and wrong: an empty dict does not mean "no data", so updating the 13 tests to match it would have encoded a falsehood. null is the correct JSON for "not recorded", so those same test updates become legitimate. A Tier C change whose tests can be honestly updated is a different proposition from one whose tests cannot.

Two things established on the way, both outside this finding's scope. Every object_data site turned out to be paired with an object_data_v2 site -- signals.py at 152/153, 391/392 and 488/489 -- so the write path needed no other changes. And get_snapshots carried a latent AttributeError: its mixed-format branch dereferenced prior_change.object_data without checking prior_change is not None, reachable in principle for an UPDATE whose prior changelog was pruned on a record lacking v2. Unreachable today because every record has v1.

### 23 · Backfill object_data_v2 for historical ObjectChange rows

**Wall clock.** not applicable -- priced by migration runtime (8.75ms per row), not by request latency

| | |
|---|---|
| cost | 8.75ms per row measured. ~5 minutes at this dataset's 36,552 rows; ~2.4 hours at one million; unbounded where CHANGELOG_RETENTION is 0 or its cleanup Job was never scheduled |

Recorded so the option is priced rather than rediscovered. seq 22 gets the same win without it.

### 24 · Index maintenance on extras_objectchange, priced at a ~0 ceiling

**Wall clock.** Three rounds each way, snapshot restore before every arm, arm order reversed between rounds: with 3434/3307/3358ms, without 3296/3414/3300ms.

The medians differ by 59ms, and in all three rounds the arm that ran SECOND was faster by 60-130ms regardless of which arm it was. The difference and the artifact are the same size, so the real cost of index maintenance here is indistinguishable from zero.

Consistent with the rest of the branch: db_ms is only ~780ms of a 3358ms operation, and index maintenance is a fraction of that. The cost on this path is Python-side.

| | |
|---|---|
| ceiling | dropping all 17 non-unique indexes: wall 3358 -> 3300ms median, -59ms (-1.7%), db_ms 776 -> 752ms, on bulk.create.interfaces.x100.deferred |
| observed | 18 non-primary indexes on extras_objectchange, 17 of them non-unique |

Scoped to non-unique indexes deliberately, and the reason is worth keeping. The first attempt tried to drop all 18 and failed: extras_objectchange_time_request_id_changed__f5f870cc_uniq backs a UNIQUE constraint, and PostgreSQL refuses to drop such an index directly. That is the right boundary rather than an obstacle -- a unique index is a constraint, not an optimization, so dropping one removes a guarantee and is a behaviour change rather than index tidying.

It also corrects the count this record used to carry. It said eleven indexes; that was the never-scanned subset from a pg_stat reading. The table has 18 non-primary indexes, 17 non-unique.

Adding indexes remains a dead end here and that stays measured rather than assumed: 325,152 queries totalling 4.6 seconds, mean 0.014ms, and query count fell 57% while total DB time went 601ms to 602ms. Both directions on this table are now priced.

### 25 · Join parent_bay when serializing nested Devices

**Wall clock.** api.interface.depth1 1808.3 -> 1703.9ms (-5.8%), median of three separate runs of 15 reps on a quiesced host, spread 0.7% across rounds. All 13 other benched endpoints flat within +-1.6%. The before-state is the round-two baseline bench, which is this experiment's immediate predecessor on the same host.

Worth reading alongside the query figure rather than instead of it: removing 34% of the endpoint's queries bought 5.8% of its wall clock, about 1ms per query. That is consistent with this branch's finding that a query costs ORM round-trip overhead rather than SQL time -- total database time across the whole run moved 601ms to 602ms while query count fell 57%.

**Controls.** All 38 scenarios 200/302, no query count unstable, no other scenario moved. Re-running the attribution shows the core/api/serializers.py:619 call site gone entirely rather than reduced.

**Tests.** dcim test_api 1673 OK (41 skipped), core test_api 61 OK (2 skipped)

Found by attribution rather than by reading code, and the attribution predicted the exact figure: 100 dcim_devicebay queries from one call site, 100 removed. Verified the mechanism in isolation first -- 20 interfaces cost 20 queries with a plain select_related(device) and 0 with select_related(device__parent_bay) -- so the change was measured before it was written.

Rollout candidates: every viewset whose serializer nests a Device carries the same cost. FrontPort, RearPort, ConsolePort, PowerPort and the other component viewsets are the obvious set, and they use the same connection_prefetch_related_fields() helper. Left for a follow-up so this commit's number stays attributable, as the TemplateColumn fix was rolled out separately from its measurement.

### 26 · Prefetch cable paths for peer terminations that are not on the page

**Wall clock.** api.interface.depth1 1703.9 -> 1458.0ms, median of three rounds of 15 reps against the finding 25 state on the same host. Controls flat: api.cable.list +0.3%, ui.device.detail -1.2%, api.interface.list -0.8%.

It beat the -9.5% predicted from finding 25's ~1ms-per-query relationship, because the batch collapses the GenericForeignKey destination fetches as well as the path lookups. A per-query heuristic understates a fix that removes two query classes at once.

The depth guard is worth 56ms on its own. Without it api.interface.list runs 791ms against 735ms guarded -- 7.6% -- over three alternating rounds each way, because four batch queries fetching 83 rows each are not four point lookups. Arm-differ proof: guarded reported api.interface.list at [23, 23, 23] queries and unguarded at [27, 27, 27], with api.interface.depth1 at [30, 30, 30] in both.

Method caveat: the A/B ran the guarded arm first in every round rather than reversing order, so a monotonic drift biases one arm. api.device.list and api.device.list.depth1 read +7.1% and +4.9% for the guarded arm despite having no get_cable_peer at all -- within their known 7.4% worst-case spread and aligned with that bias, so those two readings are not claimed.

**Controls.** api.cable.list +0.3%, ui.device.detail -1.2%, api.interface.list -0.8%; all 38 endpoints 200/302 with no unstable query counts

**Tests.** dcim test_api 1673 OK (41 skipped), circuits test_api 162 OK (3 skipped)

Found by attribution, and attribution also killed the obvious version of the fix. Adding a cable_paths prefetch to the viewset would have measured exactly zero: Interface.connection_prefetch_related_fields() already declares a GenericPrefetch for cable_paths__destination. The queries were never a missing prefetch -- they are peers that are not page members, so the viewset's prefetches never reach them, and the fix has to land where peers are fetched.

Also disproved: .first() on a prefetched reverse FK does not clone the queryset and re-query. It costs 0 queries, so _first_cable_path's memoization was never the bottleneck it resembled, and a fix built on that premise would have been wrong.

Rollout candidates: the other PathEndpointMixin viewsets -- FrontPort, RearPort, ConsolePort, PowerPort -- serialize peers the same way and share the same helper. Left for a follow-up so this commit's number stays attributable.

**Retracted follow-up, and finding 30 is what retracts it.** This record used to claim that the depth guard withholds this fix from dcim.interface-connections -- that the endpoint ignores `depth`, never satisfies `depth > 0`, and so pays _first_cable_path and _destination 50 times each on a page of 25, and that re-keying the guard on whether peers are serialized would fix both endpoints. It would not. With the guard deleted outright on an otherwise unchanged tree, interface-connections still costs 359 queries and those two sites still cost 50 each: its page is CablePath rows, CablePath has no get_cable_peer, and _warm_peer_cable_paths therefore skips every row whatever the guard says. The guard here is measured, correct, and not involved in that endpoint at all. What actually fixed it was prefetching both ends of the connection from the viewset -- see finding 30, which carries the measurement.

### 27 · Batch get_prev_change for deferred change logging

**Wall clock.** The prize was measured before the fix was attempted, by wrapping get_prev_change to count and time its calls while running the same operations Tier 1W runs. Batching cannot recover all of it -- the batch is still one query -- so the realistic saving is ~3.0% of a bulk update or delete.

This also corrects how the target was described. get_snapshots() was called the largest remaining SQL item on the write path, which is true by query count -- 100 queries per 100-object operation -- and false by time. Query count misranking a target, again: the same lesson as the rejected FK pre-warming, arrived at from the other direction.

| | |
|---|---|
| measured prize | 49 get_prev_change calls costing 101.3ms of a 3278.7ms bulk.update.x100.deferred (3.1%); 50 calls costing 96.9ms of 3535.0ms on bulk.delete.x100 (2.7%); 0 calls on bulk.create.deferred, since it only fires for non-CREATE actions; 2 calls costing 4.1ms on delete.device (0.9%) |

Upstream issue #6303 remains open and the TODO in context_managers.py:273 is accurate about the mechanism -- one query per object change. What this record adds is the size of the prize, so nobody spends the effort twice to find out.

Worth revisiting if the write path ever becomes latency-critical, or if the ObjectChange insert and index-maintenance costs (finding 24) are removed first, since 3% of a smaller number is a larger share.

### 30 · Prefetch both ends of an interface connection, which the serializer renders in full

**Wall clock.** Three alternating rounds (B/A, A/B, B/A), each round the median of five reps, under the timing lock on a quiesced host. Round medians then A / B / delta: limit 50 1407/1699/1432 -> 1432 against 556/555/548 -> 555, −61.3%. Limit 25 695/755/698 -> 698 against 358/350/352 -> 352, −49.6%. Limit 5 191/277/189 -> 191 against 184/202/183 -> 184, −3.4%, which is what a fixed-cost-dominated page should look like and is the reason the per-object normalization in finding 28 exists.

The saving scales with page size because the cost removed was per object and what is left is not.

| | |
|---|---|
| call sites | all seven eliminated -- .cable''s two queries, _get_cable_peer_rows, CableToCableTermination.termination, _first_cable_path, _destination, and the tag lookup. What remains is the viewset's own prefetches. |
| response | byte-identical between arms -- same sha256 for all three connection endpoints at all three page sizes |
| read path | Tier 1 total 695 -> 695, no endpoint moved by a single query |

**Controls.** power-connections limit 25 +5.1% and limit 50 −0.5%; console-connections limit 25 −0.5% and limit 50 −2.8%; all four with query counts identical in both arms. The UI interface-connections list view is the important one -- it shares the classmethod this changed -- and reads 8 queries in both arms and −1.4%. The +5.1% sets the noise floor, which the two headline figures clear by an order of magnitude. Tier 1: all 38 scenarios 200/302, nothing unstable, gate PASS.

**Tests.** dcim test_api + test_views 4158 OK; dcim test_cablepaths + test_tables + test_filters + test_models 1150 OK (29 skipped). 5308 in total, no failures.

This record previously carried three claims about this endpoint. One was right and two were wrong; the wrong ones are recorded in more detail.

Wrong, and previously described as the interesting part: that finding 26's `depth > 0` guard withholds its own fix from here, and that re-keying the guard on whether peers are serialized would fix both endpoints. Falsified by measurement rather than by reading -- with the guard deleted outright on an otherwise unchanged tree, this endpoint still costs 359 queries and _first_cable_path and _destination still cost 50 each. The page here is CablePath rows; CablePath has no get_cable_peer, so _warm_peer_cable_paths skips every row whatever the guard says. Finding 26's guard is measured, correct, and simply not involved. Finding 26's own record carried this as a follow-up and is corrected too.

Wrong: that the queryset has "no select_related and no prefetch_related of any kind". It has a GenericPrefetch on both ends joining the parent device, and its two queries are visible in the attribution under paginate_queryset. The endpoint was under-prefetched for what its serializer renders, not unprefetched.

Wrong: that powerconnections and consoleconnections are "almost certainly the same viewset shape, so one fix plausibly covers three endpoints". They are separate viewsets over PowerPort and ConsolePort with their own querysets. This change does not touch them and they are measured here still at 5.2-5.4 queries per object. One fix covered one endpoint. The other two are a named follow-up and want the same treatment: their serializers render a full ConsolePortSerializer / PowerPortSerializer per row and their querysets prefetch cable_paths__destination but nothing for .cable, the cable peer rows, or tags.

Right, and it was the whole basis of the fix: the 9 + 14n fit, which held to the query at three page sizes, and the attribution of 350 of 359 queries to seven call sites at exactly 50 each.

The first two are the same mistake in two forms -- reading the code and reporting what it looked like, in a record whose own numbered sites were measured. The measured half was correct and the read half was not. That split has now appeared six times on this branch.

### 31 · Fix the tag cache in serialize_object, which can never fire for an untagged object

**Wall clock.** Three alternating rounds (B/A, A/B, B/A), snapshot restore and container restart before every arm, each round the median of nine reps. Round medians then A / B / delta: bulk.create.x100.loop 3256/3218/3110 -> 3218 against 3047/2926/2994 -> 2994, −6.9%. bulk.create.x100.deferred 3108/3111/3090 -> 3108 against 2933/2817/2990 -> 2933, −5.6%. create.interfaces.x50 1573 -> 1499, −4.7%. create.interface 48.9 -> 45.1, −7.6%. create.device 83.7 -> 79.8, −4.7%.

bulk.update.x100.deferred is the interesting non-result: −48 queries and −0.8% wall, inside the noise floor. The tag lookups it removes are point selects on an empty table, so removing them buys nothing there, and the operation is another entry in this branch's ledger of query count misranking a change.

The nine-rep median is a new instrument, and the reason for it is finding 32: tier1w_writes.py reports the LAST of its reps, and a gen-2 GC collection costing ~200ms recurs on a period that is deterministic for a given tree. It landed on the reported rep of create.device for this change and not for its baseline, in all three rounds, reading 89ms against 248ms for an operation that actually costs ~84ms either way. Three alternating rounds cannot cancel a bias that is not random. See finding 32.

| | |
|---|---|
| read path | unchanged -- Tier 1 total 695 -> 695, no endpoint moved by a single query |
| serialize object | 40 queries for 20 Interfaces -> 20; the extras_tag lookup disappears entirely |
| not improved | bulk.delete.x100 stays at 2019. Django clears an instance's pk on delete, so at flush time there is nothing to look the tags up by, and the batch warm skips those rows. |

**Controls.** bulk.create.x100.bulk_create, which skips change logging entirely, −2.2%; bulk.delete.x100, whose query count does not move, +2.6%. Those two set the noise floor at about ±2.6%, which every claimed figure above clears. Read path: all 38 Tier 1 scenarios 200/302, no query count changed, no query_count_stable went false.

**Tests.** 4027 OK across five suites and no failures -- extras test_changelog + extras test_tags + core test_utils 113 OK; extras test_api + circuits test_api + core test_api 1698 OK (74 skipped); dcim test_api + ipam test_api 2216 OK (59 skipped)

Three parts, and the first is not optional. `getattr(obj, "_tags", None) is not None` in serialize_object makes the cache fire. But TaggedModelSerializerMixin.update already wrote `instance._tags = tags or []` unconditionally, and `tags` is None whenever the payload does not mention tags -- so fixing the check alone would have made every REST PATCH that leaves tags alone log the object as untagged. The mixin now sets the cache only when the request actually carried tags. This is the trap the falsy check was accidentally covering, and it is why the check and the mixin have to change together.

Then the two populate sites. A just-created object provably has no tags: the row must exist before a TaggedItem can reference it, assigning tags fires its own m2m_changed and its own ObjectChange, and deleting an object deletes its TaggedItem rows with it -- verified -- so a recycled primary key cannot leave stale ones. The deferred flush cannot make that assumption, because it serializes later, so it reads the whole batch's tags in one query instead. Both caches are dropped again at the end of the scope that knew they were true.

This is the Tier A route to roughly half of what finding 22 measured, and it arrives at Tier C for a reason finding 22 does not share: 22 changes the API payload by removing data, this one changes a single logged value by correcting it. 22 needs a deprecation cycle and 13 test updates; this needs neither, and no test asserted the old behaviour -- 1,811 tests pass unchanged.

The obvious follow-up is bulk.delete, and it is probably not worth taking. Warming `_tags` to an empty list for an instance whose row is gone would remove its remaining 50 tag queries, and it would be correct -- the TaggedItem rows are deleted with the object, so both paths return nothing. But the comparable case is already measured: bulk.update.x100.deferred lost 48 queries of exactly this kind and moved 0.8%, inside the noise floor. Priced before it is built rather than after.

Supersedes this record's own earlier claim of −50% on serialize_object (4.59 -> 2.30ms per object). That figure is still what the isolated probe measures and it reproduced exactly (40 / 40 / 20 queries, 4.88 -> 2.49ms per object), but it is the ceiling of the serializer in isolation, not what the change is worth in a write: the operations it sits inside improve by 5-7%, not 50%.

### 34 · Prefetch the cable walk for power- and console-connections, the two endpoints finding 30 left

**Wall clock.** Three alternating rounds (B/A, A/B, B/A), each round the median of five reps, on a quiesced host. power-connections limit 50: B 541/517/520 against A 254/253/257, -51.0%. Limit 25: B 299/317/284 against A 170/178/171, -42.7%. console-connections limit 50: B 510/536/510 against A 248/246/248, -51.4%. Limit 25: B 275/272/290 against A 164/164/167, -40.4%. No overlap between arms on any of the four, and every arm's three rounds sit within 1.1x of each other.

The controls set the noise floor. interface-connections, which this change does not touch, read 17 queries in both arms and +0.2% at limit 25 and +0.4% at limit 50; the UI interface-connections list view read 8 queries in both arms and -1.5% and -3.3%. So the floor is about 3.3% and the four wins clear it by an order of magnitude.

These are the second set of numbers taken for this change. The first set was measured with a container restart between arms and is void -- see finding 35, which that measurement is what found.

| | |
|---|---|
| attribution | a page of 25 put 125 of power-connections' 135 queries in five call sites at exactly 25 each -- device_components.py:367 and :368 (.cable, reading the join row then the cable), device_components.py:466 (_get_cable_peer_rows), cables.py:1308 (termination, resolving the per-type FK), and core/api/serializers.py:619 (tags). console-connections is identical. |
| response | byte-identical between arms -- the same sha256 for both endpoints at all three page sizes across all six arms of the A/B |
| read path | Tier 1 total 695 -> 695, gate PASS, no endpoint moved by a single query |

**Controls.** interface-connections and the UI interface-connections list view, both untouched by this change: query counts identical in both arms, wall clock within +-3.3%

**Tests.** dcim test_api 1673 OK (41 skipped) in 324.0s; dcim test_cablepaths 68 OK. 1741 in total, no failures and no errors, both labels confirmed resolved. Run with --no-keepdb, because an earlier --keepdb run was killed mid-flight. Findings 34 and 36 were tested together in one tree, so this evidence covers both.

Finding 30 is why this was cheap, and specifically the part of it that refused a guess. Its record originally read that power- and console-connections were "almost certainly the same viewset shape, so one fix plausibly covers three endpoints"; the commit corrected that to "one fix covers one endpoint, the other two are a named follow-up, not a freebie". Both halves of the correction turned out to matter. The viewsets really are different, so the fix is not a copy -- but the underlying cost really is the same five call sites, so the prefetch set transfers exactly. Attribution settled in one command what neither guess would have.

The shared helper is the whole change. `_interface_connection_endpoint_queryset()` became `_with_connection_prefetches(queryset)`, taking the queryset instead of building one, with the interface caller kept as a one-line wrapper so finding 30's path is provably unchanged -- and its 17-query count in both arms of this experiment is that proof. Two of the seven sites (`_first_cable_path` and `_destination`) are paid only by interface-connections, whose serializer resolves each end's cable path; prefetching `cable_paths__destination` for all three costs the other two nothing, since they already had it.

Risk tier B1, no migration. The new state is prefetch caches on a queryset, which live and die with the request.

### 36 · Prefetch the cable's terminations for cable-to-cable-terminations at depth 1

**Wall clock.** Three alternating rounds (A/C, C/A, A/C), each round the median of nine reps, on a quiesced host, with no container restart between arms (see finding 35). Limit 25: 1227/1160/1181 against 380/377/369, -68.1%. Limit 50: 2161/2162/2212 against 524/528/533, -75.6%. No overlap between arms, and each arm's three rounds sit within 1.06x.

Controls, both untouched by this change and both with identical query counts in each arm: interface-connections +2.1% at limit 25 and +0.9% at limit 50; power-connections -2.8% and +0.2%. That sets the floor at about 2.8%, which the two wins clear by roughly 25x.

| | |
|---|---|
| attribution | a page of 25 put 475 of its 487 queries in six call sites -- cables.py:623 _get_termination_attr 300 (12 per row), cables.py:1308 termination 75 (3 per row), cables.py:799 connected_lanes 25, device_components.py:466 _get_cable_peer_rows 25, api/serializers.py:261 _first_cable_path 25, api/serializers.py:270 _destination 25 |
| staged | the cable__terminations prefetch alone took a page of 25 from 487 to 63 and removed four of the six call sites; the nine per-type cable_paths prefetches removed the remaining two, 63 -> 15 |
| residual | at limit 50, 29 of the 33 queries are inside paginate_queryset -- the prefetch machinery evaluating -- and the table breakdown is 15 dcim_location, 3 dcim_cablepath, 3 dcim_device, 3 tenancy_tenant. No per-row N+1 remains; what is left scales with the number of distinct locations on the page rather than with rows. |
| response | byte-identical between arms at every page size -- same digest and same byte count (94,901 bytes at limit 25, 188,450 at limit 50) |
| read path | Tier 1 total 695 -> 695, gate PASS |

**Controls.** interface-connections (17 queries both arms) and power-connections (12 both arms), wall clock within ±2.8%

**Tests.** dcim test_api 1673 OK (41 skipped) in 324.0s; dcim test_cablepaths 68 OK. 1741 in total, no failures and no errors, both labels confirmed resolved. Run with --no-keepdb, because an earlier --keepdb run was killed mid-flight. Findings 34 and 36 were tested together in one tree, so this evidence covers both.

The generalization is Kevin's and it is better than the one this record first carried. I described `_get_termination_attr` as code diverging from its docstring. It does not. The docstring says iterating `.all()` rather than `.filter()` means a prefetched cache IS HONORED IF ONE EXISTS -- it never claims one always exists. The method is written correctly and deliberately to be prefetch-friendly, and this fix works precisely because its authors wrote it that way.

The actual pattern is an affordance added without its call sites being updated to use it, and this branch has already paid for the same thing once: finding 7 built a caching TemplateColumn and finding 11 existed only because just the device tables had been switched over to it. TERMINATION_PARENT_FK_FIELDS is a third instance waiting to be checked -- its own comment says it exists "to extend select_related so that rendering termination.parent ... stays query-free per row", which is an affordance with a stated purpose and an unaudited adoption list.

That reframing is more useful than the fix, because it predicts where to look rather than describing what was found. Screening affordances by adoption searches a far smaller space than the 166 endpoints a cost screen covers, and it points at causes rather than symptoms. Proposed as an instrument, not yet built.

Risk tier B1, no migration. The new state is prefetch caches on a queryset, which live and die with the request.

### 41 · Natural-key map on three more traversed-into models

| | |
|---|---|
| screen | 79 of 159 models have a traversing natural key; 416,459 hop-queries to serialize every object once. Worst payers: dcim.interface 24 hops x 8,925 rows, ipam.ipaddresstointerface 37 hops x 2,937, dcim.device 16 x 2,902. |
| ab | write screen both ways, 254 measurements present on both arms: 19,111 -> 19,027 queries, **−0.4%**. 242 of 254 unchanged. Four measurements got one query *worse* -- ipam.ipaddress and ipam.ipaddressrange pay the map's build query without traversing into the mapped tables often enough to recoup it. |
| where it helps | ipam.ipaddresstointerface create.x10 637 -> 590 (−7.4%); load_balancers.virtualserver create.x10 168 -> 149 (−11.3%); ipam.prefixlocationassignment create.x10 351 -> 342 (−2.6%). Real, and confined to models that traverse into namespace or prefix repeatedly. |

**Controls.** Both arms measured with screen_writes.py against the same campus dataset on the same tree, differing only in the three attributes. Coverage identical: 105 models measured either side, 257 measurements each.

**Tests.** not run: the change was declined before it earned a test run

What this closes.

**Natural-key traversal is not the write path's problem.** Four separate approaches have now been measured against it: hop-by-hop with a whole-table map (finding 14, accepted, and it does earn its place on the read path), the same map in Redis across requests (finding 17, +0-2%, rejected), eager select_related over the whole chain (finding 15, −666 queries and 62% slower, rejected), and this. The ceiling on the whole class is small because the cost is small.

**A stored natural-key fragment -- materialized path -- was considered and not attempted.** Two things block it independently. Location's natural key is recursive by name (["name", "parent__name", "parent__parent__name", ...]), so renaming one Location changes the natural key *value* of every descendant and of every object whose key reaches one: the screen counts 24 models traversing into dcim.location across 249,575 rows. And QuerySet.update() and bulk_update() compile to one SQL statement, emit no post_save, and so cannot invalidate a stored column -- the same hole finding 17 rejected a Redis cache for, except a column has no TTL to fall back on. A cache can be invalidated coarsely by dropping the model's map; a column cannot.

**What the screen is still good for.** dcim.device is the largest un-mitigated traversal target -- 15 models traverse into it across 89,054 source rows -- and it is too large for a whole-table map. Nothing addresses it today. And Nautobot already depends on django-tree-queries, whose recursive CTE returns an ancestor chain in one query; natural_key() walks with getattr instead. Neither is priced.

### 42 · Route natural_key() through the ancestor walk Nautobot had already optimized

| | |
|---|---|
| apply | whole-workflow datacenter apply, three arms on one box, trees proved different by content hash: baseline 711,568 queries / 1257s; map flags only 707,252 / 1251s; both changes 699,894 / 1254s. **−11,674 queries, −1.6%**, of which the ancestor routing owns 7,358 (63%) and the three map flags 4,316. |
| isolated | a depth-4 Location's natural key costs 4 queries walked hop by hop and 2 via ancestors(), the 2 including the ancestors() call itself. |
| screen | write screen both ways, 254 measurements present on both arms: 19,111 -> 19,027 (−0.44%), 242 unchanged, zero unstable query counts. Four measurements are one query *worse* -- ipam.ipaddress and ipam.ipaddressrange pay the map's build query without traversing into the mapped tables often enough to recoup it. |

**Controls.** Four arms measured on the same box against the same dataset from the same empty template, each swap proved by a content hash over nautobot/**/*.py: fda67f172 (neither), 9dba9144c (map only), 90dbd96ff (both), against ce01a0464 stock next from the earlier series. Natural keys verified unchanged rather than merely cheaper -- full ancestor chains resolve identically, checked on the three deepest Locations and on an Interface whose key traverses five Location levels.

**Tests.** **Not run.** Landed at Kevin's direction without a suite run, because the full suite is 2-2.5 hours. This touches natural_key(), which every serialization path goes through, so the gap is real and is recorded here rather than implied.

Taken on query count and code duplication, not on speed. Wall clock across the three arms is 1257 / 1251 / 1254 seconds -- a six-second spread, inside the 0.2% within-arm reproducibility this host holds. Nobody will perceive this. What it removes is 11,674 database round trips per apply and a hand-rolled ancestor walk that duplicated a better one twenty files away.

**Two predictions I made that measurement contradicted.** First, that the ancestor routing would contribute nothing over HTTP: the write screen showed it doing exactly zero there, because the screen posts one model at a time and the whole-table map is already warm. On a real apply -- 9,972 objects across 68 models, many requests, many cache scopes -- it owns 63% of the saving. The screen and an apply are not the same HTTP path, and this is the third time today the screen under-read something the apply caught, after cable cost (44.9 against 220 queries) and the map flags themselves.

Second, finding 41 declined those three map flags at −0.4% off the write screen. On an apply they are worth 4,316 queries. The screen averages 105 models; an apply creates thousands of rows in a handful of them. **Ranking a change by an instrument whose weighting does not match the workload is how both errors happened.**

**This is the fifth instance of one pattern on this branch**: an affordance added and its call sites never updated. Finding 7 built a caching TemplateColumn and only device tables adopted it (finding 11 was the cleanup); finding 34 was _with_connection_prefetches with two endpoints left behind; finding 36 was .all() written to be prefetch-friendly and never prefetched; and CableSerializer._apply_terminations still does not use defer_cable_path_rebuilds(), which is item 3 on the queue. Screening affordances by adoption searches a far smaller space than a cost screen over every endpoint, and it points at causes rather than symptoms.

### 48 · Defer cable path rebuilds in the REST create path, the fourth instance of one shape

**Wall clock.** Three alternating rounds with arm order reversed between them, rep 1 discarded per the cold-process-cache rule, on a quiesced host through measure.sh. The reps disagree on sign: rep2 631.5ms -> 605.4ms (−4.1%), rep3 463.5ms -> 465.1ms (+0.3%). The control settles it as noise rather than the README's spread figures having to: the one-ended case read −11.2% for arm B, which does strictly more work there, and a directionally impossible reading is demonstrably noise. The attributable rebuild_ms is clean at ~66.5ms -> ~35.5ms but is a sub-measurement, not end-to-end. Per the gate rules the query count decides and wall clock only ranks, so this is recorded as inconclusive rather than dressed up.

| | |
|---|---|
| attribution | measured before the change, not inferred from it. rebuild 1 costs 13 queries and 20ms against a cable with one termination attached; rebuild 2 costs 30 queries and 43ms against the finished cable. Together 43 of 214 queries (20%) and 63ms of ~570ms (11%). Re-derived through a second mechanism -- counting post_save/post_delete receiver fires on CableToCableTermination -- as 2 saves and 0 deletes, and corroborated structurally by 6,556 termination rows across 3,278 cables, exactly two each. |
| queries in rebuild | 43 -> 22 |
| rebuilds | 2 -> 1 |
| compounding | the coalesced rebuild costs 22 queries, not the 30 rebuild 2 cost alone, because rebuild 2 had been partly clearing rebuild 1's partial CablePath rows. Skipping the first rebuild avoids that cleanup too, so the saving compounds instead of merely dropping the cheap half. A prediction of −10 to −12 made before the run was too conservative for that reason, and is recorded here because the mechanism was flagged as unknown in advance. |
| read path | Tier 1 pre against post 2,057 -> 2,057 queries, gate PASS, nothing newly erroring, no query_count_stable false. Tier 1W 13 operations, zero query-count differences. |

**Controls.** a one-ended create, which regresses by design: one termination row means one rebuild in both arms, so the only difference is the deferral's fixed overhead. 125 -> 128 queries, +3. Attributed by diffing the two arms' SQL rather than left as a residual -- +2 from one extra SAVEPOINT/RELEASE pair, because defer_cable_path_rebuilds() opens its own transaction.atomic() nested inside DRF's perform_create (6 pairs against 7), and +1 from the flush's Cable.objects.filter(pk=cable_id).first(). The arithmetic closes exactly: −21 inside the rebuilds, +3 overhead, −18 net. Availability control: 36 of 36 probe measurements HTTP 201.

**Tests.** nautobot.dcim.tests.test_cablepaths plus test_api.CableTest and test_api.CableToCableTerminationTest, --no-parallel so a worker exception surfaces rather than exiting 0. Ran 145 tests, OK, skipped 4, zero failures and zero errors, system check clean. All three labels resolved: an unresolvable label errors rather than silently shrinking the run, and errors was 0.

Scope limit that bounds what this figure means. Measured on enterprise-campus/large, whose cable A-ends are interface (2,780) and power_port (340) with **zero** front_port -- verified a true negative rather than a broken filter by confirming the deprecated termination_a_type filter still works and returns 0. The front-port patch-panel shape is what a databot apply sends and what finding 40 measured at ~220 queries per cable, and its paths walk on through rear ports, so its rebuilds should cost more than the shapes measured here. This is a conservative floor on the change's value, not a central estimate; a datacenter-dataset run would price it against the apply directly.

Neither gate instrument can see this change. The write screen's generated cable payload leaves both terminations null (finding 40), so the loop never runs -- which is the same reason the screen reads dcim.cable at 44.9 queries per object against ~220 for a real one. Tier 1W has no cable operations at all. The measurement had to come from a probe that posts real terminations, perf/scripts/probe_defer_cable_paths.py.

Not taken here, and logged in queue.md as *Nested transactions on the write path*: the +3 overhead splits 2:1. Removing the re-fetch is the smaller half and the only part that can produce a wrong answer -- CableViewSet prefetches terminations (api/views.py:815), so on an update the instance arrives with a cache the loop invalidates, and rebuild_paths reads obj.terminations.all(), so using the caller's instance unguarded would build paths from a stale termination set with no error anywhere. The savepoint half is larger, has no correctness surface, and carries exact predictions from this control: one-ended 128 -> 126, two-ended 195 -> 193.

### 49 · savepoint=False in defer_cable_path_rebuilds() — measured, works, rejected on the trade

**Wall clock.** Three alternating rounds, rep 1 discarded. Medians moved −8ms on the two-ended case and +5ms on the one-ended, both inside the spread that finding 48 demonstrated is noise at this granularity -- there, a case doing strictly more work read 11.2% faster. A 4-statement change was never going to clear that floor and the figure is recorded as inconclusive rather than quoted.

| | |
|---|---|
| why four not two | the step-3 gate measured the helper being entered twice per create rather than once, both already inside a transaction. The −4 on both payload shapes corroborates that independently -- including the one-ended case, where Cable.save()'s entry materializes zero rows and paid a savepoint pair for nothing. |
| scope | the savepoints removed were the helper's, not rebuild_paths()'s own. Queries inside the rebuild are unchanged at 22 (two-ended) and 13 (one-ended) across both arms, which is the discriminating detail: the inner atomic still creates its savepoint, exactly as intended. |
| read path | Tier 1 2,235 -> 695, gate PASS, availability intact, 36 of 36 probe measurements HTTP 201. |

**Controls.** both cable shapes are targets here rather than one being a control, because both pay the savepoint. The control set is Tier 1W's 13 non-cable write operations. Availability held on every Tier 1 scenario.

**Tests.** nautobot.dcim.tests.test_cablepaths: **ran 68 tests, FAILED, errors=1** -- test_defer_rolls_back_on_exception in CableToCableTerminationSignalTestCase, with TransactionManagementError at test_cablepaths.py:2425. Note the run **exited 0** while failing, because it was piped to tail; the summary line is what surfaced it, which is why this branch does not read exit status.

Two things this experiment found that outlive its rejection.
**Cable.save() enters the deferral and writes nothing on the REST path.** Measured: cables.py:1023 rows_saved=0, serializers.py:1236 rows_saved=2. The serializer pops the legacy termination_a/b fields into its own entries before the model is constructed, so _materialize_initial_terminations() opens a transaction, materializes zero rows, and exits. The two methods are not redundant -- one takes two generic FKs by attribute assignment, the other a terminations payload with cable_end and connector for breakout lanes -- but one of them runs empty on every REST cable create.
**Nothing in the codebase distinguishes the two things atomic() does.** 61 transaction.atomic() call sites in nautobot/ outside tests, and zero use savepoint=False, while one cable create sits 6 savepoint pairs deep. ATOMIC_REQUESTS is unset, so those are genuine transaction boundaries rather than decoration -- roughly 32 are view-level and 22 are outside the request path entirely (dcim/signals.py 5, ipam/models.py 3, core/jobs 3, dcim/models/cables.py 2, plus management commands, the git datasource and the CLI). See the queue candidate.

### 50 · Where a list view's 283ms goes, and it is template rendering rather than the view

**Wall clock.** **The instrument was validated before any of these numbers were quoted**, because probe_page_phases attributes ~48ms to inc/nav_menu.html and this branch has a standing rule about that exact figure. Three rounds, arms alternated, rep 1 discarded: uninstrumented 278.1ms, instrumented 282.5ms — overhead +4.5ms, +1.6%. Stubbing inc/nav_menu.html to the empty string removes 45.6ms, 16.4% of the page, against 48ms attributed: agreement within 5% by a method that shares no code with the timing wrapper. Timing a wrapper says what a template cost according to the wrapper; deleting the template says what the page costs without it, and it is the two agreeing that makes the first quotable.

| | |
|---|---|
| on cpu | ui.device.list is 277.9ms CPU of 282.9ms wall — 98.2% on-CPU and 97.5% of that user time (275.8ms user, 2.1ms sys), 5.0ms off-CPU of which the database explains 4.5ms, and 29 voluntary context switches. No hidden wait: no Redis stall, no lock, no page-fault storm. **eBPF would have found 5ms.** The question was never whether the page blocks, it was which Python runs. |
| phases | view 21.4ms, render 261.1ms, database 4.3ms across 9 queries. **91.6% of the request is template rendering.** That kills the standing hypothesis in queue.md that filter-form construction or table-column construction over an empty queryset is where the cost is — both happen in the view, which is 7.6% of the page. view_ms is 21ms on the document and 21ms on the HX rows request, the same view either side, which is the internal check. |
| chrome | inc/nav_menu.html costs 46.2–48.4ms exclusive on **every** chrome-bearing page — 47.4 on the 404, 46.2 on home, 47.8 on device detail, 48.4 on rack detail, 47.2 on the device list. A 2ms spread across five unrelated views. It is a plain `{% include %}` from base_django.html:24, and also from admin/base.html, graphiql.html and the browsable API. Absent from HX partials and from API responses, which pay no chrome at all. |
| construction vs rendering | **_build_nav_menu is 1.1ms; rendering the same data is 48.4ms, 44x more.** One build per request, so finding 12 fixed the right thing and fixed it completely. This also reconciles the cProfile incident behind this branch's never-quote-profiler-time rule: 40ms profiled against ~16ms true was the *builder*, now 1.1ms. The 48ms is a separate cost nothing had measured, because nothing timed templates. |
| list view extra | the filter drawer is ~46.5ms inclusive — 33 select widgets and 156 renders of django/forms/widgets/attrs.html, Django's per-attribute template. List views only: no drawer on the 404, home, or either detail page. So the fixed cost is ~47ms on every full HTML page and ~95ms on a list view, not ~95ms everywhere. |
| biggest single item | **~0.6ms to render one table cell.** ui.device.list.rows emits 1,000 <td> cells at 10 per row; inc/table.html is 479.9ms exclusive (0.480ms/cell) and the 500 string-compiled TemplateColumn renders add 119.4ms (0.239ms each). Together ~64% of a 747ms request.

**The template file is not the defect and reading it will not show you anything** — it is 105 lines of ordinary markup, a header loop with sort links and a body loop. The figure is time executing its *nodes*, which is loop overhead plus what each node pulls on: table.visible_columns, column.attrs.th.as_html, column.header, column.order_by_alias, querystring_replace per orderable column, and per-cell Accessor resolution. Those are django-tables2 properties evaluated once per cell, which is why the work lands on the template that happens to be executing. Exclusive time only subtracts nested Template.render calls, and the only nested templates here are the TemplateColumns already counted separately.

Recurs everywhere: 93.9ms across 6 renders on rack detail, 17.3ms across 7 on device detail. role_retrieve.html includes it **eighteen times** and is not in the workload. Anything done about it inherits `third-party-coupled`, as findings 07 and 11 do, because the cost is inside django-tables2 rather than in Nautobot. |
| queries fire inside templates | on the detail pages the queries are not in the view. ui.device.detail: 49 queries inside rendering against 3 outside, 54.6ms against 1.9ms. ui.rack.detail: 60 inside against 3 outside. Prefetching on those views has nothing to attach to. |
| other scenarios | api.device.list is 441ms of database across 8 queries — ~55ms per query, render 2.3ms, no templates: a different problem needing different work. ui.home issues 79 queries. Both were found by extending the probe past the three scenarios it started with. |
| nav menu json | inc/javascript.html emits the whole menu again via `json_script`: 12,841 bytes, 3.2% of a 401,183-byte page. It is consumed — ui/src/js/search.js:26 reads it to build a Fuse.js index — but it flattens tabs and groups away and uses only item_link -> name, 5,231 bytes or 40.7% of the blob. The is_active and weight keys on all 123 items are never read. So ~7.6KB per page is serialised and discarded. Small against the 48ms of HTML: at most 2.3ms, since that is inc/javascript.html's whole exclusive cost. |

**Controls.** ui.chrome.404 and ui.search independently render identical chrome at 66.3ms and 64.7ms, which is the floor the 218ms is measured above. api.device.list and ui.device.list.rows act as negative controls for the chrome claim: neither renders nav_menu at all, and both show it absent rather than merely cheap.

**Tests.** not applicable — no product code changed. The instruments are validated instead, see wall_clock_detail.

The instrument is committed at 87643ff3b — three probes, harness only, nothing to adopt, which is why `commit` is null rather than naming it.

**This resolves the contradiction in queue.md's top candidate.** Two readings were on the record and disagreed: chrome ~274ms with list machinery ~83ms, or the reverse. On a quiet box chrome is 64–66ms measured twice independently, and the per-model term is ~218ms — the per-model reading, by 3.4x. The ~274ms chrome figure was contention inflation, taken with the suite running inside the nautobot container on the same cpuset as uwsgi. So the target is list-view machinery, not every page in the application.
Two caveats carried deliberately. The 404's view/render split is meaningless — error responses do not go through TemplateResponse, so it reports render=0.0 and view=wall; its per-template figures are still valid, which is how chrome reads as ~56ms of template work. And nothing establishes that only search.js reads the nav_menu script tag; one consumer was found, a second was not searched for properly.
**No change is proposed here and nothing is ranked.** What the numbers support is that nav_menu.html is a fragment-caching target rather than an algorithmic one — a pure function of registry["nav_menu"] plus what the user may see, 15 tabs / 42 groups / 123 items, static per deploy. queue.md already carries *Fragment caching with a derived change stamp* as an unranked candidate, parked because the thing it was aimed at measured ~0.6ms and failed the stopping rule. The mechanism may have been shelved against the wrong target. The obstacle is is_active, which is per-request state inside both the HTML and the JSON.

### 51 · Resolve the navbar-favorites URLs once per process, not once per menu item

**Wall clock.** Three alternating rounds with arm order reversed between them, on a quiesced host through measure.sh, five reps per scenario with one warm-up discarded. 66.9 -> 38.1ms, 197.3 -> 163.9ms, 280.5 -> 251.6ms. **The wall saving exceeds the reversal saving** — 28.8ms against 22.7ms on the 404 — because replacing `{% url %}` with `{{ variable }}` also drops URLNode.render's argument resolution, context lookups and autoescaping. That is consistent with the earlier split measuring 29.3ms inside the tag against 24.0ms inside reverse().

| | |
|---|---|
| attribution | 387 reversals in a chrome-only response produced **18 distinct URLs**, every one argument-free, costing 23.4ms of a 69ms page. 246 calls to user:navbar_favorites_add (123 items x 2 sites: the hx-post conditional and data-add-url), 123 to user:navbar_favorites_delete (data-delete-url), 18 across 16 other names once or twice each. Both paths are literal — path("user/") plus path("navbar-favorites/") — with no captured parameters, which is why the reversal takes no arguments. |
| reversals | ui.chrome.404 387 -> 18, ui.home 425 -> 56, ui.device.list 399 -> 30. Redundant repeats 369 -> 2, 368 -> 1, 369 -> 2. Bit-identical across all three rounds of each arm. |
| reverse time | ui.chrome.404 23.8 -> 1.1ms, ui.home 27.6 -> 4.2ms, ui.device.list 25.2 -> 2.0ms |
| read path | Tier 1 2,057 -> 2,057 queries (+0.0%), gate PASS, 57 scenarios, nothing newly erroring, no query_count_stable false, byte counts identical on every scenario. This change removes no queries, so the query counter cannot see it — which is why the reversal count is the gate. |
| what a reversal costs | ~51us: namespace resolution against a URLconf Nautobot mounts twice (user/ and users/, worth ~15us — the difference between a namespaced 52.6us and a plain 31.4us), a reverse_dict lookup, substitution, then a regex re-match to prove the URL routes back. Django caches every structure involved (get_resolver, get_ns_resolver at 2,113 hits and 0 misses, reverse_dict) and never caches the answer, because the answer depends on the thread-local script prefix and urlconf. Only the caller can know those are stable. |
| why this scope | reverse_lazy does not help — django.utils.functional.lazy re-invokes on every coercion, measured at 52.6us against reverse()'s 51.5us, so a lazy URL in a loop costs the same. An lru_cache keyed on (viewname, prefix, urlconf) is correct and was verified correct under a prefix change, but costs 10.5us per call for two thread-local reads plus hashing. A bare global with a None check is 0.18us, 284x cheaper than reversing. |

**Controls.** **ui.device.list.rows renders no chrome**, so it had no redundant menu reversals to remove: 502 reversals, 402 distinct and 100 redundant all unchanged, reverse time within 0.2ms across three rounds. Its wall reads +1.5%, against the change, within the noise this host has demonstrated at this magnitude — recorded rather than rounded away. Its 402 distinct reversals are per-row dcim:device URLs taking a pk, which are legitimately different and not memoisable; that is what the control demonstrates.

**Tests.** nautobot.core.tests.test_navigations, test_views and test_config, --no-parallel so a worker exception surfaces rather than exiting 0: ran 131 tests, OK, zero failures and zero errors, system check clean. test_views carries ForceScriptNameTestcase, the only test in the tree that mutates the script prefix; it issues no requests, so it cannot interact with the global — checked rather than assumed.

Found while attributing finding 50's ~47ms of chrome. That finding established nav_menu.html costs 46.2-48.4ms on every page and that construction is only 1.1ms; this says where roughly 28ms of the rendering went. The remaining ~20ms is ordinary Django template interpretation across 180 items and is not addressed here.

This is the fifth instance on this branch of work done per item that could be done once, after findings 07, 11, 12 and 34.

**Not done here.** inc/nav_favorites.html:21 has the same `{% url %}` inside a loop over the user's favourites. The measurement user has none, so including it would have put an unmeasured change in a measured commit. Same pattern, same one-line fix.

Two things worth knowing if this is revisited. The favourites are identified by a body field (`link`) rather than a URL segment, which is why both URLs are argument-free and therefore memoisable at all; a move to `DELETE /user/navbar-favorites/<pk>/` would make them 123 genuinely distinct reversals and this optimisation would evaporate. And core/urls.py mounts nautobot.users.urls twice, at user/ and at users/, which is why namespace resolution costs ~15us of every namespaced reversal in the application — logged in queue.md rather than fixed.

### 53 · Select_related the device list's primary IPs, which its own table cannot see

**Wall clock.** Three alternating rounds (A/B, B/A, A/B) through measure.sh on a quiesced host, nine reps per arm with one warm-up discarded. Arms proven distinct by nautobot/ content hash, e4f5405d23a05d09 against 422d3c225be16121.

**The conservative figure is the one quoted, and deliberately.** By mean of round medians the headline scenario reads −16.7%; by minimum it reads −14.3%. Arm A's round-to-round spread is 5.2% against arm B's 0.9%, so the mean is partly an artifact of where one of A's three noisy rounds lands, and the minimum is the defensible end. The noise asymmetry is itself coherent: arm A issues 100 extra point lookups and that is where the variance comes from. Range across the four target scenarios: −9.4% to −14.3% on the minimum, −9.9% to −16.7% on the mean.

Ordering bias is ruled out rather than assumed. Arm B ran second, first, and second across the three rounds and was faster in all of them; the effect (~127ms) dwarfs the ordering variation (~5%, ~38ms). Per finding 26 that is the direction of evidence worth trusting.

| | |
|---|---|
| ui.device.list.rows | 107 -> 7 queries, ipam_ipaddress 100 -> 0, wall 760.9 -> 633.5ms |
| ui.device.list.page100.rows | 108 -> 8 queries, ipam_ipaddress 100 -> 0, wall 758.8 -> 651.6ms |
| ui.device.list.deep.rows | 58 -> 8 queries, ipam_ipaddress 50 -> 0, wall 484.6 -> 436.9ms |
| ui.device.list.filtered.rows | 59 -> 9 queries, ipam_ipaddress 50 -> 0, wall 452.5 -> 402.8ms |
| scaling | the removed query count tracks page size exactly -- 100/100/50/50 across the four scenarios -- which is what makes it an N+1 rather than a fixed cost. The saving works out at roughly 1ms per eliminated query, the point-lookup cost on this host. |
| equivalence | response bodies are byte-identical between arms on all four targets. select_related changes how rows are fetched and not what they contain, and the digests prove it rather than asserting it. |
| what it does not fix | `get_settings_or_config` calls hold at 200 in both arms, but they are cheap: 0.94ms in total for the whole request. See the correction in `note` -- an earlier version of this record priced them at ~45ms and that was wrong. The remaining 633ms is template rendering, and after this change `primary_ip` is no longer the column to look at: re-attribution puts it at 39.8ms behind `actions` at 71.8ms and `tenant` at 69.4ms. |
| column re attribution | measured on the fixed tree rather than carried over from finding 50. `primary_ip` 178.9 -> 39.8ms, a 139.1ms drop that matches the ~127ms wall saving; accessor time within that column 137.1 -> 4.3ms. Every other column is unchanged to within 0.9ms (actions 76.5 -> 71.8, tenant 70.6 -> 69.4, location 36.1 -> 36.1, name 28.0 -> 28.0, device_type 26.7 -> 26.7, pk 23.8 -> 23.7, role+status 43.1 -> 42.4, rack 3.6 -> 3.6), which is the independent confirmation that this change did what it claims and nothing else. |

**Controls.** four, all flat: ui.device.list -0.3%, ui.devicetype.list.rows +1.4%, ui.rack.list.rows -0.3%, ui.interface.list.rows +0.0%. `ui.device.list` is the sharpest one available -- the same view and the same queryset construction, but it renders `queryset.none()` (core/views/renderers.py:96), so it isolates the cost of the two added LEFT OUTER JOINs from the saving on the rows and shows it is not observable. `ui.interface.list.rows` is kept because it displays IP addresses through a different mechanism (INTERFACE_IPADDRESSES via TemplateColumn); it does not move, so nothing here generalised further than intended.

**Tests.** `nautobot.dcim.tests.test_views.DeviceTestCase` 83 tests OK (skipped=1); `nautobot.core.tests.test_ui` + `nautobot.core.tests.test_utils` 132 tests OK. A regression test ships with the change and is proven in both directions: reverting the fix makes it fail with "rendering the device list issued 3 per-row ipam_ipaddress queries", three being the device count in the test data. It discriminates on the FROM clause, since a point lookup reads `FROM "ipam_ipaddress"` while the fixed version reaches the same rows through `FROM "dcim_device" LEFT OUTER JOIN "ipam_ipaddress"`.

**Correction, and it supersedes a claim in this record's own first version.** That version said the Constance read behind `primary_ip` costs ~224us per access and therefore ~45ms per request, making it "the larger half of finding 50's 178.9ms column". The 224us is real but it describes the path with no request cache: measured standalone, `get_settings_or_config("PREFER_IPV4")` costs 211-219us per call, stable to n=10,000. Measured *inside* the request it is a different number entirely -- 200 calls totalling **0.94ms**, the first costing 489us to fill the cache and calls 2 through 200 running at a median of **2.38us**. `nautobot/core/utils/config.py` carries `get_request_cache` and `_invalidate_request_cached_config`, and the cache is demonstrably per-request rather than per-process: the warm-up request did not make the measured request's first call cheap.

So the surviving config cost on this page is ~1ms, not ~45ms, and it is already cached by the mechanism the queue proposed adding. `perf/queue.md`'s "A Constance read costs 224us" entry drew the same wrong conclusion from the same standalone measurement -- its "config is 98% of the select_related case" line is an artifact of measuring outside a request -- and is corrected in the same commit. This does not affect any number in the experiment above: the query counts, the wall clock, the controls and the equivalence digests are all unchanged, because the config read was never what the change touched.

**The instrument had to be fixed before the experiment could start, and the defect was silent.** `attribute.py` read only `url` from the workload row and dropped `headers`. Nautobot defers row rendering to an HTMX follow-up, so `ui.device.list` and `ui.device.list.rows` share the path `/dcim/devices/` and differ only by `HX-Request` — which meant every `.rows` attribution ever run through that instrument described the chrome page instead. Both scenarios reported the same 9 queries and no `dcim_device` query at all, against a table that renders 100 rows: a plausible-looking attribution of a page the instrument was not measuring. Fixed in afaf66fdf. `perf/findings/` was grepped and no finding cites attribute.py, so nothing already published is affected. This is the fourth instrument defect on this branch to produce a confident zero rather than an error, after the three hook-placement mistakes.

**The same blind spot has three more instances and none is measured.** `VirtualMachineUIViewSet` select_relates `tenant__tenant_group` only and has `primary_ip` among its default columns; `VirtualDeviceContextTable` declares the same property column; and the `primary_ip` property exists twice in dcim/models/devices.py, on Device at 1006 and on VirtualDeviceContext at 2330, plus once more on VirtualMachine. Neither VM nor VirtualDeviceContext appears in the workload, so the cost there is inferred from shape rather than measured.

**The general fix is better and is deliberately not taken here.** Teaching core/tables.py to honour an explicit select_related hint on a property-backed column would fix all four sites at once, in one place, and would stop the next property column from silently reintroducing the same N+1. It also touches the table machinery every list view in Nautobot uses, which makes it a separate experiment with a far wider blast radius rather than something to fold into a two-line viewset change. Logged as a queue candidate.


## Corrections forced by measurement

**Query counts are bounded by page size rather than dataset size.** At 11x the data the counts
barely moved, which retroactively validates the small dataset as an instrument for finding
N+1s.

**Measurement corrected conclusions I had already drawn.** I claimed the hierarchy endpoints
scale with dataset size; page-size sensitivity testing showed that is true for exactly one of
five, and the UI list endpoints are entirely fixed cost. I put `nav_menu` at 40 ms per request
from a cProfile figure; true wall clock was ~16 ms. And `ui.prefix.detail` appeared to regress
11% until three repeat runs produced 329 / 447 / 347 ms on identical code.

**Hypotheses formed by reading code were wrong**, each corrected by instrumentation:
`Breadcrumbs.as_pair` was blamed for calling `.ancestors()` four times and calls it once;
`prepare_cloned_fields` was thought to run twice and runs once; an attribution of
`api.interface.depth1` named three small items and missed the item worth 70%; and the
ObjectChange double-serialization was assumed expensive on the redundant half, which is the
cheaper half. Reading code proved reliable for explaining a measurement and unreliable for
predicting one.

**A commit message credited a change with improvements it did not make.** `5f351dc5b` credits
serializer reuse with `bulk.update` 1,542 -> 1,472 and `bulk.delete` 2,089 -> 2,019. Both were
already present in the preceding run, because the commit compared against a stale *before* file
rather than the immediately preceding one. Its config-read result (98 -> 2, 101 -> 3) stands and
is the change's real effect, and no cumulative figure is affected. Recorded because an A/B
result depends on which *before* file it was compared against.

**A recorded prize is a measurement of a tree that no longer exists.** Finding 22 recorded
-14.2% on bulk create and was re-measured at -7.7% when it came to be implemented, because
finding 31 had since removed one of the two queries per record it was going to save. Nothing in
the ledger flagged the overlap. Re-measure anything deferred at the point of implementing it,
not only at the point of proposing it.

**A change can fail on performance and still be worth making.** That same finding was declined
on write-path performance and landed on storage: `object_data` is 11.8% of the changelog table,
which matters wherever `CHANGELOG_RETENTION` is long. The measurement was right; the
recommendation drawn from it was too narrow.

## Getting the HTTP instrument right

Tier 2 originally ran 30 requests at concurrency 4 and reported the 95th percentile. On a host
that holds in-process timings to +/-4%, that configuration spread **25.3% at the median and
41.4% at worst** across three rounds of identical code. The p95 was drawn from roughly two
samples in the tail, against three uwsgi workers sharing two physical cores, in integer
milliseconds.

At concurrency 1 reporting the median, the same 38 endpoints on the same code spread **1.6% at
the median and 5.7% at worst**, a sixteenfold improvement in precision from changing the
instrument, with no change to the hardware.

The two independent instruments now agree. HTTP at concurrency 1 runs a median **1.08x**
in-process across fourteen shared endpoints, range 1.04-1.19x, which is what HTTP parsing, WSGI
and response transfer should cost on top of identical Django work. Under the old configuration
they disagreed by 4-6x and nothing could say which was right.

Earlier still, `ui.device.interfaces` reported a 66 ms HTTP p95 against a 607 ms in-process
median. An API token authenticates DRF only, so every UI endpoint had been answering **403**
with a 299 KB permission-denied page that renders in ~50 ms. cassowary counts an answered
request as a success, and the driver recorded no status code at all, so the run reported 38
endpoints and zero failures while 27 of them timed an error page. Tier 2 now probes each
endpoint once before timing it, carries the status alongside the timing, and refuses to time
anything that does not answer 200.

## Still open

The live queue is `perf/queue.md`, which carries the ordering and the reasoning behind it. The
current head of it:

- `dcim.cable` is the most expensive create on the write surface, at roughly **220 queries and
  0.34 seconds per cable**, linear from 25 to 96 per request. It is also the worst model on the
  read side: `dcim.cable?depth=1` issues 159 queries for a page of 25, of which 145 are repeats
  of a shape already seen, and 50 of those are one `SELECT` on `dcim_device`, two per cable, one
  per termination's parent. Depth 0 is 7 queries with no duplicates, so all of it is nested
  serialization. Known shape, known mechanism, not yet attributed to a fix.
- `ipam.ipaddresstointerface` at 65.6 marginal queries per created object, the top of the write
  ranking, on a 2,937-row table.
- Row-scaling N+1s that memoization cannot help. `PowerFeed.utilization` and the prefix
  hierarchy column both grow linearly with row count. Every fix on this branch removes
  *repeated* work; these need a different shape.
- `api.prefix.list` spends 13.7 ms planning a query that executes in 1.3 ms, re-planned every
  request, about 21% of that endpoint's time. Explicitly not an index: an index makes planning
  worse. The lever is prepared-statement reuse or narrowing the serializer's `select_related`
  fan-out.
- An affordance-adoption screen has not been built. Five findings on this branch exist only
  because an affordance was added without its call sites being updated to use it. Screening
  endpoints by cost finds symptoms; screening affordances by adoption finds causes, over a much
  smaller search space.
- No stock-vs-branch aggregate read-path wall clock has been taken. Round one and round two ran
  on different hosts, so the only cumulative read-path figure that survives the host change is a
  query count. The measurement is affordable on the current host and has not been run.
- The demo-instance gap needs re-measuring with one instrument. The 4.7x figure in the report
  compares in-process medians here against HTTPS-to-uwsgi there. Both sides can now be taken at
  concurrency 1 under uwsgi.

## Caveats

- Test coverage is complete for the tree these numbers describe. A whole-suite run of **17,505
  tests in 8,151 s** passed against the exact tree every measurement below was taken against:
  `OK (skipped=663, expected failures=1)`, zero failures, zero errors, product tree content hash
  `90dbd96ffbeae6d1b9ed83b3e1042af10f95cdcc6fcc19e4a734be0d30ff36bf`. The hash is quoted rather
  than a commit because that is what `arm_control.sh` proves before each arm, so the suite and
  the measurements are attached to the same artefact rather than to a branch name that moves.
  The tree has since gained two test modules, which changes that hash, since test files live
  under `nautobot/` and `arm_control.sh` hashes every `.py` under it. Nothing measured here
  moved: the difference is test code only, and it was added after every figure above was taken.
- The new request-scoped state is now tested, and the third-party coupling fails loudly. It was
  neither before. `CachingTemplateColumnCouplingTestCase` renders the same cell through the
  caching column and through django-tables2's stock `TemplateColumn` and asserts the output is
  identical, so an upgrade that changes `render()` semantics breaks a test rather than producing
  wrong cell content. Writing it surfaced a coupling nobody had recorded: the reimplementation
  depends on `kwargs["bound_row"]` reaching `get_context_data()`, not only on that hook
  existing, so there is now an explicit test for the kwarg contract.
  `NaturalKeyFieldLookupsCacheScopeTestCase` covers the other half: the cache is absent outside
  its scope, present inside, removed on the exception path, reused rather than shadowed when
  nested, and returns the same answer as the uncached path for Device and Location. 11 tests,
  all passing. The aggregate is still a lot of new caching for one review, and it still deserves
  to be read as a set.
- Absolute wall-clock figures are not comparable across machines or datasets. Round one ran on
  Apple Silicon, round two on an Intel i5-8259U with turbo disabled, 4-6x slower and far more
  precise. Relative comparisons hold; absolute ones do not.

## Further caveats

- The write screen measures a floor, and the factor is now bounded for most of the surface.
  `perf/scripts/screen_writes.py` populates required fields only. Re-running it with
  `--include-optional` and comparing 73 models measured both ways puts the median understatement
  at **1.00x**, so for the typical model the floor is the cost, with a mean of 1.31x and a
  maximum of 4.96x; 10 of 73 exceed 1.5x. **What it cannot bound is the part that matters**: 27
  models could not be measured with populated payloads at all, including `dcim.cable`,
  `dcim.interface` and `dcim.powerfeed`, because a model with expensive optional relations is
  both the most understated and the hardest to build a payload for. `dcim.cable` is quantified
  by another route: finding 40 measured a connected cable at ~220 queries against the floor's
  44.9, which is 4.9x and consistent with the top of the measured range. See finding 47.
- Silk middleware remains in the request chain. It is inert without a session flag and constant
  across runs, so it does not distort relative comparisons, but it is present in every absolute
  number here.
- UI response bodies are not byte-stable between identical requests, so HTML endpoints are
  compared on query count and status rather than content.

---

Measured against `nautobot/next` at `ce01a0464` (3.3.0a0) on an isolated stack with pinned resources. Harness, workload definition, findings and baseline data are on the `perf/experiments` branch under `perf/`; every scenario and operation above is reproducible with `perf/scripts/run_experiment.sh`. Instruments, baselines and per-finding working are in `perf/methodology.md`.

Both files are generated. Edit `perf/findings/*.yml` for numbers and `perf/report.template.md` or `perf/methodology.template.md` for narrative, then run `perf/scripts/build_report.py`. `--check` exits non-zero when they have drifted apart.
